{
  "title": "Countdown (Top N)",
  "description": "Top-10 list revealed #N → #1 with the #1 slam. Use odd-feeling lists; tease #1.",
  "fields": {
    "kicker": { "type": "string", "label": "Kicker" },
    "hook": { "type": "string", "label": "Hook" },
    "teaseHook": { "type": "string", "label": "#1 tease line" },
    "cta": { "type": "string", "label": "Outro CTA" },
    "unit": { "type": "string", "label": "Value unit (e.g. ×, $B)" },
    "items": { "type": "array", "label": "Items (ranked, #1 last or first per scene logic)", "item": {
      "name": { "type": "string", "label": "Name" }, "value": { "type": "number", "label": "Value" } } }
  }
}
