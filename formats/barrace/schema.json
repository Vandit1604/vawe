{
  "title": "Bar race (overtake over time)",
  "description": "Sorted bars racing across time frames — the overtake is the payoff.",
  "fields": {
    "hook": { "type": "string", "label": "Hook" },
    "hookSub": { "type": "string", "label": "Hook subtitle" },
    "cta": { "type": "string", "label": "Outro CTA" },
    "unit": { "type": "string", "label": "Value unit (e.g. $B)" },
    "items": { "type": "array", "label": "Competitors", "item": {
      "name": { "type": "string", "label": "Name" }, "color": { "type": "string", "label": "Bar color (hex)" } } },
    "frames": { "type": "array", "label": "Time frames", "item": {
      "label": { "type": "string", "label": "Frame label (e.g. 2024)" },
      "values": { "type": "array", "label": "Values (one per item, in order)" } } }
  }
}
