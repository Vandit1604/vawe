# Brief: vawe-oblique

The five lines a requester gives, recorded because they were never written down. This film was briefed
in conversation across a long session, and a brief that exists only in a transcript is a brief nobody
after today can check the film against.

The same five lines outside the fence, because `invent-look` strips fenced code before reading a
brief and a brief only it can parse is a brief only it can use.

SUBJECT: the vawe engine itself, how you write a film, build it, and get a video
DATA: this repo, every number on screen read off it
PAYOFF: the same input gives the same pixels, every time
AUDIENCE: developers and technical marketers who make product video by hand today
FEELING: modern, engineered, confident. A tool that is precise and looks it.

```
SUBJECT   the vawe engine itself: how you write a film, build it, and get a video
DATA      the repo. Every number on screen is read off it, never remembered:
          the canvases from core/layout/safe.js ASPECTS, the CLI output from a real
          ./bin/vawe run, the mark from site/public/assets/favicon.svg, the JSON from
          this film's own beats
PAYOFF    the same input gives the same pixels, every time
AUDIENCE  developers and technical marketers producing product video by hand today
FEELING   the together.ai launch film: near-white ground, one blue, white planes
          separated by depth and never a border, tilted surfaces running off the frame,
          sentences revealed word by word with one word in the accent
```

Two lines the requester never has to give, and the author always does:

```
SPECTACLE beat 4 · the plate layer · the rendered frame landing at 58% of the canvas,
          the largest object in the film by a factor of 1.5 over the next beat
NOT       no gradient hero · no fade on every cut · no stock photography · no invented
          statistic · no dark surface anywhere · no border where a shadow does the work ·
          no square corner on a card · no chat UI in a film about a command line
```

## What the reference gives, and what it does not

The reference is a design language, not a script. Its GRAMMAR travels: the ground, the depth, the
tilt, the accent word. Its OBJECTS do not: their hero is a chat input because their product is chat.
Every beat that borrows from it carries a `borrows:` line naming what the device becomes here, and
beat 3 is the one that had to be corrected after it borrowed a shape without its role
(`engine-doctrine/MISTAKES.md` #596).

## The four things a brand site would have given, manufactured

1. **Taste anchor**: the reference above, plus `themes/vawe.json`, which is the real site's own palette
   and faces (Anybody, JetBrains Mono, cobalt #2563eb on white).
2. **Real assets**: the repo's own favicon, its own scene JSON, its own CLI output. Nothing invented.
3. **Story spine**: write → build → get, carried as a rail on beat 1 and closed on beat 7.
4. **Real copy**: every on-screen line is true of the engine as it stands today.
