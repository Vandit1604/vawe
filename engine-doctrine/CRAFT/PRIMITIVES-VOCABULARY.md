---
when: "you want to see every primitive the engine has, the plain words that find it, and the sentence that says what it does"
answers: "the full word-action listing every defineRegistry registry carries, grouped by registry, graded the same way make word-action grades it"
group: reference
---

# PRIMITIVES VOCABULARY: every registry entry, its words, its action

> GENERATED by `scripts/site/vocab-catalog.mjs` (`make vocab`) from `registries()` in `core/registry/registry.js`. Do not edit.

Every row is one primitive from one `defineRegistry` call. **words** are its `aka` (never printed
elsewhere, folded into search only); **action** is its `blurb`. A row with either column blank
fails `make word-action`: `words` needs 2+ phrases, `action` needs a stated number or a full
sentence naming the mechanism. See `quality/gates/word-action.mjs` for the exact contract.

## adjustment  `[kind]`

| name | words | action |
|---|---|---|
| `bloom` | _missing_ | a real GLOW over everything beneath: the light spills past its edges and the subject stays sharp. The one kind that composites back rather than replacing |
| `blur` | _missing_ | soften everything beneath, in pixels. The rack-focus of a whole beat, not of one layer |
| `brighten` | _missing_ | lift everything beneath, as a percentage |
| `contrast` | _missing_ | harden the tones beneath, as a percentage |
| `darken` | _missing_ | dim everything beneath, as a percentage |
| `desaturate` | _missing_ | drain the colour beneath. 1 is fully grey |

## ambient shader  `[shader]`

| name | words | action |
|---|---|---|
| `aurora` | northern lights, colour wash, flowing colour glow | drifting colour aurora (moves) |
| `auroraCurtain` | _missing_ | six vertical curtain lines undulating top to bottom, each drifting sideways on its own noise offset, fading warm to cool along its length. ported from pbakaus/radiant |
| `auroraVeil` | _missing_ | seven wide aurora ribbons undulating over a starfield, with a frosted ice ground plane reflecting them below the horizon. the richest field here after bands. ported from pbakaus/radiant |
| `bands` | _missing_ | a ramp repeated over a scalar field (rotated panels, concentric arcs or nested rounded boxes) tinted by a gradient with a shaped light behind it. the most dialled effect here; engine-doctrine/LIGHTFIELD.md |
| `barrel` | _missing_ | the LENS, not the picture: a corner vignette with a faint violet chromatic fringe riding the far edge only |
| `chromaticBloom` | _missing_ | twelve luminous colour orbs (five vivid, seven dim) drifting and blending on black, each on its own noisy orbit, with a vignette and grain over the top. ported from pbakaus/radiant |
| `crt` | _missing_ | a tube: 4px RGB phosphor stripes, scanlines, a corner vignette and a refresh bar rolling down. an OVERLAY |
| `curlSmoke` | _missing_ | a rising plume of ink or smoke that rolls into vortices as it climbs, its filaments stretching and folding. slow, continuous, and never repeating; the one field here with real fluid motion rather than a drifting pattern |
| `domainWarp` | _missing_ | marbled ink: fBm sampled through a domain that is itself two levels of fBm, so the field folds back over itself. the classic warp, and the only field here with real interior structure |
| `dotCrawl` | _missing_ | the NTSC artifact: a fine diagonal chroma lattice creeping one subcarrier phase per frame, concentrated where there is detail. an OVERLAY |
| `drift` | _missing_ | seven big soft bokeh discs rising up the frame and blending as they pass |
| `filmGrain` | _missing_ | grain re-struck 24 times a second per ~2px cell, plus dust specks; bright or dark only, never mid-grey, so contrast survives. an OVERLAY |
| `flow` | _missing_ | a soft mesh gradient: three big blobs drifting slowly over a vertical wash, the premium default |
| `gateWeave` | _missing_ | a projector gate: the soft dark frame border, dust re-struck each projected frame and a hair that catches for a second or two, all riding ONE drifting offset so the picture appears to float |
| `godRays` | _missing_ | shafts of light: sun through a canopy, beams through a window, crepuscular rays. the light sits just off the top edge, a drifting cloud of leaves breaks it into blades, and dust turns slowly inside the bright ones. the deepest field here, because the beams recede toward one point. it is BRIGHT through the middle, so drop `intensity` toward 0.4 before putting white type over it |
| `heatShimmer` | _missing_ | rising warm haze in fine wavy bands, strongest low in the frame and thinning as it climbs. self-generated. It does not warp what is beneath it |
| `kaleidoscope` | _missing_ | a 6-fold mirrored mandala turning slowly and fading out toward the corners. A symmetric field of its own, never a mirror of your content |
| `laserLabyrinth` | _missing_ | six volumetric light cones sweeping down from just above the top edge through drifting fog, three dim and far, three bright and near with a rhythmic beat snap. ported from pbakaus/radiant |
| `lightLeak` | _missing_ | three warm blobs drifting in from the edges on a loop, tinted from the palette. an OVERLAY |
| `matrixDecode` | _missing_ | digital rain: near-white heads falling down 44 glyph columns at per-column speeds, each dragging a fading tail. palette stop 0 tints it |
| `metaballs` | _missing_ | five signed-distance circles merging and parting on a polynomial smooth minimum, so they fuse into one body instead of overlapping |
| `mist` | _missing_ | near-still layered noise haze. The quietest field here, for a backdrop that must move without being noticed |
| `nebula` | _missing_ | deep-field gas clouds from three octaves of noise with a hot core, dusted with twinkling stars off a hashed grid |
| `plasma` | _missing_ | two crossed sine waves interfering into a slow two-tone swell |
| `ripple` | _missing_ | gentle water caustics: three rings of cool light expanding and overlapping |
| `vhs` | _missing_ | tape: scanlines, magenta/cyan chroma snow, dropout streaks and a soft tracking band creeping up. an OVERLAY, place it ABOVE content |
| `voronoi` | _missing_ | cellular (Worley) noise: seeded cells drifting on their own loops, each one flat-tinted, with a lit line along every shared border |

## anim  `[anim]`

| name | words | action |
|---|---|---|
| `clock` | _missing_ | radial sweep from 12 o'clock, clockwise |
| `defocus` | blur out exit, soft focus fade | leave through blur (dense/faces) |
| `fade` | _missing_ | opacity only |
| `iris` | _missing_ | circular iris opens from the centre of the layer |
| `lift` | _missing_ | travels further than `pop` and settles alive, faces, avatars, a staggered row of cards |
| `none` | _missing_ | no move and no fade. The layer just appears at its window edges |
| `pop` | _missing_ | springs past its size and settles back, a small confident arrival for a chip, a badge or an icon |
| `rise` | _missing_ | translate up + fade in |
| `scale` | zoom in, grow from small | punch in from small (overshoot) |
| `slide-down` | _missing_ | enters from below; as an `out`, leaves downward |
| `slide-left` | _missing_ | enters from the left edge; as an `out`, leaves toward it |
| `slide-right` | _missing_ | enters from the right edge; as an `out`, leaves toward it |
| `slide-up` | _missing_ | enters from above; as an `out`, leaves upward |
| `up` | _missing_ | translate up + fade in, the alias of `rise` |
| `wipe` | _missing_ | clip reveal grows rightward. The default direction, same as `wipe-right` |
| `wipe-down` | _missing_ | clip reveal grows downward from the top edge |
| `wipe-left` | _missing_ | clip reveal grows leftward from the right edge |
| `wipe-right` | _missing_ | clip reveal grows rightward from the left edge |
| `wipe-up` | _missing_ | clip reveal grows upward from the bottom edge, so a column grows upward instead of appearing |

## background preset  `[bg[].preset]`

| name | words | action |
|---|---|---|
| `accent` | _missing_ | the brand accent as a radial with rippling dots and a slow spotlight (moves), the loud brand field |
| `accentPlain` | _missing_ | the brand accent as a clean full-bleed field, grain only. FLAT, for plain sites whose hero is one colour |
| `aurora` | northern lights, colour wash, flowing colour glow | drifting colour aurora (moves) |
| `black` | _missing_ | literally #000000, no tint, no wash, no grain. FLAT, when the only light in the film is the subject itself |
| `blobs` | _missing_ | the airier light wash: smaller, separated pools with white between them (moves). No grid: write `grid: true` on a softwash fx if you want the blueprint rules |
| `brandglow` | pulsing accent light, soft breathing glow, radial pulse | breathing accent glow |
| `constellation` | _missing_ | drifting connected nodes (moves), telemetry/data feel |
| `dark` | _missing_ | a plain dark radial, no dots. FLAT, the quiet backdrop for busy content |
| `deep` | _missing_ | the deepest plain radial, no dots. FLAT, when the content must own the whole frame |
| `dotmatrix` | _missing_ | a light field ruled by evenly spaced dots whose size waves across it, the printed halftone look (moves) |
| `gradient` | _missing_ | an agent-controlled colour gradient backdrop, linear/radial/conic from your own hex colours, angle and stops, or a named recipe (`opts.recipe`). FLAT unless `opts.kind` is "mesh", which moves it |
| `gradientWash` | _missing_ | one big saturated pool bleeding off a corner into white, a mesh gradient (moves), light and premium |
| `ink` | _missing_ | dark radial with slow accent-tinted dots pulsing in place (moves), for a clean flat dark use `plain` + value:"dark" |
| `liquid` | _missing_ | folds of the brand hue against true black (moves). It OWNS the frame, so quiet type on it and nothing else |
| `mesh` | _missing_ | soft gradient mesh (dark/saturated, check contrast) |
| `metallic` | _missing_ | vertical light rods with a travelling SHIMMER (brushed metal / lit equaliser), dramatic dark bg, brand-coloured |
| `metallicSheen` | _missing_ | the quieter metallic: fewer, slower rods with a sweep crossing them (moves), a dark field type can sit on |
| `paper` | _missing_ | the paper gradient with grain only, FLAT, the white-first default when the motion lives in the content |
| `paperDots` | polka dot, dot grid pattern, light dotted texture | faint drifting dot grid (light) |
| `paperShapes` | light geometric pattern, subtle shapes texture | faint drifting geometric shapes (light, subtle) |
| `plain` | solid color, flat color, no texture backdrop | flat theme field |
| `soft` | _missing_ | gentle light radial with faint accent rings and discs drifting over it (moves) |
| `spotlight` | _missing_ | a deep field with one soft pool of light wandering across it on a slow cycle, the eye follows the bright patch (moves) |

## blend mode  `[mixBlend]`

| name | words | action |
|---|---|---|
| `color` | _missing_ | takes tint and richness from the layer and brightness from the backdrop. The way to recolour a photo |
| `color-burn` | _missing_ | darkens the backdrop hard wherever the layer is deep, crushing the shadows |
| `color-dodge` | _missing_ | brightens the backdrop hard wherever the layer is pale, blowing the highlights out |
| `darken` | _missing_ | keeps whichever is darker at every pixel, the layer or what is behind it |
| `difference` | _missing_ | subtracts one picture from the other, so matching areas go black and opposites invert. The classic glitch look |
| `exclusion` | _missing_ | the same subtraction with the contrast taken out, greying the midtones instead of driving them to black |
| `hard-light` | _missing_ | as if a harsh lamp shone through the layer: strong, unsubtle contrast |
| `hue` | _missing_ | takes only the colour angle from the layer and keeps the backdrop's brightness and richness |
| `lighten` | _missing_ | keeps whichever is lighter at every pixel, the layer or what is behind it |
| `luminosity` | _missing_ | takes only brightness from the layer and keeps the backdrop's tint. The inverse of `color` |
| `multiply` | _missing_ | darkens everything: white drops out and the rest deepens, the way ink sits on paper or two slides stack |
| `normal` | _missing_ | no blending at all: the layer simply covers what is behind it. The default, and the way to turn a blend off |
| `overlay` | _missing_ | deepens the dark areas and brightens the light ones at once, so contrast rises against the backdrop |
| `plus-lighter` | _missing_ | adds the two pictures together, so light over light runs to white. For glows and additive light |
| `saturation` | _missing_ | takes only the richness from the layer and keeps the backdrop's tint and brightness |
| `screen` | _missing_ | lightens everything: black drops out and the rest brightens, the opposite of multiply, like two projectors on one wall |
| `soft-light` | _missing_ | as if a diffuse lamp shone through the layer: a gentle wash rather than a hit |

## camera dial  `[cameraBlur (a top-level boolean)]`

| name | words | action |
|---|---|---|
| `cameraBlur` | _missing_ | CAMERA MOTION BLUR: blur the frame when the camera whips. Every layer smears by its velocity RELATIVE TO THE CAMERA, so a fast pan streaks the whole frame and a layer travelling with the camera stays sharp. Off by default; the film's `shutter` (degrees) says how much, and one layer opts out with `motionBlur: false`. A zoom and a roll are radial and are not modelled |

## camera move  `[cameraMove.move]`

| name | words | action |
|---|---|---|
| `cameraShake` | _missing_ | an IMPACT: a decaying ~16Hz shake pre-sampled at author time to one key per frame, then 0.1s of eased recovery so the frame LANDS instead of stopping |
| `diveIn` | _missing_ | zoom INTO a target point (it travels to centre) |
| `dollyZoom` | _missing_ | THE VERTIGO SHOT: the lens ramps while the camera holds its distance, so the subject on the picture plane keeps its exact size and the world BEHIND it rushes in or falls away · the only move here that changes the relationship between planes rather than the framing, and it needs layers standing at a `plane` depth or there is nothing to counter-scale against |
| `driftHold` | handheld, breathing camera, operator float, not locked off | a held frame that is never dead: a sub-12px Lissajous micro-drift, x and y at different frequencies so it breathes instead of walking a diagonal |
| `followCursor` | _missing_ | THE CAMERA FOLLOWS THE CURSOR: derived from the `path` and `clicks` on a `cursor` layer, so the pointer stays the single owner of where the camera goes. It pushes toward the spot the pointer is about to click, arrives just BEFORE the click, holds across it and releases. Clicks too close in time or space share one framing, so six clicks are never six crash zooms |
| `followLayer` | _missing_ | CAMERA TRACKS A LAYER BY ID, at RENDER time, off the same live-box accessor `follow` (the layer track) reads. Holds still while the target sits inside a soft margin of frame centre, translating only the minimum to keep it in once it would cross the edge, rather than rigidly re-centring it every frame (which reads as the world sliding, not the camera tracking). Zoom is HELD (`to`), not framed. The one move here `bakeCameraMove` resolves onto `data.cameraFollow`, not `data.camera`, and it cannot be composed with another leg |
| `hold` | _missing_ | the camera is LOCKED OFF: zero motion, on purpose. Not driftHold, which keeps breathing; this emits no keyframes at all, the explicit way to declare "no camera" instead of leaving `camera:` blank |
| `multiPhase` | _missing_ | chain legs into one journey (push, hold-drift, settle) |
| `orbit` | _missing_ | a gentle 3D swing around the frame (ry through 0) |
| `panFollow` | _missing_ | camera pans to track downward-growing content (terminal) |
| `punchIn` | _missing_ | a crash zoom: the frame accelerates AT you (easeInExpo), recoils past its resting scale, then rings back elastic. the only move here that is not a `.out` |
| `slowPush` | _missing_ | gentle continuous zoom in (the frame stays alive) |
| `travel` | _missing_ | station-to-station flight between points in STAGE coords. THE CAMERA AS THE TRANSITION (no cut) |
| `truck` | _missing_ | plain lateral travel, linear, so it reads as tracking rather than a lurch |
| `workspaceZoomOut` | _missing_ | pull back from a detail to reveal the whole |

## camera word  `[cameraMove.move]`

| name | words | action |
|---|---|---|
| `circle` | _missing_ | the camera orbits around the subject, so the subject stays put and the view changes. |
| `dive` | _missing_ | a fast move INTO the frame, ending close on one element. For entering a product surface. |
| `drift hold` | _missing_ | a very slow move on a held frame, so a static shot is never fully still. |
| `follow` | _missing_ | the camera pans to keep a moving subject in frame. |
| `pan stations` | _missing_ | the same touring move, named for what it does: stopping at each station in order. |
| `pull back` | _missing_ | the camera retreats to reveal the wider context around what you have been looking at. |
| `punch in` | _missing_ | an abrupt jump closer, not a glide. Punctuation: it hits on a beat. |
| `push in` | _missing_ | the camera moves closer, slowly and continuously. Tightens attention without a cut. |
| `shake` | _missing_ | a short handheld jolt. An impact, a hit, a moment of instability. |
| `slow push-in` | _missing_ | the same slow move closer, spelled the way a storyboard usually spells it. |
| `sweep` | _missing_ | the camera trucks sideways across the scene, revealing what was beyond the edge. |
| `tour` | _missing_ | a multi-leg move that visits several places in turn, one continuous shot rather than cuts. |
| `ui focus zoom` | _missing_ | a dive aimed at one control or panel, for showing exactly where a click lands. |
| `zoom out` | _missing_ | the same widening move, spelled the way most people ask for it. |

## canvas fx  `[canvasFx]`

| name | words | action |
|---|---|---|
| `ascii` | _missing_ | one monospace glyph per cell, picked off a light-to-dark ramp; phosphor green on near-black. this pass IS the terminal look, so it ignores the theme |
| `crosshatch` | _missing_ | pencil / engraving: diagonal strokes whose density STEPS up with darkness (one, then a cross, then a vertical), ink on a warm paper stock |
| `dither` | _missing_ | Bayer 4x4 ordered dither down to TWO tones, theme ink on theme paper. No greys anywhere |
| `edgeDetect` | _missing_ | a Sobel magnitude: the outline only, bright lines on a dark ground. the base of the `blueprint` preset |
| `halftone` | newspaper print, newspaper dots | one ink dot per cell on a paper ground, its radius growing with that cell's darkness |
| `mosaic` | _missing_ | pixelate: every cell filled flat with its own average colour |
| `pixelSort` | _missing_ | sorts each contiguous BRIGHT run of a scan line by luma. The signature glitch smear, and deterministic: the span is bounded by a threshold, nothing is random |
| `stipple` | _missing_ | seeded ink dots on paper, denser where the source is dark, each jittered inside its own cell |

## caption style  `[captionStyle]`

| name | words | action |
|---|---|---|
| `clipWipe` | _missing_ | LINE-level: an accent copy of the line is revealed left to right, the wipe front tracking the spoken word rather than wall-clock time |
| `flipUp` | _missing_ | the word hinges up from edge-on · an upcoming word sits at -90deg, which is invisible without being dim, so it costs the text no contrast at all |
| `focusPull` | _missing_ | a rack focus: an upcoming word sits 2.8px out of focus and resolves sharp as it is spoken, a spoken one settling back to a soft 1px · the one style that says its state with focus rather than with ink, light or size |
| `ghostSplit` | _missing_ | two offset ghosts converge as the word is spoken, the accent one way and a muted ink the other · a split reads as a split from the OFFSET, not from being red and blue, so it stays on the theme |
| `highlight` | marker highlight, highlighter sweep, underline sweep | marker highlight sweep |
| `inkFill` | _missing_ | the karaoke fill runs through the GLYPHS rather than behind them, full ink trailing a narrow accent front · the word body is never the accent, so its contrast is that of the plate itself |
| `kineticSlam` | _missing_ | the word lands at 1.22 with its tracking open and settles cubically to 1, so the travel is all in the first third of its window and the rest holds still |
| `letterRise` | _missing_ | the letters of the whole line rise the last 12px into their slots as they are spoken, one per CHARACTER · not a second typewriter: the line is present from the first frame, where typeOn assembles it out of nothing |
| `neonEdge` | _missing_ | the accent lives only in a halo: the spoken word blooms and settles, earlier words keep a quieter glow, upcoming words hold a 76% text-mix with no light at all |
| `pillKaraoke` | _missing_ | a pill fill sweeps left to right through the line, the accent mixed 42% into the bg so the bg stays dominant |
| `readerFocus` | _missing_ | a teleprompter: three ink levels and three scales, upcoming at 76% and 0.90, spoken at 88% and 0.96, the current word full ink at 1 |
| `scramble` | _missing_ | the letters settle out of noise, left to right · the only style that rewrites the text rather than its style, so it carries the same carve-out clipWipe does |
| `typeOn` | _missing_ | a typewriter, per CHARACTER: an unarrived letter holds its space at visibility:hidden so the line never reflows, and the current one carries an inset accent caret |
| `underlineDraw` | _missing_ | a 4px accent rule draws under each word as it is spoken and stays, the quiet sibling of highlight: it sits below the ink, so it costs the text no contrast |
| `waveRide` | _missing_ | one crest per word, ridden as it is spoken · a half-sine is bounded by the word window, where a looping wave would simply be cut off by it |
| `weightShift` | _missing_ | the spoken word RAMPS along the font's own wght axis to 800 with a small rise-and-settle bump, the rest hold 600 at a 76% text-mix |
| `weightWave` | _missing_ | a crest of WEIGHT travels the line, each word riding the font's wght axis from 500 to 900 and back · the one register a static font cannot fake, and the line breathes as the crest passes |
| `wordFlash` | _missing_ | ONE word on screen, swapped whole at the next onset, landing at 1.14 and settling cubically · the default of short-form video, and it needs no dimming because the unread words are absent, not faint |
| `wordSlide` | _missing_ | the same one-word swap arriving from 26px below instead of from scale · for a film already moving vertically, where a second unrelated motion would fight it |

## composition  `[comp]`

| name | words | action |
|---|---|---|
| `commaSplit` | _missing_ | a delimited line pulls itself apart into a table: each comma flies to the gutter, shrinks to a point, and a column rule grows out of that same point, so the delimiter visibly becomes the structure |
| `pipelineFlow` | _missing_ | staged pipeline: cards pop in, connectors draw, a token travels each link, a check draws on (one hand-authored timeline) |

## cursor style  `[style]`

| name | words | action |
|---|---|---|
| `arrow` | mouse pointer, arrow cursor, default cursor | the default macOS pointer arrow, unchanged pixels from before this registry existed |
| `block` | terminal cursor, block caret, block cursor | a filled terminal cell that blinks on a fixed cadence, a block text cursor |
| `hand` | pointer hand, hand cursor, link cursor, clickable cursor | a pointing hand, for hovering a clickable button or link in a product demo |
| `ibeam` | i-beam, text cursor, text selection cursor | a text-selection I-beam, for hovering editable or selectable text |

## cut  `[cut]`

| name | words | action |
|---|---|---|
| `barn` | _missing_ | barn doors open from the centre outward, cinematic opener; masks, so a whole-frame cut needs sceneUnits |
| `blinds` | _missing_ | venetian slat mask sweeps open, editorial reveal; masks, so a whole-frame cut needs sceneUnits |
| `blur` | _missing_ | resolve out of blur, calm, premium |
| `clock` | _missing_ | clock-hand sweep reveal, masks, so a whole-frame cut needs sceneUnits |
| `collapse` | _missing_ | vertical fold down to a line, terminal and data beats |
| `cube` | _missing_ | perspective hinge with travel, the beats turning like faces of a cube |
| `drop` | _missing_ | falls in from above under gravity, leaves by falling away |
| `fade` | _missing_ | opacity only, masks, so a whole-frame cut needs sceneUnits |
| `flip` | _missing_ | perspective hinge flip about an edge, cards and panels |
| `iris` | _missing_ | circular reveal growing from a point (cx/cy), masks, so a whole-frame cut needs sceneUnits |
| `jitter` | _missing_ | decaying deterministic shake, alarm and glitch beats only |
| `letterbox` | _missing_ | cinema curtains open and close top and bottom, cinematic opener; masks, so a whole-frame cut needs sceneUnits |
| `matchCut` | _missing_ | a GRAPHIC MATCH: both beats are clipped to the same circle at cx/cy, the content swaps inside it at the midpoint, and the shape opens back out. The eye follows one form across the join. Unlike `iris`, the shape belongs to BOTH shots, which is what makes it a match rather than a reveal. You still have to place the two subjects at the same size and spot; masks, so a whole-frame cut needs sceneUnits |
| `none` | _missing_ | no transition at all, the beats simply replace each other. Masks nothing and moves nothing, so a whole-frame cut needs sceneUnits |
| `punch` | _missing_ | scale burst, the leaving beat bursts past the camera, product focus |
| `rise` | _missing_ | translate up + fade in |
| `riseBlur` | _missing_ | slow rise through heavy defocus, premium slow beats |
| `roll` | _missing_ | tilts in from a corner and settles level |
| `skewWhip` | _missing_ | sheared throw whose shear straightens as it lands, velocity you can read in the letterforms, same-background beats only |
| `slide` | _missing_ | the frame travels one way, dir-aware. The plain workhorse, between same-background beats only |
| `softiris` | _missing_ | feathered circular reveal from a point (cx/cy), masks, so a whole-frame cut needs sceneUnits |
| `softwipe` | _missing_ | feathered wipe, a 20%-wide gradient band instead of a hard line; masks, so a whole-frame cut needs sceneUnits |
| `spin` | _missing_ | rotate in with a scale settle, logos, badges, seals |
| `squeeze` | _missing_ | smear-stretch along the travel axis, a speed ramp you can see |
| `whip` | _missing_ | motion-blurred directional throw, momentum, between same-background beats only |
| `wipe` | _missing_ | hard directional reveal, playful, "notice the cut"; masks, so a whole-frame cut needs sceneUnits |
| `zoom` | _missing_ | push-through: the leaving beat shrinks away, the arriving one lands from too close, product focus |

## cut timing  `[cutTiming]`

| name | words | action |
|---|---|---|
| `brake` | _missing_ | decelerates in. The entrance curve: it arrives slower than it set off |
| `linear` | _missing_ | no easing at all. A machine, a wipe with no personality, a ticker |
| `out` | _missing_ | decelerates into place. The default feel for something appearing |
| `pop` | _missing_ | overshoots past the mark and comes back. Playful, use once |
| `ramp` | _missing_ | the editor's slow-fast-slow speed ramp. The one to reach for on a whip or a camera throw |
| `rush` | _missing_ | accelerates away. The exit curve: it leaves faster than it left rest |
| `smooth` | _missing_ | eases at both ends: reads as travel rather than as arrival |
| `snappy` | _missing_ | decisive, no overshoot. The cut lands and stops |
| `spring` | _missing_ | a damped-spring overshoot that settles, physical life for something landing: a badge, a chip, a number |

## depth  `[depth]`

| name | words | action |
|---|---|---|
| `back` | _missing_ | behind the picture plane: the layer the camera passes, the one that gives the move its parallax. Held, 0.73x the rate |
| `far` | _missing_ | the far plane: a backdrop, a wall, a field the subject stands in front of. Held at its laid-out size, moved by the camera at 0.57x the picture plane's rate |
| `front` | _missing_ | just in front of the picture plane: a caption or a chip that rides ahead of the subject. Held, 1.18x the rate |
| `near` | _missing_ | nearest the eye: the thing that crosses the frame fastest and leaves it first. Held, 1.39x the rate |

## destination  `[destination]`

| name | words | action |
|---|---|---|
| `broadcast` | _missing_ | the classic title-safe 90% box, 5% off every edge, for displays that overscan |
| `feed` | _missing_ | Instagram and X in-feed, where the player furniture sits OUTSIDE the media, so nothing is painted over the picture and only the margin applies |
| `reels` | _missing_ | Instagram Reels on 9:16: a 14% right rail, 10% off the top, 22% off the bottom. Looser than tiktok, tighter than shorts |
| `shorts` | _missing_ | YouTube Shorts on 9:16, the most generous phone target: a 13% right rail, 8% off the top, 16% off the bottom |
| `tiktok` | _missing_ | the tightest phone target: a rail down the RIGHT at 16.7% of the width for the action buttons, 12.5% off the top and 30.2% off the bottom for the caption and handle. Serves 9:16 |
| `web` | _missing_ | no platform chrome at all: the whole frame is usable and only the 4% margin applies. A site hero, an X or LinkedIn post, a docs clip |

## duration word  `[enterDur]`

| name | words | action |
|---|---|---|
| `fast` | quick, brief, snappy pace | 0.18s, under a fifth of a second. A payoff snapping into place, a value ticking over. |
| `instant` | snap, no visible motion, immediate | 0.08s, about a twelfth of a second. Barely perceptible as motion: a state flip that must not feel animated. |
| `luxurious` | lingering, held, extended hold | 1.2s, over a second. The pace of a hero moment, and monotonous if more than one beat uses it. |
| `medium` | default speed, normal pace, moderate | 0.3s, BASE_ENTER, the engine's own default entrance. About a third of a second, reach past it on purpose. |
| `slow` | deliberate, unhurried, takes its time | 0.6s, a little over half a second. A thesis line taking its time, a deliberate reveal. |

## easing  `[ease]`

| name | words | action |
|---|---|---|
| `brake` | _missing_ | half the distance in the first quarter, then a long decline to a stop |
| `easeInBack` | _missing_ | pulls the opposite way first, a wind-up, before it goes |
| `easeInBounce` | _missing_ | bounces in place before it departs, like a ball gathering itself |
| `easeInCirc` | _missing_ | creeps, then whips away on a curve that turns hard at the last moment |
| `easeInCubic` | _missing_ | gathers pace with real commitment, stronger than quad. A departure that should read as decided |
| `easeInElastic` | _missing_ | swings in place with growing wobble before it leaves. The loudest departure here |
| `easeInExpo` | _missing_ | barely stirs, then clears frame all at once. The most violent departure available |
| `easeInOutBack` | _missing_ | winds up, crosses, passes the mark and comes back. Playful at both ends and loud |
| `easeInOutBounce` | _missing_ | bounces at both ends. Cartoon physics, and it reads as exactly that |
| `easeInOutCirc` | _missing_ | flat, sudden, flat. Reads as machinery, not as anything with muscle |
| `easeInOutCubic` | _missing_ | the everyday travel curve, soft at both ends. A layer crossing from one place to another on screen |
| `easeInOutElastic` | _missing_ | wobbles at both ends. Almost always more than a film wants |
| `easeInOutExpo` | _missing_ | held, flung, held. The most theatrical of the symmetric curves and the easiest to overuse |
| `easeInOutQuad` | _missing_ | soft at both ends with an even middle, the least dramatic way to carry something across the frame |
| `easeInOutQuart` | _missing_ | unhurried at both ends, fast through the middle. A long journey that should read as deliberate |
| `easeInOutQuint` | _missing_ | lingers at both ends and hurries the middle, dramatic across a long journey |
| `easeInOutSine` | _missing_ | the quietest way to shift anything. Ambient drift, backdrops, anything not asking to be watched |
| `easeInQuad` | _missing_ | leaves gently and keeps gathering pace, the mildest departure here. For an exit that should not feel yanked |
| `easeInQuart` | _missing_ | builds speed hard before it clears frame, a departure with weight behind it |
| `easeInQuint` | _missing_ | almost still, then gone. The most extreme departure short of exponential |
| `easeInSine` | _missing_ | the softest pick-up there is, barely perceptible as gathering pace |
| `easeOutBack` | _missing_ | passes its target and returns, a small pop of emphasis on something landing |
| `easeOutBounce` | _missing_ | drops and bounces to rest, a ball meeting a floor |
| `easeOutCirc` | _missing_ | comes in at speed and flattens off almost immediately, a machined landing rather than a living one |
| `easeOutCubic` | _missing_ | the everyday landing: quick off the mark, unhurried into place. Reach for it when nothing argues otherwise |
| `easeOutElastic` | _missing_ | passes its target repeatedly with a decaying wobble, the springiest landing available |
| `easeOutExpo` | _missing_ | the most violent arrival: effectively there on the first frames, then a long quiet settle |
| `easeOutQuad` | _missing_ | comes in quickly then rolls to a stop, the gentlest landing of the power curves. Small elements, short distances |
| `easeOutQuart` | _missing_ | lands fast and settles crisply, snappier than cubic and it never passes its target. Good on an element arriving |
| `easeOutQuint` | _missing_ | covers nearly all the distance at once then creeps the last of it, a very sharp arrival |
| `easeOutSine` | _missing_ | the softest landing there is, closer to speed fading away than to stopping |
| `hold` | _missing_ | does nothing whatever until the last instant, then jumps. Parks a value across a span rather than moving it |
| `linear` | _missing_ | no acceleration at all, constant speed start to finish. Right for a loop or a marquee, wrong for anything a viewer watches arrive |
| `ramp` | _missing_ | mild and symmetric: gathers pace, crosses the middle at full speed, tails off |
| `rush` | _missing_ | holds back for most of the span then covers the distance late. The exit curve: it leaves in a hurry |
| `settle` | _missing_ | most of the move happens immediately, then it eases the remainder and stops dead |
| `snap` | _missing_ | covers the distance almost at once with a hair of overshoot, the fastest landing that still reads as movement |
| `spring` | _missing_ | passes the mark by a little and falls back, a physical landing with some give in it |
| `spring-bouncy` | _missing_ | visibly passes the mark and swings back, the playful one. One per film at most |
| `spring-stiff` | _missing_ | most of the journey early, then a firm settle with no wobble at all |
| `springEase` | _missing_ | spring shaped but damped flat: nearly arrived at once, then creeping the last fraction |
| `springStiff` | _missing_ | tight and quick with no visible pass beyond the mark, for something that must not look playful |

## effector drive  `[effector.drives{}]`

| name | words | action |
|---|---|---|
| `opacity` | _missing_ | added to 1. `opacity: -0.7` dims a fully affected clone to 0.3 |
| `push` | _missing_ | pixels ALONG the vector from the point to the clone. Positive shoves clones away, negative pulls them in. The only drive that reads the direction as well as the distance |
| `rotate` | _missing_ | proximity spent on rotation, in degrees. `rotate: 25` turns a fully affected clone by 25 |
| `scale` | _missing_ | what an element's proximity to the moving point is SPENT on: added to 1, so `scale: 0.6` swells a fully affected clone to 1.6 |
| `x` | _missing_ | pixels, a sideways offset that does not depend on where the point is |
| `y` | _missing_ | pixels, the same on the other axis |

## effector falloff  `[effector.falloff]`

| name | words | action |
|---|---|---|
| `linear` | _missing_ | proximity: each element reacts by its DISTANCE from the moving point, straight from full at the point to nothing at the radius. The plain ramp |
| `smooth` | _missing_ | proximity by distance from a moving point, on a smoothstep: flat at both ends, so an element eases into the point's reach and out of it. The default |
| `sphere` | _missing_ | proximity by distance, as a dome: elements near the moving point react fully, then a cliff. Reads as an object passing beneath them |
| `step` | _missing_ | no falloff: an element reacts fully inside the moving point's radius and not at all outside. A hard-edged pass, hover-like |

## energy  `[energy]`

| name | words | action |
|---|---|---|
| `brand` | _missing_ | the house slow-fast-slow speed ramp on every cut. Directed and confident, the default to reach for |
| `calm` | _missing_ | unhurried: every cut decelerates into place. Premium, editorial, a film with room to breathe |
| `hype` | _missing_ | decisive: cuts land and stop with no drift. Launch-reel energy, product drops, announcements |
| `tense` | _missing_ | restless: cuts accelerate away. Urgency, countdowns, a film that will not sit still |

## envelope anchor  `[envelope.anchor]`

| name | words | action |
|---|---|---|
| `bottom` | _missing_ | the element grows UP from the bottom edge, so the free end, the one `taper` narrows, is at the top. The default |
| `top` | _missing_ | the element hangs DOWN from the top edge, so the free end, the one `taper` narrows, is at the bottom |

## envelope shape  `[envelope.kind]`

| name | words | action |
|---|---|---|
| `arch` | _missing_ | a sine hump. It leaves the baseline at a finite slope and its shoulders sag, so it reads as a bump, not a dome |
| `circle` | _missing_ | the unit semicircular arc, which leaves the baseline UPRIGHT. A symmetric dome, a planet limb, an eclipse |
| `crescent` | _missing_ | one circular arc with a second equal arc bitten out of it. A moon horn: empty on one side, a concave inner edge, a point at the tip |
| `full` | _missing_ | the no-op envelope, 1 everywhere: every element runs the whole frame, which is what a blind does |
| `hills` | _missing_ | three gaussians of unequal width and height summed, rolling ground: three summits with soft saddles between them |
| `ramp` | _missing_ | a straight climb across the row, so the extents rise steadily from one side to the other |
| `scallops` | _missing_ | the same semicircle repeated five times. Odd, so one arc sits centred. A scalloped horizon when shallow, an arcade when tall |
| `valley` | _missing_ | the sine hump run upside down: a dip in the middle with both ends tall |
| `wave` | _missing_ | a sinusoidal ripple across the row, so the extents rise and fall more than once between the two ends |

## feel word  `[ease]`

| name | words | action |
|---|---|---|
| `bouncy` | _missing_ | a spring that gives a visible bounce on landing before it settles. Playful, and loud: one per film at most. |
| `elastic` | _missing_ | overshoots repeatedly with a decaying wobble. The loudest option here and rarely the right one. |
| `gentle` | _missing_ | slow at both ends, but shallower than smooth. Ambient drift, backgrounds, anything not asking for attention. |
| `heavy` | _missing_ | slow at both ends and slow in the middle: reads as mass. Big panels and full-frame moves. |
| `mechanical` | _missing_ | no acceleration at all, constant speed start to finish. Correct for a loop or a marquee, wrong for anything a person is meant to watch arrive. |
| `pop` | _missing_ | overshoots the target and comes back: a small bounce of emphasis on something as it lands. |
| `sharp` | _missing_ | leaves instantly and decelerates steeply. The most aggressive slow-down-at-the-end available. |
| `smooth` | _missing_ | eases in and eases out, slow at both ends. The safe choice for anything travelling a long way across frame. |
| `snappy` | springy, crisp, punchy | quick and decisive: moves off fast and slows down hard at the end. The default for a UI element landing. |
| `soft` | _missing_ | arrives and settles, with the overshoot damped out. Use where a hard stop would read as a collision. |
| `stiff` | _missing_ | a spring with almost no give: fast, tight, barely any overshoot. Springs that must not look playful. |

## field motion  `[motion.kind]`

| name | words | action |
|---|---|---|
| `breathe` | _missing_ | the whole field swells and settles on one slow cycle, the cells keeping their relation to each other |
| `drift` | _missing_ | the field travels as one body, the cells holding station against each other |
| `shimmer` | _missing_ | the cells slide against each other, so the seams open and close |
| `still` | _missing_ | nothing moves: the output reads no clock at all |

## filter  `[filter]`

| name | words | action |
|---|---|---|
| `bloom` | _missing_ | thresholds LUMINANCE and blooms the bright parts at two scales, light comes out of the picture, so a dark edge emits nothing. `ry` makes it directional (an anamorphic streak) |
| `chromaGlow` | _missing_ | a stack of zero-offset drop-shadows: white core, warm mid halo, cool outer. It follows the ALPHA, so it is right on GLYPHS and wrong on an opaque picture, where it haloes the rectangle. For a photo use `bloom` |
| `chromaSplit` | chromatic aberration, rgb split, colour fringing | the colour channels pulled apart per pixel and summed back, real fringing on the picture, not a coloured silhouette of its alpha. `warm`/`cool` tint each direction |
| `convolve` | _missing_ | a 3x3 kernel reading NEIGHBOURING pixels: emboss (a lit rubbing), edge (flat areas cancel to black, only boundaries survive), sharpen |
| `displace` | _missing_ | static turbulence drives a displacement map. The pixels are pushed around by a fixed noise field. `freq` sets lump size (low = few big lumps), `scale` sets how far |
| `duotone` | _missing_ | luminance remapped to TWO colours, shadows to highlights. The poster/press look; defaults to --ink and --accent, so it reskins per theme |
| `goo` | _missing_ | the GOOEY / METABALL threshold, and the one primitive that can morph SEVERAL elements as one body: blur the group, then push the blurred ALPHA back through a hard ramp, so neighbouring shapes grow a liquid bridge as they approach and snap apart as they part. Put it on the PARENT (a `group`, or an `html` layer whose children move), never on one shape: one shape has nothing to fuse with. `goo:14` sets the blur radius (how far shapes reach for each other), `goo:14,26` also sets the ramp hardness. Bigger radius = longer bridges; softer ramp = wetter edge |
| `gradientMap` | _missing_ | luminance remapped across any number of stops. The general case the two above are special cases of: a heat ramp, a risograph, a false-colour read |
| `morph` | _missing_ | dilate or erode, swell the bright pixels into their neighbours, or eat them away. Type gains or loses weight |
| `posterize` | _missing_ | each channel quantised to N discrete levels IN PLACE, hues kept. Banding as a decision, not an artefact |
| `relief` | _missing_ | a light source over a luminance bump map. Diffuse MULTIPLIES (ink pressed into stock), specular ADDS (a highlight on metal): same primitive, opposite composite |
| `sepia` | _missing_ | the plain CSS sepia, an amount 0..1. The cheapest warm-and-dated pass there is |
| `thermalBlur` | _missing_ | the After Effects THERMAL BLUR: blur first, then remap the falloff. White cores, an orange body, a red rim, thin strokes eaten by the ramp. On a TRANSPARENT layer the ramp bottom stops land where the alpha has already gone, so the blue rim needs the type on an opaque black plate under `screen` (see the header). `gradientMap` recolours a picture that already has midtones; this one MAKES the midtones, which is why it is the one that works on TYPE. `thermalBlur:8` sets the near radius |
| `tritone` | _missing_ | duotone with a third stop in the middle, which is what stops the midtones going muddy |
| `vignette` | _missing_ | NOT a filter. A darkening field composited over the layer box, so it is an inset radial-gradient overlay div and stays sharp at the edges |

## generator  `[generator]`

| name | words | action |
|---|---|---|
| `bands` | _missing_ | A ramp repeated over a field: panels, concentric arcs or nested rounds, with a light behind. |
| `colonnade` | _missing_ | Wide panels split by bright hairlines, soft masses under a glow. |
| `crt` | _missing_ | A cathode ray tube: the picture under it goes soft and blooms, then scanlines and a corner falloff go over the top. |
| `effector` | _missing_ | Move a hundred copies with one moving point, and keyframe none of them. Each copy reacts to how FAR the point is, not to its place in a list, so you get a wave, a ripple, or a trail that lags behind. Flip between trail, highlight and ripple to see it. |
| `keyframeHandle` | _missing_ | Why two easings that look alike FEEL different: one curve between two keyframes, drawn beside a strip of evenly spaced dots. The dots bunch where the thing is slow and spread where it is fast, which the curve alone hides. Flip between easyEase, snap, hang and overshoot to see it. |
| `rangeSelector` | _missing_ | The AE range selector's smoothness dial on this engine's kinetic presets: at 1 a glyph transitions, at 0 it swaps outright, which is the value a font morph needs. |
| `spectrum` | _missing_ | Upright bands with a colour ramp falling down the frame, each band showing less of it than the one inside it. |
| `thermalBlur` | heat vision, heat camera, infrared | White type blurred, then remapped through a heat ramp: white cores, an orange body, a blue rim, and the thin strokes eaten away. |

## glow preset  `[preset]`

| name | words | action |
|---|---|---|
| `bloom` | _missing_ | a hot near-white core falling off to the accent: light overflowing a bright source |
| `chromaCycle` | _missing_ | a saturated neon bloom whose hue sweeps the spectrum over `cycle` seconds |
| `chromatic` | _missing_ | an RGB-split halo: three channel copies offset and screen-blended, white core, colour fringe |
| `diffusion` | _missing_ | a broad low-alpha white veil, screen-blended: lifts blacks without recolouring below |
| `halation` | _missing_ | a tight warm core plus a wide faint ring, the film halation. Low intensity on purpose |
| `rimLight` | _missing_ | an off-centre crescent, lit edge up-right. Place the subject there |
| `spotlight` | _missing_ | a soft-edged cone from an apex, aimed by `angle`, faded with distance so it never clips |

## gradient recipe  `[bg[].opts.recipe]`

| name | words | action |
|---|---|---|
| `arctic-blue` | _missing_ | pale ice blue radial, a cold spotlit pool |
| `berry-crush` | _missing_ | crimson into tomato red, a hot fruit-punch ramp |
| `citrus-pop` | _missing_ | orange into bright yellow, a loud fruit-stand ramp |
| `cool-mint` | _missing_ | green into teal, a fresh spa ramp |
| `coral-reef` | _missing_ | salmon into pale pink, a beachy diagonal ramp |
| `cosmic-drift` | _missing_ | indigo, plum and hot pink drifting, a MOVING galaxy mesh |
| `deep-ocean` | _missing_ | near-black into slate blue, a moody nautical field |
| `ember` | _missing_ | crimson, tomato and amber drifting, a MOVING fire mesh |
| `emerald-pool` | _missing_ | teal into jade radial, a jungle-pool glow |
| `forest-mist` | _missing_ | teal, jade and pale sky drifting, a MOVING woodland mesh |
| `glacier` | _missing_ | sky blue into pale cyan radial, an icy pool |
| `golden-hour` | _missing_ | amber into warm yellow, a late-afternoon ramp |
| `lavender-fog` | _missing_ | pale periwinkle into indigo, a dreamy purple wash |
| `midnight-violet` | _missing_ | three-stop indigo to plum, a night-sky ramp |
| `neon-dusk` | _missing_ | hot pink into electric violet, a synthwave ramp |
| `peach-cream` | _missing_ | pale peach into blush, a soft dessert ramp |
| `plum-wine` | _missing_ | violet into dusty rose, a wine-toned diagonal ramp |
| `rose-glow` | _missing_ | blush pink radial, a warm skin-tone pool |
| `slate-storm` | _missing_ | charcoal into graphite, a neutral overcast ramp |
| `solar-flare` | _missing_ | red-orange-magenta conic ring, a sunburst wheel |
| `spectrum-ring` | _missing_ | cyan-violet-pink conic wheel, a full-hue rainbow ring |
| `steel-blue` | _missing_ | slate into charcoal-blue, a cool industrial ramp |
| `sunset-strip` | _missing_ | red, magenta and amber drifting, a MOVING sunset mesh |
| `warm-dusk` | _missing_ | orange into coral, a sunset ramp on a 40deg diagonal |

## gsap effect  `[fx]`

| name | words | action |
|---|---|---|
| `backIn` | _missing_ | rises 90px and overshoots past its mark before settling, as fadeUp with a spring on the end |
| `blurIn` | _missing_ | resolves out of heavy defocus in place, calm, premium, no travel at all |
| `bounceIn` | _missing_ | drops in from above and bounces on landing, cartoon weight, for a punchline |
| `breathe` | _missing_ | LOOP, never settles: a slow 2.2s swell with a slight dim, ambient, calmer than pulse |
| `charBlurCascade` | _missing_ | each glyph rises out of blur in turn, the calm, premium per-letter reveal |
| `charFold` | _missing_ | each glyph unfolds up from its own baseline with a small overshoot, kinetic type, warm |
| `charOvershoot` | _missing_ | each glyph pops up from small and springs past its mark, the loudest per-letter reveal |
| `charTilt` | _missing_ | each glyph swings in about its vertical axis and straightens, kinetic type, needs 3D perspective |
| `clipUp` | _missing_ | a hard bottom-to-top wipe that uncovers the layer in place, opacity untouched, type reveals behind a mask |
| `drift` | _missing_ | LOOP, never settles: a very slow 3s sideways wander. Background parallax, easy to miss on purpose |
| `driftIn` | _missing_ | floats a short diagonal out of soft blur, slowly. The quietest entrance here, for atmosphere |
| `dropIn` | _missing_ | as bounceIn but falling from much further up, so it lands harder |
| `elasticIn` | _missing_ | springs from tiny and wobbles several times before it stills, over a slow 1.1s, only ever playful, never for a serious brand |
| `expandIn` | kerning, letter-spacing, letterspacing, tracking out | letters start crushed together and spread out of blur to their real tracking, an opening line settling into place |
| `flipInX` | _missing_ | hinges up into the frame about its horizontal axis, cards and panels, needs 3D perspective |
| `flipInY` | _missing_ | hinges in about its vertical axis, like a page turning. Cards and panels, needs 3D perspective |
| `float` | _missing_ | LOOP, never settles: rises and sinks 18px forever, idle life for a hero object |
| `foldIn` | _missing_ | unfolds downward from its top edge and springs level, dropdowns, panels, receipts; needs 3D perspective |
| `glitchIn` | _missing_ | snaps in through five hard steps, sheared and offset. No smoothing at all, alarm and glitch beats only |
| `heartbeat` | _missing_ | LOOP, never settles: a fast 12% throb twice a second. Urgency, live counts, recording dots |
| `maskReveal` | _missing_ | a hard left-to-right wipe, fast then a long settle. The premium editorial reveal for a headline |
| `pulse` | _missing_ | LOOP, never settles: breathes 6% larger and back every second, draws the eye to a CTA |
| `revealUp` | _missing_ | clipUp plus a short lift and fade, so the layer rises as it is uncovered, the fuller version of clipUp |
| `rollIn` | _missing_ | rolls in from the left, its rotation unwinding as it travels, reads as a wheel arriving |
| `skewIn` | _missing_ | slides in sheared and straightens as it lands, velocity you can read in the letterforms |
| `spinIn` | _missing_ | rotates a half turn anticlockwise while growing, overshooting on the settle, logos, badges, seals |
| `swing` | _missing_ | LOOP, never settles: a slow pendulum rock. Hanging objects; needs 3D perspective |
| `tiltIn` | _missing_ | swings open about its left edge, like a door facing the camera, needs 3D perspective |
| `wobble` | _missing_ | LOOP, never settles: rocks 3 degrees each way, restless, for a warning or a toy |
| `zoomBlur` | _missing_ | rushes back from too close while the defocus resolves. A camera pulling focus, premium hero beat |

## icon  `[svgIcon()]`

| name | words | action |
|---|---|---|
| `agent` | _missing_ | an autonomous worker or assistant. The mark for something acting on its own. |
| `arrowLeft` | _missing_ | back, previous, return. The pair to arrowRight, and it should point the way the film travels. |
| `arrowRight` | _missing_ | forward, next, onward. Progression through a sequence, or a result following a cause. |
| `bolt` | _missing_ | speed or power. Fast execution, an instant action, energy. |
| `braces` | _missing_ | code, JSON, a config file. The one to use when the subject is literally the syntax. |
| `check` | _missing_ | done, passed, verified. The mark for a gate going green or a step completing. |
| `clock` | _missing_ | time, duration, waiting, a deadline. Any beat whose subject is how long something takes. |
| `cube` | _missing_ | a package, a build artifact, a module. A discrete unit of software. |
| `dollar` | _missing_ | money, price, revenue, cost. Any figure the viewer reads as currency. |
| `file` | _missing_ | a document or a single record. Use where the subject is one item of content rather than a system. |
| `globe` | _missing_ | the world, the network, a public surface. Reach, deployment, anything international. |
| `layers` | _missing_ | a stack, composition, depth. The mark for things built out of other things. |
| `link` | _missing_ | a connection between two things, or a URL. Integration, reference, chaining. |
| `plug` | _missing_ | an integration or a connector. Where one system attaches to another. |
| `shield` | _missing_ | security, protection, trust. The one to reach for on a safety or privacy claim. |
| `spark` | _missing_ | something new, generated, or AI-made. Novelty and creation rather than speed. |

## idle  `[idle]`

| name | words | action |
|---|---|---|
| `breathe` | _missing_ | 1.5% scale, ~4.6s. The held frame stays alive without moving |
| `drift` | _missing_ | slow translate on two periods, ~9px, the frame is never quite parked |
| `none` | _missing_ | no idle. The layer is truly still on its hold (the default) |

## interpolation mode  `[ease]`

| name | words | action |
|---|---|---|
| `through` | stall, stalls, stops dead, dead stop, stops in the middle, pause, pauses, hitch, hesitates, stutter, jerk, kink, waypoint, sparse keys, three keys, graph editor, continuous bezier, auto bezier, carries speed, one gesture, rounds a corner | velocity carries THROUGH the key: a cubic Hermite with neighbour tangents, so a travel across several keys is one gesture rather than a stop at each |

## keyframe handle  `[easeOut]`

| name | words | action |
|---|---|---|
| `easyEase` | _missing_ | AE's Easy Ease: reaches a third of the way in and arrives at a DEAD STOP. The default handle, and the one to use when you just want a key to stop being mechanical |
| `fling` | _missing_ | a short handle at four times the average speed: the value leaves (or arrives) FAST and the segment spends its length recovering. The steep half of a snappy move |
| `hang` | _missing_ | influence 75 at a dead stop: the value HANGS at this key and the movement is crushed away from it. On BOTH sides of a segment this is the flat-ended, near-vertical speed graph a snappy swap is cut on. 75 is the number practitioners state |
| `linear` | _missing_ | the straight line, written down: the handle sits on the diagonal so this side of the segment has constant speed. Use it to make one side explicit while the other is shaped |
| `overshoot` | _missing_ | arrives from BEYOND its key and settles back: the value sails about 10 per cent past and returns. The handle version of a back ease, and it needs the far side to stop it |

## kinetic preset  `[preset]`

| name | words | action |
|---|---|---|
| `assemble` | _missing_ | each glyph flies in from its OWN scattered offset and rotation and settles into the word, arriving in a shuffled order · the AE "text animator + randomize-order range selector" reveal. Pair it with `split: "char"` |
| `blur` | soft focus resolve, defocus in | resolve out of blur: calm, premium |
| `blurUp` | _missing_ | letters lift and sharpen out of blur together: the default kinetic headline |
| `bounce` | _missing_ | springy bounce in: playful brands only |
| `chroma` | chromatic aberration, rgb split, colour fringing | R/G/B ghosts split apart and converge to a crisp glyph |
| `colorWave` | per word colour, per-word color, each word its own colour, colour per word, words leave one by one | the accent sweeps word by word along a line, each unit lighting then settling to the resting colour · `colors` gives EACH unit its OWN arrival colour instead of one shared accent, per-word colour before the line settles to one ink |
| `decode` | _missing_ | scramble→settle, techy |
| `down` | _missing_ | words/chars drop into place from above: the mirror of `up` |
| `draw` | line drawing, path draw, svg stroke animation | stroke draw-on for SVG paths |
| `elastic` | _missing_ | elastic scale pop with visible wobble: playful brands only |
| `fade` | word by word exit, words leave one by one, text disappears word by word, split exit | plain opacity fade, no motion at all: the neutral entrance, and the neutral exit reversed |
| `fall` | _missing_ | falls from above under gravity and lands with a small squash |
| `flap` | _missing_ | the glyph steps FORWARD through the board's alphabet one flap at a time and lands on its letter, hinging as it turns. An airport board, ordered where `decode` is random |
| `flip` | _missing_ | 3D flip-up per unit, letters somersault into place: `axis` picks the hinge |
| `focus` | _missing_ | focus pull, heavy blur and over-scale resolving to crisp, dreamy, premium |
| `gradient` | rainbow text, colour sweep text, gradient text reveal | gradient sweeps through letterforms |
| `highlight` | marker highlight, highlighter sweep, underline sweep | marker highlight sweep |
| `riseClip` | _missing_ | the word climbs out from behind a hard edge at its own baseline, hidden until it clears the line. The clean editorial reveal |
| `scale` | zoom in text, grow text from small | punch in from small (overshoot) |
| `shadow` | _missing_ | a long poster shadow collapses as the word settles, poster statements |
| `shimmerWave` | _missing_ | looping light wave (per-unit): a 3D crest travelling across the word; never settles |
| `skew` | _missing_ | italic shear that straightens as it lands: editorial, sporty |
| `slide` | _missing_ | slides in from one side (`dir`): pair it with the opposite exit |
| `stretch` | _missing_ | horizontal smear that snaps true: impact words |
| `strike` | strikethrough, crossed out, struck through | a rule draws THROUGH the word and it dims behind the line, still legible. The "not X, Y" beat, where the rejection is the content |
| `swing` | _missing_ | each unit hinges from its top edge and swings upright, playful, short words |
| `tilt` | _missing_ | each unit rises and swings upright from a small angle, sporty and editorial. A flat rotation, not a 3D hinge: `flip` is the one that turns |
| `type` | typing, letters one at a time, keystroke, terminal caret | typewriter hard on/off, no transform: terminals, timers, code |
| `underline` | _missing_ | a rule grows left to right along the baseline as the word lands, the marker under a heading |
| `unfold` | _missing_ | opens from edge-on about its left hinge, a panel turning to face you, premium |
| `up` | _missing_ | words/chars rise into place, no blur: the plain lift |
| `wave` | _missing_ | sinusoidal wave across units: a LOOP that never settles; ambient only |
| `weight` | _missing_ | the glyphs THICKEN into place along the font's own `wght` axis, a crest of weight travelling the line · the one register a static face cannot fake, and it degrades to the nearest static cut rather than to a dead still |

## layer type  `[layers[].type]`

| name | words | action |
|---|---|---|
| `adjust` | _missing_ | one grade over everything BENEATH it: blur, desaturate, darken, brighten or contrast the whole frame from a single layer, keyable through `vars` on `--adjust` |
| `beam` | _missing_ | a light that travels the rounded-rect border, or a sheen that sweeps across the box; the travel is closed-form in t, not a CSS keyframe |
| `board` | _missing_ | a populated workspace from pure data: up to 4 columns of up to 5 mini issue-cards, entering as one clip |
| `clip` | _missing_ | a video played as a preloaded PNG frame sequence: the frame swaps the <img> src, so no decoder state can drift between renders |
| `component` | _missing_ | a REAL captured UI block (`make capture`), or one named part of a captured scene, scaled to fit `w` |
| `composition` | _missing_ | names a first-party hand-authored GSAP timeline in core/compositions/ and passes it DATA; for choreography `parts` and recipes cannot express |
| `count` | _missing_ | a number that counts from -> to across its own window, formatted (compacts at 1e6, prefix/suffix/decimals). The text build plus a per-frame value. `roll: true` makes it an ODOMETER: one masked wheel per digit, each sliding to its next value, geared so a wheel only turns as the wheel below it crosses 9 |
| `cursor` | _missing_ | a pointer that follows [{t,x,y}] keyframes (`path`) or eases magnetically onto another layer's live box (`snapTo`), fires a ripple at `clicks`, can change shape mid-move (`style`/`styleAt`: arrow/hand/ibeam/block), carry a name tag (`label`), and drag another layer along with it (`carry`) |
| `doc` | _missing_ | a file card from pure data: an optional filename + diff chip, then heading / body / code / bullet blocks, theme-styled and auto-height |
| `globe` | _missing_ | a dotted planet with tapered route arcs, drawn by vendored cobe: atmospheric glow and a diffuse terminator. reach for `three:"globe"` instead only when you need a real sun vector |
| `glow` | _missing_ | soft light with no WebGL: a radial centre glow, a directional beam, or a named phenomenon (bloom · halation · diffusion · rimLight · spotlight) |
| `group` | _missing_ | layout by containment: a flex/grid box whose children (any depth) flow with a gap, so a card + label + chip can never desync |
| `html` | _missing_ | raw hand-authored HTML/CSS as ONE layer, still positioned and animated by the engine; <script> is stripped and CSS transition/animation refused, use `parts` to give its pieces the clock |
| `image` | _missing_ | an <img> with cover-fit, radius, a ken-burns slow zoom and an edge dissolve; `canvasFx` bakes a per-pixel pass into it at boot |
| `lottie` | _missing_ | an After Effects (Bodymovin) export driven by ABSOLUTE seek, goToAndStop at a frame index, never autoplay |
| `paint` | _missing_ | a generative Canvas 2D field drawn per frame from local time. The canvas family written in JS rather than GLSL, and the one you can resample |
| `particles` | _missing_ | a deterministic particle emitter: `preset` picks confetti (a gravity-fed burst), sparks (a fast radial flash) or dust (a slow ambient drift). Closed-form per particle, seeded by `seed`, never a running sim |
| `raymarch` | _missing_ | a lit implicit surface from a distance field: a camera, a normal and a silhouette. the most expensive primitive in the engine. One hero shot, sized to what it needs |
| `rect` | _missing_ | a plain box, panel, card or pill. it carries no text: put that on a higher track |
| `shader` | _missing_ | a full-frame generative WebGL field (see the ambient shaders), pure in t and palette-tintable; it carries no sampler, so it cannot read what is beneath it. `shaderKeys` cycles ONE panel through several looks on hard cuts, in one context, instead of stacking a layer per look |
| `svg` | _missing_ | a vector mark that DRAWS itself on (stroke dashoffset) and then RESOLVES INTO ITS FILL, the stroke leaving as the solid logo arrives, or MELTS from one path into another (true point-lerp morph, optional spin) |
| `text` | _missing_ | theme-styled words in an optional chip box, auto-fit to a width; the typewriter reveal and caret live here too; `typingColors` flashes each word its own accent colour the instant it types, then settles to ink |
| `three` | _missing_ | a real three.js scene graph (meshes, materials, lights, a camera) posed absolutely from t, for what a distance field cannot express: a font outline, a device body, a captured UI plane, a point cloud |
| `video` | _missing_ | real footage, SEEKED to a computed source time every frame and never played, so the picture is as deterministic as a still |

## lightfield pattern  `[pattern.kind]`

| name | words | action |
|---|---|---|
| `rings` | _missing_ | concentric bands round a point, like light on water, the eye travels outwards |
| `shards` | _missing_ | a fan of rays from a pivot below the frame, the eye travels up and out |
| `slats` | _missing_ | a backlit blind: vertical bars of unequal width, each with a lit leading edge falling to a dark trailing edge, the eye travels across |

## look  `[filter]`

| name | words | action |
|---|---|---|
| `angelic` | _missing_ | blown out, desaturated, the widest bloom in the library and an INVERTED vignette that whitens the corners. Heaven light, heavy-handed by design |
| `chrome` | _missing_ | specular highlights ADDED on top, then remapped to a navy/steel/white tritone. Polished metal; two SVG filter passes, the most expensive look here |
| `crt` | _missing_ | half the split of vhs plus a white bloom and the densest, darkest scanlines, corners pulled down. A lit phosphor tube: vhs glows and closes in |
| `cyberpunk` | _missing_ | saturated hard, magenta/cyan split, magenta bloom over faint scanlines. A fixed neon-noir palette laid over your colours |
| `dreamSequence` | _missing_ | the mildest warp plus defocus, cream bloom and a warm wash. DreamyHaze with the picture set drifting |
| `dreamyHaze` | _missing_ | half-pixel defocus under a wide white bloom and a cream wash, soft-focus romance, no grain and no edges |
| `droneCinematic` | _missing_ | every dial turned down (0.5 default strength, faint grain, shallow vignette). A grade rather than a look, safe under type and product UI |
| `edgeGlow` | _missing_ | flat areas cancel to black and only boundaries survive, amplified 6x and bloomed. A neon wire drawing; six passes, and a smooth subject can come out empty |
| `emboss` | _missing_ | the kernel turns the picture into a grey lit rubbing and half the colour is thrown away, carved surface, per-pixel and not cheap |
| `fadedPolaroid` | _missing_ | eight passes: sepia, desaturated, contrast LOWERED, brown wash, corner leak, grain, vignette. A print left in the sun, the flattest look here |
| `fatten` | _missing_ | dilation swells the bright pixels into their neighbours, type gains weight and a photo goes chunky and poster-like |
| `filmNoir` | _missing_ | full grayscale, contrast crushed up, a small white bloom, grain and the deepest vignette, monochrome drama |
| `glassWarp` | _missing_ | medium-frequency turbulence pushes the image around behind a faint white bloom, seen through thick glass, still readable |
| `glitchGlow` | _missing_ | wide 3px red/blue split, source-coloured bloom, sparse scanlines. Hologram with the tint removed and the split doubled: a signal fault, not a projection |
| `halationFilm` | _missing_ | warm amber bloom off the highlights plus grain, contrast barely touched. The restrained glow: no blur, no vignette, colours survive |
| `heatWarp` | _missing_ | coarser, larger displacement under an orange wash and a lift, air over tarmac |
| `hologram` | _missing_ | pink/cyan split, cyan flood-bloom and tight scanlines. A projected image, colour forced to teal |
| `impact` | _missing_ | overexposed with the widest colour split here (4px) and a white bloom. The frame taking a punch, for one frozen hit only |
| `letterpress` | _missing_ | diffuse light multiplies the image so it sinks into a warm paper wash, lifted 1.5x to stop it going black, ink pressed into stock |
| `lomo` | _missing_ | the strongest saturation and the heaviest vignette in the library, plus a corner leak and grain. Plastic-camera punch: no glow, no split, just crush and corners |
| `melt` | _missing_ | the lowest frequency and by far the largest displacement here, big slow lumps that destroy legibility, so never under type |
| `neon` | _missing_ | the bright parts bloom in their OWN colours, nothing is tinted. Real sign-light, the one glow look that keeps the palette |
| `nightVision` | _missing_ | grayscale then sepia then hue-rotated and saturated 4x to ONE green, green bloom, lines, grain, vignette. Image intensifier; the original colour is gone, not tinted |
| `nostalgia` | _missing_ | fadedPolaroid without the wash, the flattening or the vignette, plus a warm bloom, same memory register, lighter and open at the edges |
| `rippleGlass` | _missing_ | the highest frequency and a small offset, so the distortion reads as tight ripples not lumps, over a cool bloom, water on the lens |
| `super8` | _missing_ | sepia, lifted, the heaviest grain here and a deep vignette. Warmth and dirt only, no split and no lines: film stock, not video |
| `thermal` | _missing_ | luminance remapped to a six-stop black-purple-pink-orange-white ramp, softened. A heat camera; three passes, the cheapest sci-fi look and the most total recolour |
| `timeFreeze` | _missing_ | drained of colour, washed cold blue with a matching bloom and a hairline split, the world stopped |
| `vhs` | videotape, retro tape, analog video, camcorder footage, old video | colour split 3px, slight defocus, scanlines and heavy grain. Tape: no vignette and no glow, so the frame stays flat and dirty |
| `vintageAnamorphic` | _missing_ | the only look with horizontal blue streaks off the highlights, plus split, grain and a deep vignette, old spherical glass |
| `watercolor` | _missing_ | displaced, desaturated, contrast lowered under a multiplied paper wash and grain, pigment on stock |

## modifier  `[modifiers[]]`

| name | words | action |
|---|---|---|
| `alongPath` | _missing_ | set the line of type ON A CURVE and, if you ask, send it travelling along one. The general case `circle` is one point of, and the only way to bend a headline |
| `ghost` | _missing_ | the only effect that reads TIME AS A MATERIAL: it evaluates the layer's own motion track a few frames BACK and draws from the difference, `trail` leaves faded copies at the poses it just left, `blur` samples the same poses inside one frame so the layer smears along its real direction of travel, not around a fixed centre |
| `kick` | _missing_ | hit the layer on the film's own joints. A cut, a seam or a sting shoves it, so the frame feels the edit |
| `lag` | _missing_ | FOLLOW-THROUGH: this layer trails another layer's motion by a frame or three and overruns its stop before settling. Stagger delays a sibling's entrance; this makes one layer drag behind another's continuous motion, which is half of what separates an animated object from a moved image |
| `matte` | _missing_ | a LUMA MATTE: another layer's brightness is this layer's alpha, white shows and black hides. The general case of the whole wipe family, and the matte MOVES, because it is placed from the source layer's live box |
| `mixBlend` | _missing_ | how this layer's pixels combine with what is already painted behind it, knock a headline out of a photo |
| `occlude` | _missing_ | hide this layer where another one covers it, put something BEHIND something else without reordering the stack |
| `plane` | _missing_ | stand the layer at a DEPTH so the camera moves it by a different amount than its neighbours, this is parallax |
| `progress` | _missing_ | hand the layer the FILM's progress, 0 at the first frame and 1 at the last, as a CSS custom property its markup can draw with |
| `shadow` | _missing_ | a drop shadow that knows where the light is, so every layer does not point the same way |
| `squash` | _missing_ | SQUASH AND STRETCH read off the layer's own velocity: the travel axis stretches and the perpendicular one squeezes by exactly the reciprocal, so the volume holds. Scale both and it is a zoom, not a squash |
| `tilt` | _missing_ | turn the layer out of the picture plane and hold it there. A card leaning away, a phone at an angle, panels receding |
| `upright` | _missing_ | AUTO-ORIENT: hold this layer UPRIGHT while the layer carrying it rotates, so an arrangement can turn without its contents turning with it. Parenting gives you the rotation for free and it is almost always wrong: photographs carried round a circle go upside down at the bottom exactly when the motion peaks |
| `wordSlot` | _missing_ | one word of the sentence swaps for the next while NOTHING after it reflows. The slot is a grid cell auto-sized to the widest candidate, and `chip: true` makes it the brand-coloured box the launch rule asks for |

## motion voice  `[audio.cues[].name]`

| name | words | action |
|---|---|---|
| `bloom` | _missing_ | something OPENS: placed automatically on an `iris`, `softiris`, `rise` or `riseBlur` cut, and on a declared `spectacle`. Slower in than the others, because an opening is not an arrival |
| `chime` | _missing_ | a small bright accent where a moment resolves. Noticed rather than announced, so it survives repetition better than a sting does |
| `droplet` | _missing_ | something falls into place: a short pitched drop, placed automatically on a `drop` or `zoom` cut. The lightest of the arrival sounds |
| `impact` | _missing_ | something heavy ARRIVES and lands hard: a hit with an edge, a mass and a room, in that order. A frame-sized card reaching its mark, a panel slamming home |
| `pluck` | _missing_ | punctuation, for a small element or a counter digit. Quiet on purpose: this is the one that becomes a machine gun, and the density rules exist because of it |
| `whoosh` | _missing_ | air moving past. One camera gesture, one whoosh, never one per keyframe: the sound rises as the move starts and falls away as it passes |

## move shape  `[move:<shape>:<band>]`

| name | words | action |
|---|---|---|
| `blast` | _missing_ | four-key punctuation: arrives oversize, settles, drifts, then grows OUT through the frame. A hard pop, never a fade |
| `drift` | _missing_ | a slow ambient hold on one axis that never returns to where it began, for a layer that should keep living through a beat |
| `enter` | _missing_ | a keyed entrance track on the layer itself rather than a preset name: rises in, overshoots slightly, settles |
| `exit` | _missing_ | a keyed departure that accelerates off-frame, opacity trailing the move: the layer is gone because it LEFT, not because it dimmed |
| `pan` | _missing_ | an irregular multi-key horizontal (or vertical) pan, linear interior keys: surges then gives up, reads as a hand scrolling, not an animation of a page |

## output target  `[aspect]`

| name | words | action |
|---|---|---|
| `16:9` | _missing_ | wide landscape, the desktop shape: a website hero, a YouTube upload, a deck slide, anything watched on a laptop |
| `1:1` | _missing_ | a square, equal on both sides: the feed post that crops the same everywhere it is shown |
| `4:3` | _missing_ | boxy landscape, the old television and slide-projector shape: archive footage, a retro monitor |
| `4:5` | _missing_ | the tall feed post: Instagram portrait, more height than a square without taking the whole phone |
| `9:16` | _missing_ | tall and vertical, the whole phone screen edge to edge: TikTok, Reels, Shorts, a story |

## paint fx  `[paint]`

| name | words | action |
|---|---|---|
| `aurora` | _missing_ | slow curtains of coloured light. Calm, ambient, and the least busy field here. |
| `matrix` | _missing_ | columns of falling glyphs. Code, data streaming, a machine at work. Unmistakably a reference, so use it deliberately. |
| `meteor` | _missing_ | streaks crossing the frame on a diagonal. Motion and event on a background that would otherwise be still. |
| `starfield` | _missing_ | stars drifting past as if the camera were moving through them. Depth and travel on an otherwise flat frame. |
| `waves` | _missing_ | rolling bands, a moving surface. Rhythm underneath content without competing for the eye. |

## part entrance  `[parts[].anim]`

| name | words | action |
|---|---|---|
| `drawOn` | _missing_ | an SVG stroke draws itself along its own path, measured at build with getTotalLength() |
| `fade` | _missing_ | opacity alone, no displacement · the quiet default when a part should arrive without moving |
| `fadeUp` | _missing_ | a short rise with a fade, the quiet default for any part |
| `growUp` | _missing_ | scales up from its own bottom edge, a bar growing to its reading |
| `popIn` | _missing_ | scales from nothing at its centre with a fade, dots, chips, markers |
| `riseIn` | _missing_ | a longer rise with a fade, for parts that should feel like they arrive |
| `slide-left` | _missing_ | enters from its left and, with `out`, keeps going right · one direction of travel, never a retreat |
| `slide-right` | enter from the right, slide in from the right side | enters from its right and, with `out`, keeps going left · the mirror of slide-left |
| `widen` | _missing_ | scales out from its left edge. A row, a rule, anything that should fill rather than appear |

## particles preset  `[preset]`

| name | words | action |
|---|---|---|
| `confetti` | _missing_ | a gravity-fed burst launched near the bottom of the box, tumbling rects as it falls: celebratory |
| `dust` | _missing_ | a slow ambient drift that loops forever, low-alpha specks with a gentle twinkle: atmosphere behind a subject |
| `sparks` | _missing_ | a fast radial flash from the box centre, additive-blended, gone in under a second: an impact or a reveal |

## path curve  `[move:<curve>:<band> | alongPath.curve]`

| name | words | action |
|---|---|---|
| `arc` | _missing_ | a shallow rise-and-fall, peak in the middle |
| `dip` | _missing_ | a shallow sag-and-rise, trough in the middle |
| `ramp` | diagonal line, straight diagonal path | a straight diagonal, top-left to bottom-right |
| `wave` | _missing_ | an S-curve, one full up-down cycle |

## placement  `[pin]`

| name | words | action |
|---|---|---|
| `bottom` | _missing_ | horizontally centred, pinned to the bottom edge of the safe area |
| `bottom-left` | _missing_ | pinned into the bottom-left corner of the safe area |
| `bottom-right` | _missing_ | pinned into the bottom-right corner of the safe area |
| `center` | _missing_ | dead centre of the canvas, vertically at the optical centre rather than the exact middle |
| `left` | _missing_ | pinned to the left safe edge, vertically centred |
| `lower-band` | _missing_ | a thin strip near the bottom safe edge, left-anchored: where a closing rule or a small persistent label sits |
| `right` | _missing_ | pinned to the right safe edge, vertically centred |
| `stage` | _missing_ | the full-width content column between the left and right composition margins, at any aspect ratio: the repo's hand-typed x:160/w:1600 anchor, portable |
| `text-band` | _missing_ | the horizontal strip roughly two-thirds down the frame where a headline or a sub-line usually sits |
| `thirds-b` | _missing_ | centred horizontally, sat on the lower third line |
| `thirds-bl` | _missing_ | lands on the lower-left rule-of-thirds power point |
| `thirds-br` | _missing_ | lands on the lower-right rule-of-thirds power point |
| `thirds-l` | _missing_ | vertically centred, sat on the left third line |
| `thirds-r` | _missing_ | vertically centred, sat on the right third line |
| `thirds-t` | _missing_ | centred horizontally, sat on the upper third line |
| `thirds-tl` | _missing_ | lands on the upper-left rule-of-thirds power point |
| `thirds-tr` | _missing_ | lands on the upper-right rule-of-thirds power point |
| `top` | _missing_ | horizontally centred, pinned to the top edge of the safe area |
| `top-left` | _missing_ | pinned into the top-left corner of the safe area |
| `top-right` | upper right corner, northeast corner | pinned into the top-right corner of the safe area |

## ransom face  `[ransom.faces (a list of {family, weight} records on a text layer)]`

| name | words | action |
|---|---|---|
| `Anybody` | _missing_ | an extremely wide display sans at its heaviest: poster shout, sci-fi proportions |
| `Archivo` | _missing_ | a workhorse grotesque cut heavy and wide: the neutral, newspaper-headline shout of the set |
| `Caveat` | handwriting font, handwritten font, cursive font, sticky note | handwriting, a felt-tip scrawl: the letter somebody wrote by hand and stuck down |
| `Fraunces` | _missing_ | a high-contrast display serif with a soft wobble, old seed-catalogue flavour, slanted here |
| `Hanken Grotesk` | _missing_ | a friendly rounded sans at maximum weight: soft, solid, no edge to it |
| `Instrument Serif` | _missing_ | a slanted editorial serif with thin stems: magazine headline, elegant against the noise |
| `JetBrains Mono` | _missing_ | a monospace typewriter for code and terminals, every letter the same width |
| `Space Grotesk` | _missing_ | a technical geometric sans, cool and even, the computer-lab voice in the note |

## raymarch  `[raymarch]`

| name | words | action |
|---|---|---|
| `caustics` | _missing_ | a water surface built from crossed low-frequency waves, lit so the caustic bands come from the same field that shapes it rather than sitting on top |
| `chromeGlass` | _missing_ | a tumbling torus and a bobbing sphere in mirror chrome, reflecting a studio horizon with a hard specular glint |
| `glassRefract` | _missing_ | a tumbling block of clear glass. the ray bends going in, crosses the body, and bends again coming out, and the exit is taken three times at three slightly different indices, so edges split into red and blue the way a prism does. thick parts drink the light and take the palette colour. transparent, dispersive, refractive: the crystal, the diamond, the ice cube |
| `holoFoil` | _missing_ | a rippling disc of foil: thin-film interference over bright metal, the hue turning with viewing angle. the band is deliberately NARROW, so it reads as one colour sliding rather than a rainbow, and it is bounded to a disc so it keeps a silhouette |
| `mandelbulb` | _missing_ | the mandelbulb fractal, depth-shaded near-to-far, its exponent breathing between 5.5 and 8.5 on a slow sine |
| `metaballs` | _missing_ | five spheres orbiting and smooth-union-ing into one blob of soft glossy candy, two palette stops shading it top to bottom |

## resample fx  `[resample.fx]`

| name | words | action |
|---|---|---|
| `bitCrush` | _missing_ | quantise the palette down until it bands, each 0.25 of `amount` halving the bit depth, a degrade beat, never decoration |
| `chromaShift` | _missing_ | radial RGB separation, the channels pulling apart from the centre outwards |
| `directionalBlur` | _missing_ | a straight-line smear at a fixed `angle` (degrees, 0 = rightward), the same distance everywhere in the frame, unlike `zoomBlur` which radiates from the centre. This is the AFTER EFFECTS "Directional Blur": a look an author SETS, not a byproduct of a layer's own travel speed (that one is automatic, see `engine-doctrine/CRAFT/AFTER-EFFECTS-TECHNIQUES.md` #4). `count` trades sample quality for cost (2..32, default 16) |
| `dissolve` | _missing_ | noise-thresholded erosion lit by an ember front. The way OUT of an image; ramp `amount:[0.05, 0.95]` to burn it away |
| `fisheye` | _missing_ | real lens distortion: barrel above the middle of the dial, pincushion below, `0.5` the identity. Outside the source reads empty, never a stretched edge |
| `macroblock` | _missing_ | the flat blocks and dropped tiles of a starved codec. A glitch/degrade beat, never decoration |
| `refract` | _missing_ | liquid glass: the image BENDS along a noise gradient with per-channel dispersion and a specular glint, what a blur cannot do |
| `spinBlur` | _missing_ | smear along the arc with the radius preserved, so the pivot itself stays sharp, a rotating badge or seal. `cx`/`cy` move the pivot off screen-middle (0..1, default 0.5/0.5); `count` trades sample quality for cost (2..32, default 16) |
| `zoomBlur` | _missing_ | radial smear out from a centre, near samples kept crisp. An impact moment; ramp `amount:[0.6, 0]` so the frame rushes in and snaps sharp. `cx`/`cy` move the centre off screen-middle (0..1, default 0.5/0.5); `count` trades sample quality for cost (2..32, default 16) |

## scramble charset  `[presetOpts.chars]`

| name | words | action |
|---|---|---|
| `binary` | _missing_ | ones and zeros, the loudest cliche in the set, use it once and only where the subject IS binary |
| `blocks` | _missing_ | four shades of block, so the word dissolves into a bar of noise instead of into other letters |
| `lowerCase` | _missing_ | lowercase only, quieter still, and the right set under a lowercase headline |
| `mixed` | _missing_ | capitals, digits and four symbols, the house default and the busiest of the sets |
| `numbers` | _missing_ | digits only, for a counter, a price, a code or anything the film is about to state as a number |
| `symbols` | _missing_ | punctuation and operators, a terminal or a cipher rather than a word |
| `upperCase` | _missing_ | capitals only, the calmest scramble and the one that keeps a headline reading as type |

## seam fx  `[seam]`

| name | words | action |
|---|---|---|
| `barnDoor` | _missing_ | two doors split from the centre outward along dir, opening onto the next beat. A confident, symmetric reveal |
| `blindsWipe` | _missing_ | slatted blinds sweep across the frame along dir, revealing the next beat. A crisp graphic wipe |
| `burnThrough` | _missing_ | the frame burns away along a noisy front with a hot ember edge, revealing the next beat through the fire. A dramatic organic pivot |
| `cinematicZoom` | push in zoom, dolly zoom transition | dive-in zoom into a screen |
| `clockWipe` | _missing_ | a hand sweeps around the frame like a clock, wiping the next beat in behind it with a soft glowing edge. A timed, mechanical reveal |
| `crossWarp` | _missing_ | both beats drag toward the centre and swap through a soft noise front. A wipe with grit, for organic brands and dark scenes |
| `dispersion` | _missing_ | prism channel-split across the seam along a seeded axis, peaking mid-way, optical, techy pivots |
| `dissolve` | _missing_ | grainy film dissolve, each pixel flipping as a noise front passes it, time/place change |
| `fade` | _missing_ | flat cross-dissolve of both beats. The universal fallback every seam degrades to with no WebGL or a blank raster |
| `flashWhite` | _missing_ | white flash on an energy pivot |
| `irisRound` | _missing_ | a soft circle opens from the centre with a bright rim, irising the next beat into view. A classic spotlight reveal |
| `lens` | _missing_ | one moving optical centre bends BOTH beats through a single lens, with a warm flare. Premium product glamour, dark scenes |
| `lumaWipe` | _missing_ | the next beat bleeds through the darkest parts of the current one first, along a soft noisy threshold. A cinematic dissolve with grain |
| `pixelDissolve` | _missing_ | the frame coarsens into big pixels at the midpoint then sharpens into the next beat. A retro digital dissolve |
| `portal` | wormhole, tunnel reveal, vortex transition | glowing portal reveal (once) |
| `push` | _missing_ | both beats shove together toward dir, the arriving one following the leaving one off screen, dir-aware basic |
| `rippleWave` | _missing_ | concentric waves ripple out from the centre as the frame dissolves, like a drop hitting water. Soft, calming links |
| `sdfIris` | _missing_ | the arriving beat revealed through an expanding seeded polygon iris (star, hex, diamond or triangle) with a bright rim. Playful reveal, the shape is the personality |
| `shatterGlitch` | _missing_ | the frame breaks into blocks whose RGB channels tear apart and flicker, then snap back clean. A hard digital pivot |
| `slide` | _missing_ | the arriving beat slides in over a held outgoing one (cover), dir-aware, the basic every tool has |
| `spinZoom` | _missing_ | the outgoing beat spins and scales out while the incoming beat spins in. A kinetic whip between shots |
| `swirlWarp` | _missing_ | a vortex twists the centre of the frame and unwinds into the next beat, strongest mid-cut. Organic, dreamy pivots |
| `uncover` | _missing_ | the leaving beat slides off toward dir and reveals a held arriving beat under it, dir-aware basic |
| `whipPan` | _missing_ | momentum swipe between beats |
| `wipe` | _missing_ | a soft-edged line sweeps toward dir, the arriving beat revealed behind it, playful, "notice the cut" |
| `zoomBlur` | _missing_ | both beats streak toward the centre in a radial blur and cross-dissolve. A punchy push between shots |

## shadow direction  `[shadow.direction]`

| name | words | action |
|---|---|---|
| `bottom` | _missing_ | away is downward: the frame drains towards the bottom edge and stays lit at the top. The default bearing |
| `bottom-left` | _missing_ | a diagonal fall away to the bottom left, light rarely leaves along an axis, and no edge keyword says this |
| `bottom-right` | _missing_ | a diagonal fall away to the bottom right, light rarely leaves along an axis, and no edge keyword says this |
| `center` | _missing_ | a radial: away in EVERY direction at once, so no edge can darken without the others darkening too |
| `left` | _missing_ | a linear fall away to the left. A bearing and no centre, so only the move along it reaches the fall |
| `left-and-right` | _missing_ | a lit vertical band with darkness at both sides and nothing taken off the top or bottom |
| `right` | eastward shadow, shadow falls east | a linear fall away to the right |
| `top` | _missing_ | away is upward: the frame drains towards the top edge and stays lit at the bottom |
| `top-and-bottom` | _missing_ | a lit band across the middle with darkness above and below it and nothing taken off the sides, the shape a low sun makes |
| `top-left` | _missing_ | a diagonal fall away to the top left, light rarely leaves along an axis, and no edge keyword says this |
| `top-right` | upper right corner, northeast shadow | a diagonal fall away to the top right, light rarely leaves along an axis, and no edge keyword says this |

## spectacle device  `[spectacle.device]`

| name | words | action |
|---|---|---|
| `chromaticSplit` | _missing_ | the frame tears into red/green/blue and snaps back, impact, energy, a hard landing |
| `cinematicZoom` | _missing_ | a fast push with the blur that comes off it, the frame lunging at the subject |
| `dispersion` | _missing_ | the picture separates into its spectrum and reassembles, glass, prisms, luxury |
| `flash` | _missing_ | a single bright bloom over the whole frame, up and gone, the plainest peak there is |
| `glitch` | _missing_ | a stepped horizontal shear, no smoothing. Alarm, breakage, a system under load |
| `iridescence` | _missing_ | an oil-slick sheen washes across the frame once, colour as the event |
| `lens` | _missing_ | a wide optical bulge and release. The frame bending under the weight of the moment |
| `ripple` | _missing_ | a ring travels out from the centre and distorts what it crosses, an impact you can watch spread |
| `sdfIris` | _missing_ | a hard iris opens from the centre. A shutter on the moment, theatrical and exact |
| `streak` | _missing_ | a bright bar sweeps the frame. A specular pass over a mark, the cheapest premium peak |
| `vortex` | _missing_ | the frame twists about its centre and unwinds, the loudest of the radial family |
| `whipPan` | _missing_ | the frame smears sideways as if the camera whipped to it, motion the picture cannot carry alone |

## stagger order  `[stagger.from]`

| name | words | action |
|---|---|---|
| `center` | _missing_ | starts at the middle unit and opens outward both ways, so the word arrives as ONE object rather than as a train |
| `edges` | _missing_ | starts at BOTH ends and closes on the middle, a line that shuts like a door |
| `first` | _missing_ | the wave starts at the first unit and runs to the last, the default, and what a line of type being typed looks like |
| `last` | _missing_ | starts at the last unit and runs backwards to the first, pair it with a right-to-left exit |
| `random` | _missing_ | a hashed, seeded shuffle of the order, scattered arrival that is identical on every render and at every seek |
| `typewriter` | _missing_ | types one character at a time at a fixed TYPING RATE (`cps`, chars/sec) instead of a shared budget, so any split layer can reveal char by char at a chosen speed and still use a preset, a colour ramp, or a reversed exit |

## sting fx  `[sting]`

| name | words | action |
|---|---|---|
| `blinds` | _missing_ | venetian bars opening together. Mechanical and rhythmic. |
| `bokeh` | _missing_ | soft out-of-focus discs drifting across frame. Ambient and quiet: closer to a texture than a cut. |
| `burn` | _missing_ | a film burn: the frame chars through and blows out, the way heat eats a print. Warm, analogue, and loud. |
| `chromaticSplit` | _missing_ | an rgb-fringed shock ring with colour fringing at the edges. An impact with a lens defect. |
| `cinematicZoom` | _missing_ | a dolly punch-in used as a cut cover: the camera lunges forward and the join hides inside the lunge. |
| `circle` | _missing_ | a disc expanding from the centre. Focus opening outward from one point. |
| `confetti` | _missing_ | a burst of coloured pieces thrown across frame. Celebration, and hard to use without looking cheap. |
| `crossWarp` | _missing_ | a noise-smeared directional veil dragging the frame sideways as it goes. |
| `dispersion` | _missing_ | a spectral prism band sweeping the frame, splitting light into its colours as it passes. |
| `dissolve` | _missing_ | the frame dissolves away to white. A clean act break when the next beat starts bright. |
| `domainWarp` | _missing_ | a liquid marble wash, noise folded through noise. Organic, slow, and unlike anything geometric here. |
| `doors` | _missing_ | panels closing in from both sides to meet in the middle. |
| `flash` | _missing_ | a single hard white flash across the whole frame. The bluntest cut cover there is, and the one to use when the two shots have nothing in common. |
| `glitch` | _missing_ | rgb channel split into displaced bars. Digital failure, interference, a system under strain. |
| `grain` | _missing_ | dense fine film grain rising over the frame. Texture rather than event: use it under something else. |
| `gridPixelateWipe` | _missing_ | chunky pixel blocks sweeping a diagonal, each block a quantised tint. A wipe and a pixelate at once. |
| `ink` | _missing_ | an ink bleed spreading to near-black. The dark twin of dissolve, for a beat that gets heavier. |
| `iridescence` | _missing_ | a thin-film interference sheen, the colour of oil on water shifting with angle. |
| `leak` | _missing_ | a light leak, seed-generative and multi-hue. Colour bleeding in at the edges as if the camera back opened. |
| `lens` | _missing_ | a full anamorphic flare: hot core, ghosts down the axis, a horizontal streak. Cinematic and very loud. |
| `pinwheel` | _missing_ | an angular sweep with three arms rotating around the centre. |
| `pixel` | _missing_ | a chunky mosaic that fills the frame and then clears. Reads as resolution being lost and regained. |
| `polka` | _missing_ | a curtain of dots growing until they cover the frame. |
| `ridgedBurn` | _missing_ | a filament ember front sweeping upward, like paper catching along a ragged edge. |
| `ripple` | _missing_ | concentric rings expanding across the whole frame, like a struck water surface. |
| `scan` | _missing_ | a CRT scanline band sweeping through, with a trailing glow. Terminal, monitor, surveillance. |
| `sdfIris` | _missing_ | an iris wipe through a seeded shape, so the aperture is not simply a circle. Focus, with character. |
| `squares` | _missing_ | grid cells filling in on a stagger. Modular, and it suits a layout that is already a grid. |
| `streak` | _missing_ | sharp radial light rays from a hot core. Photographic rather than digital: a lens catching a source. |
| `swirl` | _missing_ | rotational light streaks thrown out from the centre. |
| `thermal` | _missing_ | an iron-bow heat veil in coarse cells, colour mapped from luminance rather than painted on. |
| `vortex` | _missing_ | an ink spiral pulling inward to a dark eye. A beat being swallowed rather than ended. |
| `warp` | _missing_ | a barrel-warped refraction shock ring expanding outward. An impact felt through the lens. |
| `whipPan` | _missing_ | horizontal smear streaks racing across, as if the camera whipped sideways. Energy carried into a payoff. |
| `wipe` | _missing_ | a directional band sweeping across the frame. The plainest geometric transition, and it always reads. |

## theme look key  `[theme.look]`

| name | words | action |
|---|---|---|
| `backdrop` | _missing_ | ordered bg preset names the brand turns through, one window per beat: scaffold-only, seeds `make scaffold`'s `bg[]`, never read at render (bg is required, engine-doctrine/MISTAKES.md #159) |
| `cuts` | default transition style, brand cut preference | the default and accent cut/transition names the brand favours |
| `field` | backdrop texture defaults, grain and vignette settings | grain and vignette defaults for the backdrop |
| `layout` | _missing_ | the anchor band (left/center/right) and margin every beat composes against |
| `marks` | _missing_ | the logo path plus its end-card and headline-adjacent sizes |
| `scale` | _missing_ | how big text should be: named px sizes for the hook / headline / body / caption roles at 16:9, so a layer writes `"size": "headline"` instead of guessing a number |

## three scene  `[three]`

| name | words | action |
|---|---|---|
| `codeAssemble` | _missing_ | thousands of GPU points flying from a seeded cloud to the exact glyph positions and resolving into readable code |
| `codeDissolve` | _missing_ | code resolving out of seeded noise behind a chromatic burn edge, then holding crisp |
| `codeExtrude` | _missing_ | syntax-coloured code on lit beveled slabs, rising out of depth and growing to thickness, top line first |
| `deviceShowcase` | _missing_ | a GLTF device body turning in real light with a LIVE HTML screen mapped onto it |
| `extrudeText` | _missing_ | a real font outline extruded into a lit 3D solid that rotates through space |
| `globe` | _missing_ | a rotating globe of land dots, with arcs available between coordinates |
| `liquidBackground` | _missing_ | a subdivided plane churning under summed sine displacement, specular highlights sliding across the swells |
| `litPlane` | _missing_ | a captured UI plane, LIT (MeshStandardMaterial under the studio rig, not MeshBasicMaterial), rising with a tilt and a rotation into a soft-shadowed ground with an overshoot settle; `motionBlur` (true, or a 0..1 strength, the same word every layer uses) opts it into a shutter-accumulated smear on the fast rise |
| `magnetic` | _missing_ | field lines arcing from pole to pole, traced from the real summed inverse-square field, with charges sliding along them |
| `pointCloud` | _missing_ | a GPU point cloud: thousands of lit points posed absolutely from t |
| `shatter` | _missing_ | one solid slab holds, then breaks into a seeded grid of shards that tumble outward and toward camera |
| `uiParallax` | _missing_ | flat UI planes stacked at depth, the camera moving past them so the layers separate |

## time remap  `[timeRemap]`

| name | words | action |
|---|---|---|
| `freeze` | _missing_ | FREEZE FRAME: the layer plays out over the first 60% of its window, then holds dead still on its own last pose for the rest of it |
| `hold` | _missing_ | the slow-motion HOLD: in fast, down to 30% speed across the middle half, out fast. The part you want the eye to actually read sits in the slow part |
| `rewind` | reverse, backwards, boomerang, ping-pong, play in reverse | plays to the end at double speed, then runs itself BACKWARDS to where it started. One layer, one window, and the return is the same motion reversed |
| `whip` | _missing_ | SPEED RAMP: crawls at 20% speed, whips through the middle at about 530%, lands slow. The AE three-key ramp, for a dull passage you want crossed fast |

## transition relationship  `[transition_why]`

| name | words | action |
|---|---|---|
| `act-break` | _missing_ | a beginning or an ending, an act break: open or close the film or a major section. Candidates: fade. |
| `continuity` | _missing_ | nothing: invisible, respects momentum. Two beats are one continuous thought. Candidates: none. |
| `contrast` | _missing_ | shock, jolt: end on maximum tonal contrast; wake from a dream. Candidates: none, flashWhite. |
| `new-place-energy` | _missing_ | frantic energy, momentum, "meanwhile": an energetic location or time change. Candidates: whipPan, wipe. |
| `rhyme` | _missing_ | "these two things are the same": bridge scenes by a visual or compositional rhyme. Candidates: none. |
| `same-action` | _missing_ | seamless, energy carried through: cut on a movement so the eye rides it past the seam. Candidates: match-on-action. |
| `same-object` | _missing_ | magic: the same identity across states, the highest-craft continuity there is. Candidates: shared-element morph. |
| `spatial-travel` | _missing_ | one world: the beats are PLACES, not claims. The content has a spatial logic worth walking. Candidates: camera travel. |
| `time` | _missing_ | passage of time, a connection, gentleness: link two images, soften, show time passing. Candidates: dissolve, fade. |

---
_710 primitives across 60 registries, 24 meeting the word-action contract today.
Regenerate: `make vocab`. Ratchet: `make word-action`._
