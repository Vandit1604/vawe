---
when: "you want to see every primitive the engine has, the plain words that find it, and the sentence that says what it does"
answers: "the full word-action listing every defineRegistry registry carries, grouped by registry, graded the same way make check GATE=word-action grades it"
group: reference
---

# PRIMITIVES VOCABULARY: every registry entry, its words, its action

> GENERATED by `scripts/site/vocab-catalog.mjs` (`make vocab`) from `registries()` in `core/registry/registry.js`. Do not edit.

Every row is one primitive from one `defineRegistry` call. **words** are its `aka` (never printed
elsewhere, folded into search only); **action** is its `blurb`. A row with either column blank
fails `make check GATE=word-action`: `words` needs 2+ phrases, `action` needs a stated number or a full
sentence naming the mechanism. See `quality/gates/word-action.mjs` for the exact contract.

## adjustment  `[kind]`

| name | words | action |
|---|---|---|
| `bloom` | glow, halo, soft glow | a real GLOW over everything beneath: blurs it 16px, lifts brightness 9% and screens the halo back over the sharp original so the subject stays crisp |
| `blur` | soften, rack focus, defocus | soften everything beneath by 14px of blur. The rack-focus of a whole beat, not of one layer |
| `brighten` | lighten, lift the exposure, brighten up | lift everything beneath by 30%, its default `amount`, raising brightness |
| `contrast` | punch up contrast, harden tones, more contrast | harden the tones beneath by 25%, its default `amount`, widening light against dark |
| `darken` | dim, shadow the frame, dim it down | dim everything beneath by 45%, its default `amount`, cutting brightness |
| `desaturate` | grey out, drain color, black and white | drain the colour beneath by 100%, its default `amount` of 1. 1 is fully grey |

## ambient shader  `[shader]`

| name | words | action |
|---|---|---|
| `aurora` | northern lights, colour wash, flowing colour glow | a drifting colour aurora: bands of colour slowly moving and blending across the whole frame |
| `auroraCurtain` | aurora curtain lines, undulating vertical light curtains | six vertical curtain lines undulating top to bottom, each drifting sideways on its own noise offset, fading warm to cool along its length. ported from pbakaus/radiant |
| `auroraVeil` | aurora ribbons over starfield, aurora veil with ice ground | seven wide aurora ribbons undulating over a starfield, with a frosted ice ground plane reflecting them below the horizon. the richest field here after bands. ported from pbakaus/radiant |
| `bands` | ramp panels, concentric light arcs, nested rounded panels of light | a ramp repeated over a scalar field (rotated panels, concentric arcs or nested rounded boxes) tinted by a gradient with a shaped light behind it. the most dialled effect here; engine-doctrine/LIGHTFIELD.md |
| `barrel` | lens vignette, barrel distortion, corner vignette with fringing | the LENS, not the picture: a corner vignette with a faint violet chromatic fringe riding the far edge only |
| `chromaticBloom` | glowing colour orbs, drifting bloom orbs on black | twelve luminous colour orbs (five vivid, seven dim) drifting and blending on black, each on its own noisy orbit, with a vignette and grain over the top. ported from pbakaus/radiant |
| `crt` | old CRT monitor, tube TV look, scanline overlay | a tube: 4px RGB phosphor stripes, scanlines, a corner vignette and a refresh bar rolling down. an OVERLAY |
| `curlSmoke` | rising smoke, ink plume, swirling smoke trails | a rising plume of ink or smoke that rolls into vortices as it climbs, its filaments stretching and folding. slow, continuous, and never repeating; the one field here with real fluid motion rather than a drifting pattern |
| `domainWarp` | marbled ink, warped fbm noise, folding ink pattern | marbled ink: fBm sampled through a domain that is itself two levels of fBm, so the field folds back over itself. the classic warp, and the only field here with real interior structure |
| `dotCrawl` | NTSC artifact, chroma crawl, old broadcast video artifact | the NTSC artifact: a fine diagonal chroma lattice creeping one subcarrier phase per frame, concentrated where there is detail. an OVERLAY |
| `drift` | bokeh drift, rising soft discs, floating light discs | seven big soft bokeh discs rising up the frame and blending as they pass |
| `filmGrain` | film grain overlay, grainy texture, dust and grain | grain re-struck 24 times a second per ~2px cell, plus dust specks; bright or dark only, never mid-grey, so contrast survives. an OVERLAY |
| `flow` | mesh gradient, soft blob gradient, flowing colour blobs | a soft mesh gradient: three big blobs drifting slowly over a vertical wash, the premium default |
| `gateWeave` | film projector look, projector gate weave, dusty film projection | a projector gate: the soft dark frame border, dust re-struck each projected frame and a hair that catches for a second or two, all riding ONE drifting offset so the picture appears to float |
| `godRays` | light shafts, crepuscular rays, sunbeams through canopy | shafts of light: sun through a canopy, beams through a window, crepuscular rays. the light sits just off the top edge, a drifting cloud of leaves breaks it into blades, and dust turns slowly inside the bright ones. the deepest field here, because the beams recede toward one point. it is BRIGHT through the middle, so drop `intensity` toward 0.4 before putting white type over it |
| `heatShimmer` | heat haze, rising heat wave, shimmering hot air | rising warm haze in fine wavy bands, strongest low in the frame and thinning as it climbs. self-generated. It does not warp what is beneath it |
| `kaleidoscope` | kaleidoscope effect, mirrored mandala, symmetric mirror pattern | a 6-fold mirrored mandala turning slowly and fading out toward the corners. A symmetric field of its own, never a mirror of your content |
| `laserLabyrinth` | sweeping light cones, laser beams through fog | six volumetric light cones sweeping down from just above the top edge through drifting fog, three dim and far, three bright and near with a rhythmic beat snap. ported from pbakaus/radiant |
| `lightLeak` | light leak overlay, warm edge glow, film light leak | three warm blobs drifting in from the edges on a loop, tinted from the palette. an OVERLAY |
| `matrixDecode` | digital rain, matrix code rain, falling glyph columns | digital rain: near-white heads falling down 44 glyph columns at per-column speeds, each dragging a fading tail. palette stop 0 tints it |
| `metaballs` | blobby merging circles, organic merging shapes, metaball blend | five signed-distance circles merging and parting on a polynomial smooth minimum, so they fuse into one body instead of overlapping |
| `mist` | soft haze, quiet noise field, subtle fog backdrop | near-still layered noise haze. The quietest field here, for a backdrop that must move without being noticed |
| `nebula` | deep space nebula, star field with gas clouds, cosmic dust field | deep-field gas clouds from three octaves of noise with a hot core, dusted with twinkling stars off a hashed grid |
| `plasma` | plasma effect, interference waves, crossed sine waves | two crossed sine waves interfering into a slow two-tone swell |
| `ripple` | water ripples, caustics effect, rings of light on water | gentle water caustics: three rings of cool light expanding and overlapping |
| `vhs` | VHS tape effect, retro tape distortion, analog tape look | tape: scanlines, magenta/cyan chroma snow, dropout streaks and a soft tracking band creeping up. an OVERLAY, place it ABOVE content |
| `voronoi` | cellular noise, worley noise, cell pattern with lit borders | cellular (Worley) noise: seeded cells drifting on their own loops, each one flat-tinted, with a lit line along every shared border |

## anim  `[anim]`

| name | words | action |
|---|---|---|
| `clock` | clock wipe, radial reveal, sweep reveal | radial sweep from 12 o'clock, clockwise |
| `defocus` | blur out exit, soft focus fade | blurs from 14px down to 0px while fading opacity in, leaves through focus rather than through space |
| `fade` | opacity only, fade in, no movement entrance | opacity ramps from 0 to 1 with no transform at all, the plainest entrance there is |
| `iris` | circle reveal, iris open, circular wipe | circular iris opens from the centre of the layer, 0% to 100% radius |
| `lift` | float up, lively entrance, staggered card lift | travels further than `pop` and settles alive, faces, avatars, a staggered row of cards |
| `none` | no entrance, appear instantly, no animation | no move and no fade. The layer just appears at its window edges |
| `pop` | spring in, bounce in, overshoot pop | springs past its size and settles back, a small confident arrival for a chip, a badge or an icon |
| `rise` | rise up, translate up and fade | translates 48px down to 0 while fading opacity 0 to 1, a slight overshoot past rest via easeOutSnap |
| `scale` | zoom in, grow from small | scales from 0.86 to 1 while fading in, a punch-in overshoot via easeOutBack |
| `slide-down` | slide in from above, drop from the top | enters 60px from below; as an `out`, leaves 60px downward |
| `slide-left` | enter from the right, slide in from the right side | enters 60px from the left edge; as an `out`, leaves 60px toward it |
| `slide-right` | enter from the left, slide in from the left side | enters 60px from the right edge; as an `out`, leaves 60px toward it |
| `slide-up` | slide in from below, rise from the bottom | enters 60px from above; as an `out`, leaves 60px upward |
| `up` | rise up, translate up and fade | translates 48px down to 0 while fading opacity 0 to 1, a slight overshoot past rest via easeOutSnap. The alias of `rise` |
| `wipe` | wipe right, reveal left to right, clip reveal | clip reveal grows rightward, 0% to 100% width. The default direction, same as `wipe-right` |
| `wipe-down` | reveal top to bottom, clip wipe down | clip reveal grows downward from the top edge, 0% to 100% height |
| `wipe-left` | reveal right to left, clip wipe left | clip reveal grows leftward from the right edge, 0% to 100% width |
| `wipe-right` | reveal left to right, clip wipe right | clip reveal grows rightward from the left edge, 0% to 100% width |
| `wipe-up` | reveal bottom to top, clip wipe up, bar chart reveal | clip reveal grows upward from the bottom edge, so a column grows upward instead of appearing |

## background preset  `[bg[].preset]`

| name | words | action |
|---|---|---|
| `accent` | brand color field, loud accent background, accent radial | the brand accent as a radial with rippling dots and a slow spotlight (moves), the loud brand field |
| `accentPlain` | flat accent color, solid brand color, plain brand field | the brand accent as a clean full-bleed field, grain only. FLAT, for plain sites whose hero is one colour |
| `aurora` | northern lights, colour wash, flowing colour glow | three drifting colour blobs at 0.46 intensity over a dark radial, the northern-lights field (moves) |
| `black` | pure black, true black background, solid black backdrop | literally #000000, no tint, no wash, no grain. FLAT, when the only light in the film is the subject itself |
| `blobs` | soft color blobs, airy gradient pools, separated color pools | the airier light wash: smaller, separated pools with white between them (moves). No grid: write `grid: true` on a softwash fx if you want the blueprint rules |
| `brandglow` | pulsing accent light, soft breathing glow, radial pulse | a breathing accent glow at 0.5 intensity plus rippling dots, over an ink radial base |
| `constellation` | connected nodes, network graph background, data telemetry field | drifting connected nodes (moves), telemetry/data feel: 78 particles connected within 150px |
| `dark` | plain dark background, dark radial, quiet dark backdrop | a plain dark radial, no dots. FLAT, the quiet backdrop for busy content |
| `deep` | deepest dark background, very dark radial, black-ish backdrop | the deepest plain radial, no dots. FLAT, when the content must own the whole frame |
| `dotmatrix` | halftone dots, printed dot pattern, newspaper dots | a light field ruled by dots 52px apart, whose size waves from 1.6px to 4.5px across it, the printed halftone look (moves) |
| `gradient` | custom gradient background, agent gradient, hex colour gradient | an agent-controlled colour gradient backdrop, linear/radial/conic from your own hex colours, angle and stops, or a named recipe (`opts.recipe`). FLAT unless `opts.kind` is "mesh", which moves it |
| `gradientWash` | mesh gradient wash, colour pool bleed, premium gradient background | one big saturated pool bleeding off a corner into white, a mesh gradient (moves), light and premium |
| `ink` | dark pulsing dots, ink black background, dark accent dots | dark radial with slow accent-tinted dots pulsing in place (moves), for a clean flat dark use `plain` + value:"dark" |
| `liquid` | liquid light, flowing colour folds, brand colour liquid | folds of the brand hue against true black (moves). It OWNS the frame, so quiet type on it and nothing else |
| `mesh` | gradient mesh, colour pool background, dark saturated mesh | soft gradient mesh, three colour blobs at 0.42 intensity over a dark radial (dark/saturated, check contrast, moves) |
| `metallic` | brushed metal, lit equaliser bars, shimmering light rods | vertical light rods with a travelling SHIMMER (brushed metal / lit equaliser), dramatic dark bg, brand-coloured |
| `metallicSheen` | quiet brushed metal, slow metallic sweep, subtle light rods | the quieter metallic: fewer, slower rods with a sweep crossing them (moves), a dark field type can sit on |
| `paper` | white background, editorial background, plain paper field | the paper gradient with grain only, FLAT, the white-first default when the motion lives in the content |
| `paperDots` | polka dot, dot grid pattern, light dotted texture | a faint drifting dot grid on the light paper base, dots 54px apart waving between 0.14 and 0.34 alpha |
| `paperShapes` | light geometric pattern, subtle shapes texture | faint drifting geometric shapes on the light paper base, 64px apart waving between 0.05 and 0.13 alpha |
| `plain` | solid color, flat color, no texture backdrop | a flat theme colour field with a 0.02 grain wash (0.035 on dark) |
| `soft` | gentle light radial, soft glow backdrop, light radial wash | gentle light radial with faint accent rings and discs drifting over it (moves) |
| `spotlight` | wandering light pool, moving spotlight, dark field with light | a deep field with one soft pool of light wandering across it on a slow cycle, the eye follows the bright patch (moves) |

## blend mode  `[mixBlend]`

| name | words | action |
|---|---|---|
| `color` | recolor a photo, color blend, tint and richness | takes tint and richness from the layer and brightness from the backdrop. The way to recolour a photo |
| `color-burn` | burn, crush shadows, darken hard | darkens the backdrop hard wherever the layer is deep, crushing the shadows |
| `color-dodge` | dodge, blow out highlights, brighten hard | brightens the backdrop hard wherever the layer is pale, blowing the highlights out |
| `darken` | darken blend, keep the darker pixel, darker of the two | keeps whichever is darker at every pixel, the layer or what is behind it |
| `difference` | invert overlap, glitch blend, subtract colors | subtracts one picture from the other, so matching areas go black and opposites invert. The classic glitch look |
| `exclusion` | soft difference, grey the midtones, muted invert | the same subtraction with the contrast taken out, greying the midtones instead of driving them to black |
| `hard-light` | harsh light blend, hard light, spotlight blend | as if a harsh lamp shone through the layer: strong, unsubtle contrast |
| `hue` | hue blend, recolor by hue, tint only | takes only the colour angle from the layer and keeps the backdrop's brightness and richness |
| `lighten` | lighten blend, keep the lighter pixel, lighter of the two | keeps whichever is lighter at every pixel, the layer or what is behind it |
| `luminosity` | luminosity blend, brightness only, inverse of color blend | takes only brightness from the layer and keeps the backdrop's tint. The inverse of `color` |
| `multiply` | darken like ink, multiply blend, stack two slides | darkens everything: white drops out and the rest deepens, the way ink sits on paper or two slides stack |
| `normal` | no blend, plain composite, opaque | no blending at all: the layer simply covers what is behind it. The default, and the way to turn a blend off |
| `overlay` | punch up contrast, overlay blend, contrast blend | deepens the dark areas and brightens the light ones at once, so contrast rises against the backdrop |
| `plus-lighter` | additive blend, add light, glow blend | adds the two pictures together, so light over light runs to white. For glows and additive light |
| `saturation` | saturation blend, richness only, colorfulness blend | takes only the richness from the layer and keeps the backdrop's tint and brightness |
| `screen` | lighten like light, screen blend, two projectors | lightens everything: black drops out and the rest brightens, the opposite of multiply, like two projectors on one wall |
| `soft-light` | soft light blend, diffuse light, gentle wash | as if a diffuse lamp shone through the layer: a gentle wash rather than a hit |

## camera dial  `[cameraBlur (a top-level boolean)]`

| name | words | action |
|---|---|---|
| `cameraBlur` | motion blur, camera motion blur, shutter blur, whip pan blur | CAMERA MOTION BLUR: blur the frame when the camera whips. Every layer smears by its velocity RELATIVE TO THE CAMERA, so a fast pan streaks the whole frame and a layer travelling with the camera stays sharp. Off by default; the film's `shutter` (degrees) says how much, and one layer opts out with `motionBlur: false`. A zoom and a roll are radial and are not modelled |

## camera move  `[cameraMove.move]`

| name | words | action |
|---|---|---|
| `cameraShake` | camera shake, impact shake, handheld jolt, screen shake | an IMPACT: a decaying ~16Hz shake pre-sampled at author time to one key per frame, then 0.1s of eased recovery so the frame LANDS instead of stopping |
| `diveIn` | zoom into, dive toward, punch toward a target, zoom to a point | zooms to scale 1.6 over 1.6s with easeOutQuart, centring the target point (tx,ty) as it grows |
| `dollyZoom` | vertigo shot, dolly zoom, trombone effect, hitchcock zoom | THE VERTIGO SHOT: the lens ramps while the camera holds its distance, so the subject on the picture plane keeps its exact size and the world BEHIND it rushes in or falls away · the only move here that changes the relationship between planes rather than the framing, and it needs layers standing at a `plane` depth or there is nothing to counter-scale against |
| `driftHold` | handheld, breathing camera, operator float, not locked off | a held frame that is never dead: a sub-12px Lissajous micro-drift, x and y at different frequencies so it breathes instead of walking a diagonal |
| `followCursor` | camera follows the cursor, track the mouse, follow the pointer, cursor-driven camera | THE CAMERA FOLLOWS THE CURSOR: derived from the `path` and `clicks` on a `cursor` layer, so the pointer stays the single owner of where the camera goes. It pushes toward the spot the pointer is about to click, arrives just BEFORE the click, holds across it and releases. Clicks too close in time or space share one framing, so six clicks are never six crash zooms |
| `followLayer` | track a layer, follow an element, camera tracks an element, keep a layer in frame | CAMERA TRACKS A LAYER BY ID, at RENDER time, off the same live-box accessor `follow` (the layer track) reads. Holds still while the target sits inside a soft margin of frame centre, translating only the minimum to keep it in once it would cross the edge, rather than rigidly re-centring it every frame (which reads as the world sliding, not the camera tracking). Zoom is HELD (`to`), not framed. The one move here `bakeCameraMove` resolves onto `data.cameraFollow`, not `data.camera`, and it cannot be composed with another leg |
| `hold` | locked off, static camera, no camera movement, camera still | the camera is LOCKED OFF: zero motion, on purpose. Not driftHold, which keeps breathing; this emits no keyframes at all, the explicit way to declare "no camera" instead of leaving `camera:` blank |
| `multiPhase` | multi-leg camera move, chained camera move, push then hold then settle | chains legs into one journey, each defaulting to a 1s linear hold except the last, which eases out with easeOutCubic |
| `orbit` | orbit around, circle the subject, 3d swing, rotate around | a gentle 3D swing around the frame (ry through 0) |
| `panFollow` | pan down, follow the content, track downward, scroll with typing | translates the camera by -300px on y over 5s, linear, to keep pace with downward-growing content |
| `punchIn` | crash zoom, punch zoom, hard zoom in, sudden zoom | a crash zoom: the frame accelerates AT you (easeInExpo), recoils past its resting scale, then rings back elastic. the only move here that is not a `.out` |
| `slowPush` | zoom in slowly, push in, creep closer, subtle zoom in | zooms from scale 1 to 1.12 (12% closer) over 6s with easeOutCubic, so the frame stays alive |
| `travel` | station to station, fly between points, multi-stop camera flight, tour the scene | station-to-station flight between points in STAGE coords. THE CAMERA AS THE TRANSITION (no cut) |
| `truck` | truck sideways, lateral pan, track sideways, slide across | plain lateral travel, linear, so it reads as tracking rather than a lurch |
| `workspaceZoomOut` | pull back to reveal, zoom out to the whole, reveal the workspace, back out | zooms out from scale 1.4 to 1 over 3s with easeOutCubic, pulling back from a detail to reveal the whole |

## camera word  `[cameraMove.move]`

| name | words | action |
|---|---|---|
| `circle` | orbit the subject, swing around, rotate around | the camera orbits around the subject, so the subject stays put and the view changes. |
| `dive` | dive in, plunge toward, crash toward a target | a fast move INTO the frame, ending close on one element. For entering a product surface. |
| `drift hold` | handheld hold, breathing camera, never fully still | a very slow move on a held frame, so a static shot is never fully still. |
| `follow` | track the subject, pan to keep up, follow the action | the camera pans to keep a moving subject in frame. |
| `pan stations` | stop at each station, guided tour, sequential pan | the same touring move, named for what it does: stopping at each station in order. |
| `pull back` | widen out, reveal the context, back away | the camera retreats to reveal the wider context around what you have been looking at. |
| `punch in` | crash zoom, sudden zoom in, hard cut closer | an abrupt jump closer, not a glide. Punctuation: it hits on a beat. |
| `push in` | move closer, zoom in slowly, tighten on the subject | the camera moves closer, slowly and continuously. Tightens attention without a cut. |
| `shake` | camera jolt, impact shake, screen shake | a short handheld jolt. An impact, a hit, a moment of instability. |
| `slow push-in` | creep closer, gradual zoom in, slow zoom | the same slow move closer, spelled the way a storyboard usually spells it. |
| `sweep` | sweep across, truck sideways, lateral move | the camera trucks sideways across the scene, revealing what was beyond the edge. |
| `tour` | multi-stop flight, visit several spots, station to station | a multi-leg move that visits several places in turn, one continuous shot rather than cuts. |
| `ui focus zoom` | zoom to a control, focus on a panel, highlight a UI element | a dive aimed at one control or panel, for showing exactly where a click lands. |
| `zoom out` | widen the shot, show the whole scene, back off | the same widening move, spelled the way most people ask for it. |

## canvas fx  `[canvasFx]`

| name | words | action |
|---|---|---|
| `ascii` | ascii art, text art image, terminal green text | one monospace glyph per cell, picked off a light-to-dark ramp; phosphor green on near-black. this pass IS the terminal look, so it ignores the theme |
| `crosshatch` | pencil sketch, engraving lines, cross hatching | pencil / engraving: diagonal strokes whose density STEPS up with darkness (one, then a cross, then a vertical), ink on a warm paper stock |
| `dither` | ordered dither, two tone bayer, black and white dither | Bayer 4x4 ordered dither down to TWO tones, theme ink on theme paper. No greys anywhere |
| `edgeDetect` | sobel edges, outline only, blueprint lines | a Sobel magnitude: the outline only, bright lines on a dark ground. the base of the `blueprint` preset |
| `halftone` | newspaper print, newspaper dots | one ink dot per cell on a paper ground, its radius growing with that cell's darkness |
| `mosaic` | pixelate, pixelated image, blocky image | pixelate: every cell filled flat with its own average colour |
| `pixelSort` | glitch sort, databend smear, sorted pixels glitch | sorts each contiguous BRIGHT run of a scan line by luma. The signature glitch smear, and deterministic: the span is bounded by a threshold, nothing is random |
| `stipple` | pointillism, ink dot texture, seeded dots | seeded ink dots on paper, denser where the source is dark, each jittered inside its own cell |

## caption style  `[captionStyle]`

| name | words | action |
|---|---|---|
| `clipWipe` | wipe reveal captions, clip path karaoke, accent wipe subtitles | LINE-level: an accent copy of the line is revealed left to right, the wipe front tracking the spoken word rather than wall-clock time |
| `flipUp` | 3d flip captions, hinge up subtitles, card flip karaoke | the word hinges up from edge-on · an upcoming word sits at -90deg, which is invisible without being dim, so it costs the text no contrast at all |
| `focusPull` | blur to sharp captions, rack focus subtitles, out of focus subtitles | a rack focus: an upcoming word sits 2.8px out of focus and resolves sharp as it is spoken, a spoken one settling back to a soft 1px · the one style that says its state with focus rather than with ink, light or size |
| `ghostSplit` | rgb split captions, chromatic aberration subtitles, double vision karaoke | two offset ghosts converge as the word is spoken, the accent one way and a muted ink the other · a split reads as a split from the OFFSET, not from being red and blue, so it stays on the theme |
| `highlight` | marker highlight, highlighter sweep, underline sweep | a marker band sweeps left to right behind the active word, the accent mixed 28% into transparent so white text over it stays at 4.9:1 contrast or better |
| `inkFill` | karaoke fill through letters, gradient fill subtitles, color fill karaoke | the karaoke fill runs through the GLYPHS rather than behind them, full ink trailing a narrow accent front · the word body is never the accent, so its contrast is that of the plate itself |
| `kineticSlam` | punch in captions, impact word subtitles, oversize word slam | the word lands at 1.22 with its tracking open and settles cubically to 1, so the travel is all in the first third of its window and the rest holds still |
| `letterRise` | letters rise into place subtitles, per character rise captions | the letters of the whole line rise the last 12px into their slots as they are spoken, one per CHARACTER · not a second typewriter: the line is present from the first frame, where typeOn assembles it out of nothing |
| `neonEdge` | glowing captions, neon glow subtitles, halo karaoke | the accent lives only in a halo: the spoken word blooms and settles, earlier words keep a quieter glow, upcoming words hold a 76% text-mix with no light at all |
| `pillKaraoke` | pill fill karaoke, progress bar captions, fill bar subtitles | a pill fill sweeps left to right through the line, the accent mixed 42% into the bg so the bg stays dominant |
| `readerFocus` | teleprompter captions, dimmed unread subtitles, read-ahead captions | a teleprompter: three ink levels and three scales, upcoming at 76% and 0.90, spoken at 88% and 0.96, the current word full ink at 1 |
| `scramble` | decode subtitles, scramble captions, matrix style karaoke | the letters settle out of noise, left to right · the only style that rewrites the text rather than its style, so it carries the same carve-out clipWipe does |
| `typeOn` | typewriter subtitles, typing captions, letter by letter subtitles | a typewriter, per CHARACTER: an unarrived letter holds its space at visibility:hidden so the line never reflows, and the current one carries an inset accent caret |
| `underlineDraw` | underline karaoke, rule draws under word, quiet highlight subtitles | a 4px accent rule draws under each word as it is spoken and stays, the quiet sibling of highlight: it sits below the ink, so it costs the text no contrast |
| `waveRide` | bobbing captions, word bounces as spoken, wave subtitles | one crest per word, ridden as it is spoken · a half-sine is bounded by the word window, where a looping wave would simply be cut off by it |
| `weightShift` | bold the spoken word, weight ramp captions, font weight karaoke | the spoken word RAMPS along the font's own wght axis to 800 with a small rise-and-settle bump, the rest hold 600 at a 76% text-mix |
| `weightWave` | weight crest captions, variable font wave subtitles, bold wave karaoke | a crest of WEIGHT travels the line, each word riding the font's wght axis from 500 to 900 and back · the one register a static font cannot fake, and the line breathes as the crest passes |
| `wordFlash` | one word at a time subtitles, big word captions, TikTok style captions | ONE word on screen, swapped whole at the next onset, landing at 1.14 and settling cubically · the default of short-form video, and it needs no dimming because the unread words are absent, not faint |
| `wordSlide` | one word slides up subtitles, big word slide captions | the same one-word swap arriving from 26px below instead of from scale · for a film already moving vertically, where a second unrelated motion would fight it |

## comparative duration word  `[enterDur / exitDur / duration (relative)]`

| name | words | action |
|---|---|---|
| `faster` | quicker, snappier, speed up, speed it up, tighten the timing | one step toward `instant` on the duration ladder: whatever is there now, a touch quicker. |
| `slower` | more deliberate, slow down, slow it down, ease off, give it more time | one step toward `luxurious` on the duration ladder: whatever is there now, more deliberate. |

## composition  `[comp]`

| name | words | action |
|---|---|---|
| `commaSplit` | comma separated values animation, delimited line to table | a delimited line pulls itself apart into a table: each comma flies to the gutter, shrinks to a point, and a column rule grows out of that same point, so the delimiter visibly becomes the structure |
| `pipelineFlow` | pipeline diagram, flow chart animation, cards and connectors | staged pipeline: cards pop in, connectors draw, a token travels each link, a check draws on (one hand-authored timeline) |

## cursor style  `[style]`

| name | words | action |
|---|---|---|
| `arrow` | mouse pointer, arrow cursor, default cursor | the default macOS pointer arrow, unchanged pixels from before this registry existed |
| `block` | terminal cursor, block caret, block cursor | a filled terminal cell that blinks on a fixed cadence, a block text cursor |
| `hand` | pointer hand, hand cursor, link cursor, clickable cursor | a pointing hand, for hovering a clickable button or link in a product demo |
| `ibeam` | i-beam, text cursor, text selection cursor | a text-selection I-beam: an 8px serif cap at top and bottom joined by a 17.6px vertical stroke, 2.1px wide, for hovering editable or selectable text |

## cut  `[cut]`

| name | words | action |
|---|---|---|
| `barn` | barn doors, curtains open, centre split | barn doors open from the centre outward, cinematic opener; masks, so a whole-frame cut needs sceneUnits |
| `blinds` | venetian blinds, slat wipe, blinds open | venetian slat mask sweeps open, editorial reveal; masks, so a whole-frame cut needs sceneUnits |
| `blur` | blur dissolve, defocus fade, soft blur cut | resolves out of a 16px defocus, calm, premium |
| `clock` | clock wipe, radial sweep, clock hand sweep | clock-hand sweep reveal, masks, so a whole-frame cut needs sceneUnits |
| `collapse` | fold down, vertical collapse, squash to a line | vertical fold, scaleY from 0.05 up to 1, terminal and data beats |
| `cube` | cube turn, rotate cube, cube rotation | perspective hinge with travel, the beats turning like faces of a cube |
| `drop` | drop in, falls in, gravity drop | falls in from above under gravity, leaves by falling away |
| `fade` | cross fade, dissolve, fade to black | opacity only, 0 to 1 over the whole cut window, masks nothing, so a whole-frame cut needs sceneUnits |
| `flip` | card flip, page flip, hinge flip | perspective hinge flip about an edge, rotates up to 55 degrees through a 1400px perspective, cards and panels |
| `iris` | iris in, circle reveal, spotlight reveal | circular reveal growing from a point (cx/cy), masks, so a whole-frame cut needs sceneUnits |
| `jitter` | glitch shake, alarm jitter, quake cut | decaying deterministic shake, up to 14px of jitter, alarm and glitch beats only |
| `letterbox` | cinema bars, letterbox open, curtains top and bottom | cinema curtains open and close top and bottom, cinematic opener; masks, so a whole-frame cut needs sceneUnits |
| `matchCut` | graphic match, invisible cut, shape match cut | a GRAPHIC MATCH: both beats are clipped to the same circle at cx/cy, the content swaps inside it at the midpoint, and the shape opens back out. The eye follows one form across the join. Unlike `iris`, the shape belongs to BOTH shots, which is what makes it a match rather than a reveal. You still have to place the two subjects at the same size and spot; masks, so a whole-frame cut needs sceneUnits |
| `none` | hard cut, straight cut, no transition | no transition at all, the beats simply replace each other. Masks nothing and moves nothing, so a whole-frame cut needs sceneUnits |
| `punch` | scale punch, punch in, zoom burst | scale burst, the leaving beat bursts past the camera, product focus |
| `rise` | rise up, float up, rise and fade | translates up along the cut distance (default 90px) while fading in from 0 to 1 |
| `riseBlur` | blurred rise, defocused rise, slow blur rise | slow rise through a 22px defocus, premium slow beats |
| `roll` | tilt roll, corner tilt, roll in | tilts in from a corner (7 degrees) and settles level |
| `skewWhip` | skewed whip, sheared whip, shear pan | sheared throw whose shear straightens as it lands, velocity you can read in the letterforms, same-background beats only |
| `slide` | push cut, slide over, travel cut | the frame travels one way, dir-aware. The plain workhorse, between same-background beats only |
| `softiris` | feathered iris, soft circle reveal, soft spotlight | feathered circular reveal from a point (cx/cy), masks, so a whole-frame cut needs sceneUnits |
| `softwipe` | feathered wipe, soft edge wipe, gradient wipe | feathered wipe, a 20%-wide gradient band instead of a hard line; masks, so a whole-frame cut needs sceneUnits |
| `spin` | spin in, rotate and scale, seal spin | rotates in 90 degrees while scaling from 0.5 to 1, logos, badges, seals |
| `squeeze` | smear cut, stretch cut, speed smear | smear-stretch along the travel axis, a speed ramp you can see |
| `whip` | whip pan, swish pan, blur throw | motion-blurred directional throw, travels 3.2x the cut distance and blurs up to 14px, momentum, between same-background beats only |
| `wipe` | hard wipe, directional wipe, edge wipe | hard directional reveal, playful, "notice the cut"; masks, so a whole-frame cut needs sceneUnits |
| `zoom` | zoom through, push through, dolly cut | push-through: the leaving beat shrinks away, the arriving one lands from too close, product focus |

## cut timing  `[cutTiming]`

| name | words | action |
|---|---|---|
| `brake` | slows into place, decelerating entrance, eases to a stop | decel(t, 2.4): the mirror power curve, fast at the start and slows into the stop. The entrance curve, it arrives slower than it set off |
| `linear` | no easing, constant speed, robotic, mechanical timing | no easing at all, straight 1:1 progress. A machine, a wipe with no personality, a ticker |
| `out` | decelerate in, default arrival, settles in place | easeOutCubic: decelerates into place with no overshoot, the default feel for something appearing on screen |
| `pop` | overshoot and settle, bouncy pop, past the mark | easeOutBack: overshoots past the mark by about 10% and comes back. Playful, use once |
| `ramp` | slow fast slow, editor speed ramp, whip pan curve | speedRamp(t): velocity is low at both ends and peaks at the midpoint (sharp=2.4). The editor's slow-fast-slow speed ramp, for a whip or a camera throw |
| `rush` | speeds up leaving, accelerating exit, takes off | accel(t, 2.4): a power curve of exponent 2.4, starts slow and keeps speeding up. The exit curve, it leaves faster than it left rest |
| `smooth` | gentle both ends, ease in and out, soft travel | easeInOutCubic: eases at both ends, cubic in then cubic out. Reads as travel rather than as arrival |
| `snappy` | quick and decisive, fast no bounce, crisp landing | easeOutQuart: lands fast and settles crisply, quicker off the mark than cubic and it never overshoots its target |
| `spring` | spring physics, bounces into place, physical landing | easeOutSpring: a damped-spring overshoot that settles, physical life for something landing: a badge, a chip, a number |

## depth  `[depth]`

| name | words | action |
|---|---|---|
| `back` | behind the subject, parallax background, layer that passes slower | behind the picture plane: the layer the camera passes, the one that gives the move its parallax. Held, 0.73x the rate |
| `far` | far plane, background depth, distant layer | the far plane: a backdrop, a wall, a field the subject stands in front of. Held at its laid-out size, moved by the camera at 0.57x the picture plane's rate |
| `front` | just in front, floats ahead, foreground chip | just in front of the picture plane: a caption or a chip that rides ahead of the subject. Held, 1.18x the rate |
| `near` | nearest the eye, foreground depth, closest layer | nearest the eye: the thing that crosses the frame fastest and leaves it first. Held, 1.39x the rate |

## destination  `[destination]`

| name | words | action |
|---|---|---|
| `broadcast` | tv broadcast, title safe for tv | the classic title-safe 90% box, 5% off every edge, for displays that overscan |
| `feed` | instagram feed post, x feed post | Instagram and X in-feed, where the player furniture sits OUTSIDE the media, so nothing is painted over the picture and only the margin applies |
| `reels` | instagram reels, reels video | Instagram Reels on 9:16: a 14% right rail, 10% off the top, 22% off the bottom. Looser than tiktok, tighter than shorts |
| `shorts` | youtube shorts, shorts video | YouTube Shorts on 9:16, the most generous phone target: a 13% right rail, 8% off the top, 16% off the bottom |
| `tiktok` | tiktok video, tiktok upload | the tightest phone target: a rail down the RIGHT at 16.7% of the width for the action buttons, 12.5% off the top and 30.2% off the bottom for the caption and handle. Serves 9:16 |
| `web` | website hero, no platform chrome | no platform chrome at all: the whole frame is usable and only the 4% margin applies. A site hero, an X or LinkedIn post, a docs clip |

## duration word  `[enterDur]`

| name | words | action |
|---|---|---|
| `expressive` | emphasis pace, bigger moment, occasional-motion tier | 0.4s, Carbon's emphasis tier for an occasional bigger moment: slower than `productive` on purpose, so the one that matters reads as the one that matters. |
| `fast` | quick, brief, snappy pace | 0.18s, under a fifth of a second. A payoff snapping into place, a value ticking over. |
| `instant` | snap, no visible motion, immediate | 0.08s, about a twelfth of a second. Barely perceptible as motion: a state flip that must not feel animated. |
| `luxurious` | lingering, held, extended hold | 1.2s, over a second. The pace of a hero moment, and monotonous if more than one beat uses it. |
| `medium` | default speed, normal pace, moderate | 0.3s, BASE_ENTER, the engine's own default entrance. About a third of a second, reach past it on purpose. |
| `productive` | utilitarian pace, small UI move, frequent-motion tier | 0.22s, Carbon's utilitarian tier for small, frequent UI-adjacent moves: quick enough that the motion never costs the viewer attention. |
| `slow` | deliberate, unhurried, takes its time | 0.6s, a little over half a second. A thesis line taking its time, a deliberate reveal. |

## easing  `[ease]`

| name | words | action |
|---|---|---|
| `brake` | decelerate in, slows to a stop, braking curve | half the distance in the first quarter, then a long decline to a stop |
| `easeInBack` | wind-up exit, anticipation before launch, pulls back first | pulls the opposite way first, a wind-up, before it goes |
| `easeInBounce` | bounces before departing, ball gathering bounce, bouncy exit | bounces in place before it departs, like a ball gathering itself |
| `easeInCirc` | hard late turn, creeping then whipping away, circular exit | creeps, then whips away on a curve that turns hard at the last moment |
| `easeInCubic` | strong ease in, gathers speed, committed departure | gathers pace with real commitment, stronger than quad. A departure that should read as decided |
| `easeInElastic` | wobbly wind-up, loudest departure, swings before leaving | swings in place with growing wobble before it leaves. The loudest departure here |
| `easeInExpo` | violent departure, explosive exit, sudden launch | barely stirs, then clears frame all at once. The most violent departure available |
| `easeInOutBack` | playful wind-up and overshoot, loud back curve, winds up and passes | winds up, crosses, passes the mark and comes back. Playful at both ends and loud |
| `easeInOutBounce` | cartoon bounce both ends, bouncy travel, double bounce | bounces at both ends. Cartoon physics, and it reads as exactly that |
| `easeInOutCirc` | mechanical flat curve, reads as machinery, sudden flat motion | flat, sudden, flat. Reads as machinery, not as anything with muscle |
| `easeInOutCubic` | default travel curve, smooth both ends, standard cross-frame move | the everyday travel curve, soft at both ends. A layer crossing from one place to another on screen |
| `easeInOutElastic` | wobble at both ends, loud elastic curve, double wobble | wobbles at both ends. Almost always more than a film wants |
| `easeInOutExpo` | theatrical hold and fling, dramatic curve, held then flung | held, flung, held. The most theatrical of the symmetric curves and the easiest to overuse |
| `easeInOutQuad` | least dramatic curve, even middle ease, subtle travel | soft at both ends with an even middle, the least dramatic way to carry something across the frame |
| `easeInOutQuart` | deliberate long journey, unhurried travel, fast middle slow ends | unhurried at both ends, fast through the middle. A long journey that should read as deliberate |
| `easeInOutQuint` | dramatic long journey, lingers then hurries, strong hold both ends | lingers at both ends and hurries the middle, dramatic across a long journey |
| `easeInOutSine` | ambient drift curve, quietest shift, sine wave ease | the quietest way to shift anything. Ambient drift, backdrops, anything not asking to be watched |
| `easeInQuad` | mild departure, gentle acceleration, soft exit | leaves gently and keeps gathering pace, the mildest departure here. For an exit that should not feel yanked |
| `easeInQuart` | heavy departure, strong wind-up exit, weighted launch | builds speed hard before it clears frame, a departure with weight behind it |
| `easeInQuint` | extreme departure, almost still then gone, sharp exit | almost still, then gone. The most extreme departure short of exponential |
| `easeInSine` | gentlest departure, soft pickup, barely accelerates | the softest pick-up there is, barely perceptible as gathering pace |
| `easeOutBack` | overshoot landing, small bounce pop, pass and return | passes its target and returns, a small pop of emphasis on something landing |
| `easeOutBounce` | ball drop landing, bounces to rest, bouncy arrival | drops and bounces to rest, a ball meeting a floor |
| `easeOutCirc` | machined landing, flattens off fast, circular arrival | comes in at speed and flattens off almost immediately, a machined landing rather than a living one |
| `easeOutCubic` | default landing, standard ease out, everyday arrival | the everyday landing: quick off the mark, unhurried into place. Reach for it when nothing argues otherwise |
| `easeOutElastic` | springy landing, wobble settle, rubber band landing | passes its target repeatedly with a decaying wobble, the springiest landing available |
| `easeOutExpo` | instant arrival, violent landing, exponential settle | the most violent arrival: effectively there on the first frames, then a long quiet settle |
| `easeOutQuad` | gentle landing, soft stop, mild ease out | comes in quickly then rolls to a stop, the gentlest landing of the power curves. Small elements, short distances |
| `easeOutQuart` | snappy landing, crisp arrival, fast settle | lands fast and settles crisply, snappier than cubic and it never passes its target. Good on an element arriving |
| `easeOutQuint` | very sharp arrival, nearly instant landing, extreme ease out | covers nearly all the distance at once then creeps the last of it, a very sharp arrival |
| `easeOutSine` | gentlest landing, soft fade to stop, quiet arrival | the softest landing there is, closer to speed fading away than to stopping |
| `emphasized` | hero curve, material emphasis, draw the eye | MD3's hero curve, cubic-bezier(0.05,0.7,0.1,1): the decelerate half of its two-part emphasis pair, for the one moment that should draw the eye |
| `enter` | decelerate curve, material entrance, arriving from off-frame | MD3's decelerate curve, cubic-bezier(0,0,0,1): the entrance half of this engine's own decelerate/accelerate rule, spelled by intent |
| `exit` | accelerate curve, material exit, leaving the frame | MD3's accelerate curve, cubic-bezier(0.3,0,1,1): the exit half of the same rule, picked when a layer is leaving rather than arriving |
| `hold` | stepped hold, jump-cut value, parks then jumps | does nothing whatever until the last instant, then jumps. Parks a value across a span rather than moving it |
| `linear` | constant speed, no easing, mechanical motion | no acceleration at all, constant speed start to finish. Right for a loop or a marquee, wrong for anything a viewer watches arrive |
| `ramp` | slow fast slow, symmetric speed ramp, gathers then tails off | mild and symmetric: gathers pace, crosses the middle at full speed, tails off |
| `rush` | leaves in a hurry, holds back then rushes, late acceleration | holds back for most of the span then covers the distance late. The exit curve: it leaves in a hurry |
| `settle` | immediate move then ease, quick then stops dead, default entrance settle | most of the move happens immediately, then it eases the remainder and stops dead |
| `snap` | near-instant landing, fast with a hair of overshoot, default layer snap | covers the distance almost at once with a hair of overshoot, the fastest landing that still reads as movement |
| `spring` | physical landing, passes and falls back, spring physics | damping ratio 0.65 (35% bounce): enough give to pass the mark and fall back, settling in under a second |
| `spring-bouncy` | playful spring bounce, visible spring wobble, bouncy spring landing | damping ratio 0.45 (55% bounce), the least stiff spring here: swings past the mark and takes over a second to settle. The playful one |
| `spring-stiff` | firm no-wobble settle, early motion then settle, tight spring travel | damping ratio 0.88 (12% bounce): high stiffness, almost no give, and the fastest settle of the springs here at about half a second |
| `springEase` | damped spring settle, nearly-arrived creep, flat spring curve | damping fraction 1 (critically damped) at a 0.5s response: nearly arrived by a third of a second, then creeping the last fraction |
| `springStiff` | no-overshoot spring, tight firm settle, stiff spring landing | critically damped, damping ratio 1: all stiffness and no give, so it never passes the mark, settling about as fast as `spring` |
| `standard` | default MD3 curve, material motion, most transitions | MD3's default curve, cubic-bezier(0.2,0,0,1): the everyday transition when nothing argues for enter, exit or emphasis |

## effector drive  `[effector.drives{}]`

| name | words | action |
|---|---|---|
| `opacity` | fade, dim, transparency drive | added to 1. `opacity: -0.7` dims a fully affected clone to 0.3 |
| `push` | push away, repel, shove outward, radial push | pixels ALONG the vector from the point to the clone. Positive shoves clones away, negative pulls them in. The only drive that reads the direction as well as the distance |
| `rotate` | spin, turn, rotation drive | proximity spent on rotation, in degrees. `rotate: 25` turns a fully affected clone by 25 |
| `scale` | grow, scale up, swell | what an element's proximity to the moving point is SPENT on: added to 1, so `scale: 0.6` swells a fully affected clone to 1.6 |
| `x` | sideways offset, horizontal offset | pixels, a sideways offset that does not depend on where the point is |
| `y` | vertical offset, up-down offset | pixels, the same offset as x but on the vertical axis, independent of where the moving point sits |

## effector falloff  `[effector.falloff]`

| name | words | action |
|---|---|---|
| `linear` | straight ramp, plain distance falloff, linear influence | proximity: each element reacts by its DISTANCE from the moving point, straight from full at the point to nothing at the radius. The plain ramp |
| `smooth` | smoothstep falloff, eased falloff, soft-edged falloff | proximity by distance from a moving point, on a smoothstep: flat at both ends, so an element eases into the point's reach and out of it. The default |
| `sphere` | dome falloff, spherical falloff, bubble falloff | proximity by distance, as a dome: elements near the moving point react fully, then a cliff. Reads as an object passing beneath them |
| `step` | hard-edge falloff, on-off falloff, binary falloff, no falloff | no falloff: an element reacts fully inside the moving point's radius and not at all outside. A hard-edged pass, hover-like |

## energy  `[energy]`

| name | words | action |
|---|---|---|
| `brand` | house speed ramp, default film energy, confident directed pace | the house slow-fast-slow speed ramp on every cut. Directed and confident, the default to reach for |
| `calm` | unhurried film, editorial pace, premium slow cuts | unhurried: every cut decelerates into place. Premium, editorial, a film with room to breathe |
| `hype` | launch-reel energy, decisive cuts, product drop energy | decisive: cuts land and stop with no drift. Launch-reel energy, product drops, announcements |
| `tense` | restless film, urgent cuts, countdown energy | restless: cuts accelerate away. Urgency, countdowns, a film that will not sit still |

## envelope anchor  `[envelope.anchor]`

| name | words | action |
|---|---|---|
| `bottom` | grows from the bottom, rooted at the bottom edge | the element grows UP from the bottom edge, so the free end, the one `taper` narrows, is at the top. The default |
| `top` | hangs from the top, rooted at the top edge | the element hangs DOWN from the top edge, so the free end, the one `taper` narrows, is at the bottom |

## envelope shape  `[envelope.kind]`

| name | words | action |
|---|---|---|
| `arch` | a bump, a rounded hump | a sine hump. It leaves the baseline at a finite slope and its shoulders sag, so it reads as a bump, not a dome |
| `circle` | a dome, a semicircle, a planet limb | the unit semicircular arc, which leaves the baseline UPRIGHT. A symmetric dome, a planet limb, an eclipse |
| `crescent` | a moon shape, a crescent moon | one circular arc with a second equal arc bitten out of it. A moon horn: empty on one side, a concave inner edge, a point at the tip |
| `full` | no shape, runs the whole frame | the no-op envelope, 1 everywhere: every element runs the whole frame, which is what a blind does |
| `hills` | rolling hills, rolling ground | three gaussians of unequal width and height summed, rolling ground: three summits with soft saddles between them |
| `ramp` | diagonal climb, rising slope | a straight climb across the row, so the extents rise steadily from one side to the other |
| `scallops` | a scalloped edge, an arcade of arches | the same semicircle repeated five times. Odd, so one arc sits centred. A scalloped horizon when shallow, an arcade when tall |
| `valley` | a dip, an upside-down hump | the sine hump run upside down: a dip in the middle with both ends tall |
| `wave` | a ripple, rises and falls twice | a sinusoidal ripple across the row, so the extents rise and fall more than once between the two ends |

## feel word  `[ease]`

| name | words | action |
|---|---|---|
| `bouncy` | spring bounce, springy landing, visible bounce | a spring with heavy give and low stiffness, damping ratio 0.45 (55% bounce): swings past the mark before it settles, over a second to rest. Playful, and loud: one per film at most. |
| `elastic` | rubber band, wobble, repeated overshoot | overshoots repeatedly with a decaying wobble. The loudest option here and rarely the right one. |
| `gentle` | ambient drift, shallow ease, unhurried background motion | slow at both ends, but shallower than smooth. Ambient drift, backgrounds, anything not asking for attention. |
| `heavy` | weighty, massive, slow to start and stop, ponderous | slow at both ends and slow in the middle: reads as mass. Big panels and full-frame moves. |
| `mechanical` | robotic, constant speed, no acceleration, machine-like | no acceleration at all, constant speed start to finish. Correct for a loop or a marquee, wrong for anything a person is meant to watch arrive. |
| `pop` | overshoot and settle, bounce back into place, little kick | overshoots the target and comes back: a small bounce of emphasis on something as it lands. |
| `sharp` | abrupt stop, hard slowdown, instant departure | leaves instantly and decelerates steeply. The most aggressive slow-down-at-the-end available. |
| `smooth` | glide, gliding, ease in and out, flowing motion | eases in and eases out, slow at both ends. The safe choice for anything travelling a long way across frame. |
| `snappy` | springy, crisp, punchy | quick and decisive: moves off fast and slows down hard at the end. The default for a UI element landing. |
| `soft` | settle in, gentle landing, no overshoot, cushioned stop | arrives and settles, with the overshoot damped out. Use where a hard stop would read as a collision. |
| `stiff` | tight spring, barely any give, crisp spring | a spring with high stiffness and almost no give, damping ratio 0.88 (12% bounce): settles in about half a second with barely a wobble. Springs that must not look playful. |

## field motion  `[motion.kind]`

| name | words | action |
|---|---|---|
| `breathe` | pulsing field, slow swell | the whole field swells and settles on one slow cycle, the cells keeping their relation to each other |
| `drift` | slow float, moving as one body | the field travels as one body, the cells holding station against each other |
| `shimmer` | glimmering seams, cells sliding against each other | the cells slide against each other, so the seams open and close |
| `still` | frozen field, no motion at all | nothing moves: the output reads no clock at all, held on one fixed frame with zero drift |

## filter  `[filter]`

| name | words | action |
|---|---|---|
| `bloom` | glow highlights, luminance bloom, light bleed | thresholds LUMINANCE and blooms the bright parts at two scales, light comes out of the picture, so a dark edge emits nothing. `ry` makes it directional (an anamorphic streak) |
| `chromaGlow` | neon text glow, soft glyph glow, chromatic glow | a stack of zero-offset drop-shadows: white core, warm mid halo, cool outer. It follows the ALPHA, so it is right on GLYPHS and wrong on an opaque picture, where it haloes the rectangle. For a photo use `bloom` |
| `chromaSplit` | chromatic aberration, rgb split, colour fringing | the colour channels pulled apart per pixel and summed back, real fringing on the picture, not a coloured silhouette of its alpha. `warm`/`cool` tint each direction |
| `convolve` | kernel filter, emboss or edge, sharpen kernel | a 3x3 kernel reading NEIGHBOURING pixels: emboss (a lit rubbing), edge (flat areas cancel to black, only boundaries survive), sharpen |
| `displace` | turbulence warp, pixel push, noise displacement | static turbulence drives a displacement map. The pixels are pushed around by a fixed noise field. `freq` sets lump size (low = few big lumps), `scale` sets how far |
| `duotone` | two color grade, two tone, poster press look | luminance remapped to TWO colours, shadows to highlights. The poster/press look; defaults to --ink and --accent, so it reskins per theme |
| `goo` | metaball merge, gooey blend, liquid shape fuse | the GOOEY / METABALL threshold, and the one primitive that can morph SEVERAL elements as one body: blur the group, then push the blurred ALPHA back through a hard ramp, so neighbouring shapes grow a liquid bridge as they approach and snap apart as they part. Put it on the PARENT (a `group`, or an `html` layer whose children move), never on one shape: one shape has nothing to fuse with. `goo:14` sets the blur radius (how far shapes reach for each other), `goo:14,26` also sets the ramp hardness. Bigger radius = longer bridges; softer ramp = wetter edge |
| `gradientMap` | heat ramp, false color grade, luminance recolor | luminance remapped across any number of stops. The general case the two above are special cases of: a heat ramp, a risograph, a false-colour read |
| `morph` | dilate or erode, fatten or thin, grow shrink pixels | dilate or erode, swell the bright pixels into their neighbours, or eat them away. Type gains or loses weight |
| `posterize` | banding, color quantize, reduce color levels | each channel quantised to N discrete levels IN PLACE, hues kept. Banding as a decision, not an artefact |
| `relief` | lit bump map, embossed light, surface lighting | a light source over a luminance bump map. Diffuse MULTIPLIES (ink pressed into stock), specular ADDS (a highlight on metal): same primitive, opposite composite |
| `sepia` | sepia tone, old photo tint, warm and dated | the plain CSS sepia, an amount 0..1. The cheapest warm-and-dated pass there is |
| `thermalBlur` | thermal blur type, heat camera type, glowing type ramp | the After Effects THERMAL BLUR: blur first, then remap the falloff. White cores, an orange body, a red rim, thin strokes eaten by the ramp. On a TRANSPARENT layer the ramp bottom stops land where the alpha has already gone, so the blue rim needs the type on an opaque black plate under `screen` (see the header). `gradientMap` recolours a picture that already has midtones; this one MAKES the midtones, which is why it is the one that works on TYPE. `thermalBlur:8` sets the near radius |
| `tritone` | three color grade, three tone, risograph grade | duotone with a third stop in the middle, which is what stops the midtones going muddy |
| `vignette` | darkened corners, lens falloff, edge darkening | NOT a filter. A darkening field composited over the layer box, so it is an inset radial-gradient overlay div and stays sharp at the edges |

## generator  `[generator]`

| name | words | action |
|---|---|---|
| `bands` | ramp panels, concentric arcs of light | A ramp repeated over a field: panels, concentric arcs or nested rounds, with a light behind. |
| `colonnade` | tall columns of light, panels split by hairlines | Wide panels split by bright hairlines, soft masses under a glow. |
| `crt` | scanline overlay, old tv screen effect, phosphor bloom | A cathode ray tube: the picture under it goes soft and blooms, then scanlines and a corner falloff go over the top. |
| `effector` | a live dial preview, a property driver card | Move a hundred copies with one moving point, and keyframe none of them. Each copy reacts to how FAR the point is, not to its place in a list, so you get a wave, a ripple, or a trail that lags behind. Flip between trail, highlight and ripple to see it. |
| `keyframeHandle` | a keyframe easing handle, bezier handle card | Why two easings that look alike FEEL different: one curve between two keyframes, drawn beside a strip of evenly spaced dots. The dots bunch where the thing is slow and spread where it is fast, which the curve alone hides. Flip between easyEase, snap, hang and overshoot to see it. |
| `rangeSelector` | a per-character range selector, a text range dial | The AE range selector's smoothness dial on this engine's kinetic presets: at 1 a glyph transitions, at 0 it swaps outright, which is the value a font morph needs. |
| `spectrum` | color ramp bands, rainbow gradient bands | Upright bands with a colour ramp falling down the frame, each band showing less of it than the one inside it. |
| `thermalBlur` | heat vision, heat camera, infrared | White type blurred, then remapped through a heat ramp: white cores, an orange body, a blue rim, and the thin strokes eaten away. |

## glow preset  `[preset]`

| name | words | action |
|---|---|---|
| `bloom` | light source glow, overexposed light, bright point glow | a hot near-white core falling off to the accent: light overflowing a bright source |
| `chromaCycle` | color cycling glow, hue shifting light, rainbow pulse glow | a saturated neon bloom whose hue sweeps the spectrum over `cycle` seconds |
| `chromatic` | rgb split glow, colour fringe halo, rainbow edge glow | an RGB-split halo: three channel copies offset and screen-blended, white core, colour fringe |
| `diffusion` | soft veil, lifted blacks, screen wash light | a broad low-alpha white veil, screen-blended: lifts blacks without recolouring below |
| `halation` | film halo, warm highlight ring, nostalgic glow | a tight warm core plus a wide faint ring, the film halation. Low intensity on purpose |
| `rimLight` | edge light, crescent light, off-centre glow | an off-centre crescent, lit edge up-right. Place the subject there |
| `spotlight` | light cone, directional beam, stage light | a soft-edged cone from an apex, aimed by `angle`, faded with distance so it never clips |

## gradient recipe  `[bg[].opts.recipe]`

| name | words | action |
|---|---|---|
| `arctic-blue` | ice blue radial, cold spotlight | pale ice blue radial, a cold spotlit pool centred at 50% across, 40% down |
| `berry-crush` | red gradient, punch gradient | crimson into tomato red, a hot fruit-punch ramp on a 70deg diagonal |
| `citrus-pop` | yellow orange gradient, fruit stand gradient | orange into bright yellow, a loud fruit-stand ramp on a 60deg diagonal |
| `cool-mint` | mint gradient, green teal gradient | green into teal, a fresh spa ramp on a 135deg diagonal |
| `coral-reef` | beachy gradient, salmon pink gradient | salmon into pale pink, a beachy diagonal ramp on a 25deg diagonal |
| `cosmic-drift` | moving galaxy gradient, space mesh gradient | indigo, plum and hot pink drifting, a MOVING galaxy mesh |
| `deep-ocean` | dark blue gradient, nautical gradient | near-black into slate blue, a moody nautical field on a 160deg diagonal |
| `ember` | moving fire gradient, crimson amber mesh | crimson, tomato and amber drifting slowly, a MOVING three-color fire mesh |
| `emerald-pool` | jungle green radial, teal jade radial | teal into jade radial, a jungle-pool glow centred at 40% across, 60% down |
| `forest-mist` | moving woodland gradient, green mesh gradient | teal, jade and pale sky drifting, a MOVING woodland mesh |
| `glacier` | icy radial, cold blue radial | sky blue into pale cyan radial, an icy pool centred at 60% across, 30% down |
| `golden-hour` | amber gradient, late afternoon gradient | amber into warm yellow, a late-afternoon ramp on a 30deg diagonal |
| `lavender-fog` | purple wash, periwinkle gradient | pale periwinkle into indigo, a dreamy purple wash on a 200deg diagonal |
| `midnight-violet` | night sky gradient, indigo plum gradient | three-stop indigo to plum, a night-sky ramp on a 180deg diagonal |
| `neon-dusk` | synthwave gradient, pink violet gradient | hot pink into electric violet, a synthwave ramp on a 50deg diagonal |
| `peach-cream` | dessert gradient, blush gradient | pale peach into blush, a soft dessert ramp on a 45deg diagonal |
| `plum-wine` | wine gradient, violet rose gradient | violet into dusty rose, a wine-toned diagonal ramp on an 80deg diagonal |
| `rose-glow` | pink radial, skin tone glow | blush pink radial, a warm skin-tone pool centred dead middle at 50%, 50% |
| `slate-storm` | grey gradient, overcast gradient | charcoal into graphite, a neutral overcast ramp on a 120deg diagonal |
| `solar-flare` | sunburst gradient, fire conic ring | red-orange-magenta conic ring, a sunburst wheel spinning from a 0deg start angle |
| `spectrum-ring` | rainbow ring, hue wheel | cyan-violet-pink conic wheel, a full-hue rainbow ring spinning from a 0deg start angle |
| `steel-blue` | industrial gradient, charcoal blue gradient | slate into charcoal-blue, a cool industrial ramp on a 100deg diagonal |
| `sunset-strip` | moving sunset gradient, red amber mesh | red, magenta and amber drifting slowly, a MOVING three-color sunset mesh |
| `warm-dusk` | sunset gradient, orange gradient | orange into coral, a sunset ramp on a 40deg diagonal |

## group clock loop  `[clock.loop]`

| name | words | action |
|---|---|---|
| `forever` | loop forever, repeat endlessly, never stop looping | repeat the cycle without limit, restarting at 0 the instant the previous pass reaches the end of its own local duration, never reversing |
| `pingpong` | bounce back and forth, reverse every other loop, boomerang loop | repeat the cycle without limit, but reverse direction every other pass, like a ball bouncing between two walls, so it never jumps back to 0 |

## gsap effect  `[fx]`

| name | words | action |
|---|---|---|
| `backIn` | rise and overshoot, fade up with a spring, pop past the mark | rises 90px and overshoots past its mark before settling, as fadeUp with a spring on the end |
| `blurIn` | resolve into focus, defocus entrance, unblur in place | resolves out of heavy defocus in place, calm, premium, no travel at all |
| `bounceIn` | drop and bounce, cartoon bounce entrance, ball drop in | drops in from above and bounces on landing, cartoon weight, for a punchline |
| `breathe` | slow breathing loop, ambient swell loop, calm breathe loop | LOOP, never settles: a slow 2.2s swell with a slight dim, ambient, calmer than pulse |
| `charBlurCascade` | per-letter blur cascade, kinetic type blur reveal, glyph by glyph blur | each glyph rises out of blur in turn, the calm, premium per-letter reveal |
| `charFold` | per-letter unfold, kinetic type fold, glyph by glyph unfold | each glyph unfolds up from its own baseline with a small overshoot, kinetic type, warm |
| `charOvershoot` | per-letter pop, kinetic type overshoot, glyph by glyph spring | each glyph pops up from small and springs past its mark, the loudest per-letter reveal |
| `charTilt` | per-letter swing, kinetic type tilt, glyph by glyph tilt | each glyph swings in about its vertical axis and straightens, kinetic type, needs 3D perspective |
| `clipUp` | bottom to top wipe, mask uncover up, reveal from below | a hard bottom-to-top wipe that uncovers the layer in place, opacity untouched, type reveals behind a mask |
| `drift` | sideways drift loop, background parallax loop, slow wander loop | LOOP, never settles: a very slow 3s sideways wander. Background parallax, easy to miss on purpose |
| `driftIn` | soft diagonal float in, quiet blur entrance, atmosphere entrance | floats a short diagonal out of soft blur, slowly. The quietest entrance here, for atmosphere |
| `dropIn` | heavy drop in, fall and bounce, drop from above | as bounceIn but falling from much further up, so it lands harder |
| `elasticIn` | rubber band in, wobble in, springy pop in | springs from tiny and wobbles several times before it stills, over a slow 1.1s, only ever playful, never for a serious brand |
| `expandIn` | kerning, letter-spacing, letterspacing, tracking out | letters start crushed together and spread out of blur to their real tracking, an opening line settling into place |
| `flipInX` | flip up on x axis, hinge up entrance, card flip vertical | hinges up into the frame about its horizontal axis, cards and panels, needs 3D perspective |
| `flipInY` | flip in on y axis, page turn entrance, card flip horizontal | hinges in about its vertical axis, like a page turning. Cards and panels, needs 3D perspective |
| `float` | floating loop, gentle bob loop, idle rise and fall | LOOP, never settles: rises and sinks 18px forever, idle life for a hero object |
| `foldIn` | unfold from the top, dropdown entrance, panel unfold | unfolds downward from its top edge and springs level, dropdowns, panels, receipts; needs 3D perspective |
| `glitchIn` | glitch entrance, stepped snap in, digital glitch reveal | snaps in through five hard steps, sheared and offset. No smoothing at all, alarm and glitch beats only |
| `heartbeat` | throbbing loop, urgent pulse loop, live recording throb | LOOP, never settles: a fast 12% throb twice a second. Urgency, live counts, recording dots |
| `maskReveal` | left to right wipe, editorial headline reveal, mask uncover sideways | a hard left-to-right wipe, fast then a long settle. The premium editorial reveal for a headline |
| `pulse` | pulsing loop, breathing scale loop, draw attention pulse | LOOP, never settles: breathes 6% larger and back every second, draws the eye to a CTA |
| `revealUp` | clip and lift reveal, uncover and rise, wipe up with a lift | clipUp plus a short lift and fade, so the layer rises as it is uncovered, the fuller version of clipUp |
| `rollIn` | roll in from the left, wheel arrival, rotate while sliding in | rolls in from the left, its rotation unwinding as it travels, reads as a wheel arriving |
| `skewIn` | sheared slide in, skew and straighten, velocity slide | slides in sheared and straightens as it lands, velocity you can read in the letterforms |
| `spinIn` | spin and grow, rotate in, logo spin reveal | rotates a half turn anticlockwise while growing, overshooting on the settle, logos, badges, seals |
| `swing` | pendulum loop, hanging swing loop, rocking pendulum | LOOP, never settles: a slow pendulum rock. Hanging objects; needs 3D perspective |
| `tiltIn` | door swing entrance, hinge open from the left, swing into frame | swings open about its left edge, like a door facing the camera, needs 3D perspective |
| `wobble` | rocking loop, restless wobble loop, warning wobble | LOOP, never settles: rocks 3 degrees each way, restless, for a warning or a toy |
| `zoomBlur` | pull focus, camera rack focus, defocus zoom in | rushes back from too close while the defocus resolves. A camera pulling focus, premium hero beat |

## icon  `[svgIcon()]`

| name | words | action |
|---|---|---|
| `agent` | robot icon, assistant icon, AI worker icon | an autonomous worker or assistant. The mark for something acting on its own. |
| `arrowLeft` | left arrow, back arrow, previous arrow | back, previous, return. The pair to arrowRight, and it should point the way the film travels. |
| `arrowRight` | right arrow, forward arrow, next arrow | forward, next, onward. Progression through a sequence, or a result following a cause. |
| `bolt` | lightning bolt, speed icon, power icon | speed or power. Fast execution, an instant action, energy: the mark for anything that happens in under a second. |
| `braces` | curly braces icon, code icon, JSON icon | code, JSON, a config file. The one to use when the subject is literally the syntax. |
| `check` | checkmark, tick mark, success mark | done, passed, verified. The mark for a gate going green or a step completing. |
| `clock` | clock icon, time icon, duration icon | time, duration, waiting, a deadline. Any beat whose subject is how long something takes. |
| `cube` | 3D cube icon, package icon, module icon | a package, a build artifact, a module. A discrete unit of software. |
| `dollar` | dollar sign, money icon, price icon | money, price, revenue, cost. Any figure the viewer reads as currency. |
| `file` | document icon, a record icon | a document or a single record. Use where the subject is one item of content rather than a system. |
| `globe` | world icon, network icon, earth icon | the world, the network, a public surface. Reach, deployment, anything international. |
| `layers` | stack icon, layers icon, depth icon | a stack, composition, depth. The mark for things built out of other things. |
| `link` | chain link icon, connection icon, url icon | a connection between two things, or a URL. Integration, reference, chaining. |
| `plug` | plug icon, integration icon, connector icon | an integration or a connector. Where one system attaches to another. |
| `shield` | security icon, protection badge, trust badge | security, protection, trust. The one to reach for on a safety or privacy claim. |
| `spark` | sparkle icon, AI generated icon, new icon | something new, generated, or AI-made. Novelty and creation rather than speed. |

## idle  `[idle]`

| name | words | action |
|---|---|---|
| `breathe` | breathing scale, gentle pulse, alive on hold | 1.5% scale, ~4.6s. The held frame stays alive without moving |
| `drift` | ambient drift, never fully parked, slow float | slow translate on two periods, ~9px, the frame is never quite parked |
| `none` | truly still, no ambient motion, locked off hold | no idle. The layer is truly still on its hold (the default) |

## interpolation mode  `[ease]`

| name | words | action |
|---|---|---|
| `through` | stall, stalls, stops dead, dead stop, stops in the middle, pause, pauses, hitch, hesitates, stutter, jerk, kink, waypoint, sparse keys, three keys, graph editor, continuous bezier, auto bezier, carries speed, one gesture, rounds a corner | velocity carries THROUGH the key: a cubic Hermite with neighbour tangents, so a travel across several keys is one gesture rather than a stop at each |

## keyframe handle  `[easeOut]`

| name | words | action |
|---|---|---|
| `easyEase` | ease in and out, the default handle, stops smoothly at the key | AE's Easy Ease: reaches a third of the way in and arrives at a DEAD STOP. The default handle, and the one to use when you just want a key to stop being mechanical |
| `fling` | fast exit handle, throws it out, the steep half of a snappy move | a short handle at four times the average speed: the value leaves (or arrives) FAST and the segment spends its length recovering. The steep half of a snappy move |
| `hang` | hangs at the key, flat-ended handle, crushed toward the key | influence 75 at a dead stop: the value HANGS at this key and the movement is crushed away from it. On BOTH sides of a segment this is the flat-ended, near-vertical speed graph a snappy swap is cut on. 75 is the number practitioners state |
| `linear` | constant speed handle, straight handle, no easing on this side | the straight line, written down: the handle sits on the diagonal so this side of the segment has constant speed. Use it to make one side explicit while the other is shaped |
| `overshoot` | sails past and settles, ten percent overshoot handle, back-ease handle | arrives from BEYOND its key and settles back: the value sails about 10 per cent past and returns. The handle version of a back ease, and it needs the far side to stop it |

## kinetic preset  `[preset]`

| name | words | action |
|---|---|---|
| `assemble` | scatter assemble, fly in and land, particles assemble, randomized entrance | each glyph flies in from its OWN scattered offset and rotation and settles into the word, arriving in a shuffled order · the AE "text animator + randomize-order range selector" reveal. Pair it with `split: "char"` |
| `blur` | soft focus resolve, defocus in | resolves out of a 16px blur to 0 as it fades in: calm, premium |
| `blurUp` | blur and rise, sharpen in, defocus lift, the default kinetic headline | letters lift and sharpen out of blur together: the default kinetic headline |
| `bounce` | springy bounce in, bouncy entrance, playful bounce | a spring bounces the unit up 60px with a visible overshoot (bounce 0.5, settle 0.5s): playful brands only |
| `chroma` | chromatic aberration, rgb split, colour fringing | R/G/B ghosts split apart and converge to a crisp glyph |
| `colorWave` | per word colour, per-word color, each word its own colour, colour per word, words leave one by one | the accent sweeps word by word along a line, each unit lighting then settling to the resting colour · `colors` gives EACH unit its OWN arrival colour instead of one shared accent, per-word colour before the line settles to one ink |
| `decode` | scramble text, matrix decode, hacker text effect, techy scramble reveal | characters scramble through the chosen charset at 48 refreshes a second by default and resolve left to right as u climbs to 1, techy reveal |
| `down` | drop in, fall down, slide down from above | words/chars drop 40px into place from above while fading in: the mirror of `up` |
| `draw` | line drawing, path draw, svg stroke animation | an SVG stroke draws itself on: the dash offset runs from 1 to 0 on a path normalised to length 1, easeOutCubic by default, `back:true` draws from the far end |
| `elastic` | elastic pop, springy scale, wobbly bounce | scales up from 0.3 with a springy overshoot (bounce 0.62, settle 0.5s), visible wobble: playful brands only |
| `fade` | word by word exit, words leave one by one, text disappears word by word, split exit | plain opacity fade, no motion at all: the neutral entrance, and the neutral exit reversed |
| `fall` | gravity drop, falls from above, drop with a bounce landing | falls from above under gravity and lands with a small squash |
| `flap` | split flap, airport board, flip board, departure board | the glyph steps FORWARD through the board's alphabet one flap at a time and lands on its letter, hinging as it turns. An airport board, ordered where `decode` is random |
| `flip` | 3d flip, card flip, flip in, somersault | 3D flip-up per unit, letters somersault into place: `axis` picks the hinge |
| `focus` | focus pull, rack focus, blur to sharp, dreamy resolve | focus pull, heavy blur and over-scale resolving to crisp, dreamy, premium |
| `gradient` | rainbow text, colour sweep text, gradient text reveal | a two-colour band (default #8a8f98 to #ffffff) at 250% width sweeps through the letterforms via background-clip text |
| `highlight` | marker highlight, highlighter sweep, underline sweep | a marker band, default rgba(255,220,90,0.35), grows left to right to 100% width behind the unit at 78% of its height |
| `riseClip` | rise from behind a mask, clip reveal, masked rise | the word climbs out from behind a hard edge at its own baseline, hidden until it clears the line. The clean editorial reveal |
| `scale` | zoom in text, grow text from small | punches in from 0.4 scale to 1, an easeOutBack overshoot past the mark and back |
| `shadow` | poster shadow, long shadow collapse, drop shadow lift | a long poster shadow collapses as the word settles, poster statements |
| `shimmerWave` | shimmer, light sweep, 3d shimmer, traveling shine | looping light wave (per-unit): a 3D crest travelling across the word; never settles |
| `skew` | italic shear, slanted slide in, skewed entrance | slides in 70px with a 14deg italic shear that straightens as it lands: editorial, sporty |
| `slide` | slide in from the side, enter from left or right, push in sideways | slides in 80px from one side (`dir`), default left: pair it with the opposite exit |
| `stretch` | horizontal smear, stretch and snap, squash and stretch | stretches from 1.6x horizontal scale down to 1x with a 6px blur that clears as it snaps true: impact words |
| `strike` | strikethrough, crossed out, struck through | a rule draws THROUGH the word and it dims behind the line, still legible. The "not X, Y" beat, where the rejection is the content |
| `swing` | pendulum swing, hinge down, swing upright | each unit hinges from its top edge and swings upright, playful, short words |
| `tilt` | tilt in, swing upright, angled rise, small rotate in | each unit rises and swings upright from a small angle, sporty and editorial. A flat rotation, not a 3D hinge: `flip` is the one that turns |
| `type` | typing, letters one at a time, keystroke, terminal caret | a hard on/off with no transform: opacity flips from 0 to 1 the instant u passes `at` (default 0). Terminals, timers, code |
| `underline` | underline draw, draw a line under, rule grows underneath | a rule grows left to right along the baseline as the word lands, the marker under a heading |
| `unfold` | unfold open, panel turn, card unfold, door opening | opens from edge-on about its left hinge, a panel turning to face you, premium |
| `up` | rise in, float up, the default reveal, lift into place | words/chars rise 40px into place while fading in, no blur: the plain lift, and the engine's own default reveal |
| `wave` | sine wave, bobbing text, idle wave, ambient wave | sinusoidal wave across units: a LOOP that never settles; ambient only |
| `weight` | weight ramp, variable font weight, font gets bolder, thicken in | the glyphs THICKEN into place along the font's own `wght` axis, a crest of weight travelling the line · the one register a static face cannot fake, and it degrades to the nearest static cut rather than to a dead still |

## layer type  `[layers[].type]`

| name | words | action |
|---|---|---|
| `adjust` | a color grade layer, a filter over everything below, a full-frame adjustment | one grade over everything BENEATH it: blur, desaturate, darken, brighten or contrast the whole frame from a single layer, keyable through `vars` on `--adjust` |
| `beam` | a light bar, a travelling light, a light border | a light that travels the rounded-rect border, or a sheen that sweeps across the box; the travel is closed-form in t, not a CSS keyframe |
| `board` | a background card, a dimmed backdrop panel, a behind card | a populated workspace from pure data: up to 4 columns of up to 5 mini issue-cards, entering as one clip |
| `clip` | a baked animation, a frame sequence, a pre-rendered clip | a video played as a preloaded PNG frame sequence: the frame swaps the <img> src, so no decoder state can drift between renders |
| `component` | a captured UI block, a reflected site section, a recreated page piece | a REAL captured UI block (`make capture`), or one named part of a captured scene, scaled to fit `w` |
| `composition` | a hand-timed sub-timeline, nested keyframed motion, a custom mini animation | names a first-party hand-authored GSAP timeline in core/compositions/ and passes it DATA; for choreography `parts` and recipes cannot express |
| `count` | counting number, ticking stat, number that goes up | a number that counts from -> to across its own window, formatted (compacts at 1e6, prefix/suffix/decimals). The text build plus a per-frame value. `roll: true` makes it an ODOMETER: one masked wheel per digit, each sliding to its next value, geared so a wheel only turns as the wheel below it crosses 9 |
| `cursor` | a mouse pointer, a fake cursor, a click demo pointer | a pointer that follows [{t,x,y}] keyframes (`path`) or eases magnetically onto another layer's live box (`snapTo`), fires a ripple at `clicks`, can change shape mid-move (`style`/`styleAt`: arrow/hand/ibeam/block), carry a name tag (`label`), and drag another layer along with it (`carry`) |
| `doc` | a file card, a markdown card, a source-file preview | a file card from pure data: an optional filename + diff chip, then heading / body / code / bullet blocks, theme-styled and auto-height |
| `globe` | a spinning globe, a dotted planet, a world map globe | a dotted planet with tapered route arcs, drawn by vendored cobe: atmospheric glow and a diffuse terminator. reach for `three:"globe"` instead only when you need a real sun vector |
| `glow` | a soft light, a halo, a glowing highlight | soft light with no WebGL: a radial centre glow, a directional beam, or a named phenomenon (bloom · halation · diffusion · rimLight · spotlight) |
| `group` | a container, nested layers, a folder of layers | layout by containment: a flex/grid box whose children (any depth) flow with a gap, so a card + label + chip can never desync |
| `html` | a hand-written fragment, real markup, a web snippet | raw hand-authored HTML/CSS as ONE layer, still positioned and animated by the engine; <script> is stripped and CSS transition/animation refused, use `parts` to give its pieces the clock |
| `image` | a photo, a picture, a screenshot | an <img> with cover-fit, radius, a ken-burns slow zoom and an edge dissolve; `canvasFx` bakes a per-pixel pass into it at boot |
| `lottie` | a Lottie animation, an After Effects export, a JSON animation file | an After Effects (Bodymovin) export driven by ABSOLUTE seek, goToAndStop at a frame index, never autoplay |
| `paint` | a canvas 2D field, a drawn generative background, a resamplable canvas look | a generative Canvas 2D field drawn per frame from local time. The canvas family written in JS rather than GLSL, and the one you can resample |
| `particles` | confetti, sparks, a dust burst | a deterministic particle emitter: `preset` picks confetti (a gravity-fed burst), sparks (a fast radial flash) or dust (a slow ambient drift). Closed-form per particle, seeded by `seed`, never a running sim |
| `raymarch` | a 3D distance field, a lit 3D surface, a raymarched shape | a lit implicit surface from a distance field: a camera, a normal and a silhouette. the most expensive primitive in the engine. One hero shot, sized to what it needs |
| `rect` | a box, a solid panel, a rectangle | a plain box, panel, card or pill. it carries no text: put that on a higher track |
| `shader` | a generative background, a WebGL field, an ambient full-frame look | a full-frame generative WebGL field (see the ambient shaders), pure in t and palette-tintable; it carries no sampler, so it cannot read what is beneath it. `shaderKeys` cycles ONE panel through several looks on hard cuts, in one context, instead of stacking a layer per look |
| `svg` | vector art, an inline icon shape, scalable line art | a vector mark that DRAWS itself on (stroke dashoffset) and then RESOLVES INTO ITS FILL, the stroke leaving as the solid logo arrives, or MELTS from one path into another (true point-lerp morph, optional spin) |
| `text` | words on screen, headline, caption text | theme-styled words in an optional chip box, auto-fit to a width; the typewriter reveal and caret live here too; `typingColors` flashes each word its own accent colour the instant it types, then settles to ink |
| `three` | a real 3D scene, a three.js object, a posed 3D mesh | a real three.js scene graph (meshes, materials, lights, a camera) posed absolutely from t, for what a distance field cannot express: a font outline, a device body, a captured UI plane, a point cloud |
| `video` | real footage, a video clip, captured recording | real footage, SEEKED to a computed source time every frame and never played, so the picture is as deterministic as a still |

## lightfield pattern  `[pattern.kind]`

| name | words | action |
|---|---|---|
| `rings` | ripples on water, concentric light rings | concentric bands round a point, like light on water, the eye travels outwards |
| `shards` | light rays from below, sunburst fan | a fan of rays from a pivot below the frame, the eye travels up and out |
| `slats` | venetian blind, vertical bars of light | a backlit blind: vertical bars of unequal width, each with a lit leading edge falling to a dark trailing edge, the eye travels across |

## look  `[filter]`

| name | words | action |
|---|---|---|
| `angelic` | heavenly light, blown out white, overexposed halo | blown out, desaturated, the widest bloom in the library and an INVERTED vignette that whitens the corners. Heaven light, heavy-handed by design |
| `chrome` | polished metal, chrome finish, metallic surface | specular highlights ADDED on top, then remapped to a navy/steel/white tritone. Polished metal; two SVG filter passes, the most expensive look here |
| `crt` | old tv screen, phosphor tube, tube television | half the split of vhs plus a white bloom and the densest, darkest scanlines, corners pulled down. A lit phosphor tube: vhs glows and closes in |
| `cyberpunk` | neon noir, magenta cyan grade, sci-fi city grade | saturated hard, magenta/cyan split, magenta bloom over faint scanlines. A fixed neon-noir palette laid over your colours |
| `dreamSequence` | drifting dream, surreal drift, dream haze | the mildest warp plus defocus, cream bloom and a warm wash. DreamyHaze with the picture set drifting |
| `dreamyHaze` | soft focus, dreamy blur, romantic haze | half-pixel defocus under a wide white bloom and a cream wash, soft-focus romance, no grain and no edges |
| `droneCinematic` | subtle cinematic grade, aerial footage look, gentle film grade | every dial turned down (0.5 default strength, faint grain, shallow vignette). A grade rather than a look, safe under type and product UI |
| `edgeGlow` | neon wire drawing, glowing outline, edge detect glow | flat areas cancel to black and only boundaries survive, amplified 6x and bloomed. A neon wire drawing; six passes, and a smooth subject can come out empty |
| `emboss` | embossed relief, stone rubbing, carved surface | the kernel turns the picture into a grey lit rubbing and half the colour is thrown away, carved surface, per-pixel and not cheap |
| `fadedPolaroid` | old polaroid, sun-faded print, washed out photo | eight passes: sepia, desaturated, contrast LOWERED, brown wash, corner leak, grain, vignette. A print left in the sun, the flattest look here |
| `fatten` | bolder type, chunky poster look, dilated highlights | dilation swells the bright pixels into their neighbours, type gains weight and a photo goes chunky and poster-like |
| `filmNoir` | black and white drama, noir grade, monochrome film | full grayscale, contrast crushed up, a small white bloom, grain and the deepest vignette, monochrome drama |
| `glassWarp` | through glass, warped glass, distorted glass look | medium-frequency turbulence pushes the image around behind a faint white bloom, seen through thick glass, still readable |
| `glitchGlow` | signal glitch, digital fault, broken signal | wide 3px red/blue split, source-coloured bloom, sparse scanlines. Hologram with the tint removed and the split doubled: a signal fault, not a projection |
| `halationFilm` | film halation, warm film glow, amber bloom | warm amber bloom off the highlights plus grain, contrast barely touched. The restrained glow: no blur, no vignette, colours survive |
| `heatWarp` | heat haze, air shimmer, tarmac heat | coarser, larger displacement under an orange wash and a lift, air over tarmac |
| `hologram` | holographic, projected image, teal hologram | pink/cyan split, cyan flood-bloom and tight scanlines. A projected image, colour forced to teal |
| `impact` | frame hit, punch frame, impact flash | overexposed with the widest colour split here (4px) and a white bloom. The frame taking a punch, for one frozen hit only |
| `letterpress` | ink pressed paper, letterpress print, pressed into stock | diffuse light multiplies the image so it sinks into a warm paper wash, lifted 1.5x to stop it going black, ink pressed into stock |
| `lomo` | plastic camera look, lomography, crushed saturated photo | the strongest saturation and the heaviest vignette in the library, plus a corner leak and grain. Plastic-camera punch: no glow, no split, just crush and corners |
| `melt` | melting image, liquid distortion, dripping frame | the lowest frequency and by far the largest displacement here, big slow lumps that destroy legibility, so never under type |
| `neon` | sign light, neon glow, glowing colours | the bright parts bloom in their OWN colours, nothing is tinted. Real sign-light, the one glow look that keeps the palette |
| `nightVision` | night vision goggles, green night scope, image intensifier | grayscale then sepia then hue-rotated and saturated 4x to ONE green, green bloom, lines, grain, vignette. Image intensifier; the original colour is gone, not tinted |
| `nostalgia` | memory look, warm faded photo, old snapshot | fadedPolaroid without the wash, the flattening or the vignette, plus a warm bloom, same memory register, lighter and open at the edges |
| `rippleGlass` | water ripple, rippling glass, underwater ripple | the highest frequency and a small offset, so the distortion reads as tight ripples not lumps, over a cool bloom, water on the lens |
| `super8` | old film stock, super 8mm, home movie film | sepia, lifted, the heaviest grain here and a deep vignette. Warmth and dirt only, no split and no lines: film stock, not video |
| `thermal` | heat camera, thermal imaging, infrared grade | luminance remapped to a six-stop black-purple-pink-orange-white ramp, softened. A heat camera; three passes, the cheapest sci-fi look and the most total recolour |
| `timeFreeze` | frozen moment, time stop, cold freeze frame | drained of colour, washed cold blue with a matching bloom and a hairline split, the world stopped |
| `vhs` | videotape, retro tape, analog video, camcorder footage, old video | colour split 3px, slight defocus, scanlines and heavy grain. Tape: no vignette and no glow, so the frame stays flat and dirty |
| `vintageAnamorphic` | old cinema lens, anamorphic streaks, blue lens flare | the only look with horizontal blue streaks off the highlights, plus split, grain and a deep vignette, old spherical glass |
| `watercolor` | painted look, watercolour paper, pigment on stock | displaced, desaturated, contrast lowered under a multiplied paper wash and grain, pigment on stock |

## modifier  `[modifiers[]]`

| name | words | action |
|---|---|---|
| `alongPath` | text on a curve, type on a path, curved text | set the line of type ON A CURVE and, if you ask, send it travelling along one. The general case `circle` is one point of, and the only way to bend a headline |
| `ghost` | motion trail, motion blur streak, afterimage trail | the only effect that reads TIME AS A MATERIAL: it evaluates the layer's own motion track a few frames BACK and draws from the difference, `trail` leaves faded copies at the poses it just left, `blur` samples the same poses inside one frame so the layer smears along its real direction of travel, not around a fixed centre |
| `kick` | hit on the cut, jolt on the beat, impact on edit | hit the layer on the film's own joints. A cut, a seam or a sting shoves it, so the frame feels the edit |
| `lag` | follow through, drag behind, delayed follow motion | FOLLOW-THROUGH: this layer trails another layer's motion by a frame or three and overruns its stop before settling. Stagger delays a sibling's entrance; this makes one layer drag behind another's continuous motion, which is half of what separates an animated object from a moved image |
| `matte` | luma matte, brightness mask, wipe reveal | a LUMA MATTE: another layer's brightness is this layer's alpha, white shows and black hides. The general case of the whole wipe family, and the matte MOVES, because it is placed from the source layer's live box |
| `mixBlend` | blend mode modifier, composite mode, knock out of photo | how this layer's pixels combine with what is already painted behind it, knock a headline out of a photo |
| `occlude` | hide behind another layer, occlusion, depth sort hiding | hide this layer where another one covers it, put something BEHIND something else without reordering the stack |
| `plane` | parallax depth, stand at a distance, depth modifier | stand the layer at a DEPTH so the camera moves it by a different amount than its neighbours, this is parallax |
| `progress` | film progress variable, timeline percent, progress ring driver | hand the layer the FILM's progress, 0 at the first frame and 1 at the last, as a CSS custom property its markup can draw with |
| `shadow` | cast shadow, drop shadow with light, directional shadow | a drop shadow that knows where the light is, so every layer does not point the same way |
| `squash` | squash and stretch, cartoon bounce, stretch on velocity | SQUASH AND STRETCH read off the layer's own velocity: the travel axis stretches and the perpendicular one squeezes by exactly the reciprocal, so the volume holds. Scale both and it is a zoom, not a squash |
| `tilt` | 3D tilt, turn out of plane, leaning card | turn the layer out of the picture plane and hold it there. A card leaning away, a phone at an angle, panels receding |
| `upright` | auto orient, stay upright, counter rotate | AUTO-ORIENT: hold this layer UPRIGHT while the layer carrying it rotates, so an arrangement can turn without its contents turning with it. Parenting gives you the rotation for free and it is almost always wrong: photographs carried round a circle go upside down at the bottom exactly when the motion peaks |
| `wordSlot` | word swap, rotating word, text slot machine | one word of the sentence swaps for the next while NOTHING after it reflows. The slot is a grid cell auto-sized to the widest candidate, and `chip: true` makes it the brand-coloured box the launch rule asks for |

## motion voice  `[audio.cues[].name]`

| name | words | action |
|---|---|---|
| `bloom` | an opening sound, a swelling reveal sound | something OPENS: placed automatically on an `iris`, `softiris`, `rise` or `riseBlur` cut, and on a declared `spectacle`. Slower in than the others, because an opening is not an arrival |
| `chime` | a small bright accent, a ding, a resolve sound | a small bright accent where a moment resolves. Noticed rather than announced, so it survives repetition better than a sting does |
| `droplet` | a drop sound, a light plink, a short pitched drop | something falls into place: a short pitched drop, placed automatically on a `drop` or `zoom` cut. The lightest of the arrival sounds |
| `impact` | a hard landing sound, a hit sound, a thud | something heavy ARRIVES and lands hard: a hit with an edge, a mass and a room, in that order. A frame-sized card reaching its mark, a panel slamming home |
| `pluck` | a small punctuation sound, a counter tick sound | punctuation, for a small element or a counter digit. Quiet on purpose: this is the one that becomes a machine gun, and the density rules exist because of it |
| `whoosh` | a camera whoosh, an air rush sound, a swoosh | air moving past. One camera gesture, one whoosh, never one per keyframe: the sound rises as the move starts and falls away as it passes |

## move shape  `[move:<shape>:<band>]`

| name | words | action |
|---|---|---|
| `blast` | punch and hold, over-scale entrance, pop through the frame | 4-key punctuation over 1.5s: arrives at scale 1.5, settles to 1, drifts to 1.04, then grows OUT to scale 1.9 through the frame. A hard pop, never a fade |
| `drift` | ambient hold, never fully still, living idle track | a slow 3s ambient hold that swings +/-12px on one axis and never returns to where it began, for a layer that should keep living through a beat |
| `enter` | keyed rise in, overshoot entrance, custom entrance track | a keyed 0.9s entrance track that rises in from 40px, overshoots slightly past 0, then settles |
| `exit` | accelerating exit, keyed departure, leaves off-frame | a keyed 0.55s departure that accelerates 260px off-frame, opacity trailing the move: the layer is gone because it LEFT, not because it dimmed |
| `pan` | scroll rhythm, hand-scrolled pan, irregular pan | an irregular 6-key horizontal (or vertical) pan over 1.25s by default, linear interior keys: surges then gives up, reads as a hand scrolling, not an animation of a page |

## output target  `[aspect]`

| name | words | action |
|---|---|---|
| `16:9` | landscape, widescreen, horizontal video | wide landscape, the desktop shape: a website hero, a YouTube upload, a deck slide, anything watched on a laptop |
| `1:1` | square, square post | a square, equal on both sides: the feed post that crops the same everywhere it is shown |
| `4:3` | old tv shape, classic television ratio | boxy landscape, the old television and slide-projector shape: archive footage, a retro monitor |
| `4:5` | portrait feed post, instagram portrait | the tall feed post: Instagram portrait, more height than a square without taking the whole phone |
| `9:16` | vertical video, portrait, phone screen, vertical for reels | tall and vertical, the whole phone screen edge to edge: TikTok, Reels, Shorts, a story |

## paint fx  `[paint]`

| name | words | action |
|---|---|---|
| `aurora` | northern lights, aurora borealis, color blobs | slow curtains of coloured light, 5 blobs blended additively at 0.5 opacity. Calm, ambient, and the least busy field here. |
| `matrix` | matrix rain, falling code, digital rain | columns of falling glyphs, 20px cells with a 14-cell trail. Code, data streaming, a machine at work. Unmistakably a reference, so use it deliberately. |
| `meteor` | shooting stars, streaks, comet trails | streaks crossing the frame on a diagonal, 14 meteors on a 28deg angle. Motion and event on a background that would otherwise be still. |
| `starfield` | stars, flying through space, warp speed | stars drifting past as if the camera were moving through them, 220 stars by default. Depth and travel on an otherwise flat frame. |
| `waves` | wave field, rolling lines, contour lines | rolling bands, a moving surface, 26 lines with a 26px amplitude. Rhythm underneath content without competing for the eye. |

## part entrance  `[parts[].anim]`

| name | words | action |
|---|---|---|
| `drawOn` | draw the line, self-draw stroke, trace the path, line draws itself | an SVG stroke draws itself along its own path, measured at build with getTotalLength() |
| `fade` | fade in place, opacity only, no movement entrance | opacity alone from 0 to 1, no displacement · the quiet default when a part should arrive without moving |
| `fadeUp` | fade and rise, short rise with fade, quiet default entrance | rises 24px with a fade from opacity 0 to 1, the quiet default for any part |
| `growUp` | grow from the bottom, bar chart grow, scale up from base | scales its Y from 0 to 1 anchored at its own bottom edge, a bar growing up to its reading |
| `popIn` | pop from centre, scale in from nothing, appear with a pop | scales from 0 to 1 at its own centre with opacity 0 to 1, dots, chips, markers |
| `riseIn` | rise in with a fade, longer rise entrance, arrive with a lift | rises 48px with a fade from opacity 0 to 1, for parts that should feel like they arrive |
| `slide-left` | enter from the left, slide in from the left side | enters 40px from its left and, with `out`, keeps going 40px right · one direction of travel, never a retreat |
| `slide-right` | enter from the right, slide in from the right side | enters 40px from its right and, with `out`, keeps going 40px left · the mirror of slide-left |
| `widen` | widen from the left, grow sideways, fill from the left edge | scales its X from 0 to 1 anchored at its own left edge. A row, a rule, anything that should fill rather than appear |

## particles preset  `[preset]`

| name | words | action |
|---|---|---|
| `confetti` | celebration burst, party confetti, success burst | a gravity-fed burst launched near the bottom of the box, tumbling rects as it falls: celebratory |
| `dust` | floating particles, ambient dust motes, twinkling dust | a slow ambient drift that loops forever, low-alpha specks with a gentle twinkle: atmosphere behind a subject |
| `sparks` | a quick flash of sparks, impact spark burst | a fast radial flash from the box centre, additive-blended, gone in under a second: an impact or a reveal |

## path curve  `[move:<curve>:<band> | alongPath.curve]`

| name | words | action |
|---|---|---|
| `arc` | rise and fall, hill shape, arch, bump | a shallow rise-and-fall: starts and ends at 94% of the box height, bows up through a control point 50% above the top edge at the midpoint |
| `dip` | sag and rise, valley shape, trough, dip down | a shallow sag-and-rise: starts and ends at 12% of the box height, dips down through a control point at 150% of the height (below the box) at the midpoint |
| `ramp` | diagonal line, straight diagonal path | a straight diagonal line from 90% height at the left edge to 18% height at the right edge, top-left to bottom-right |
| `wave` | s-curve, s shape, sine wave, zigzag | one full S-curve cycle: leaves the 50% height midline through control points near the top at 18-32% width, crosses back through the midpoint, then dips through 82% width near the bottom before landing at 50% height |

## placement  `[pin]`

| name | words | action |
|---|---|---|
| `bottom` | bottom center, bottom edge middle | horizontally centred, pinned to the bottom edge of the safe area |
| `bottom-left` | bottom left corner, lower left corner | pinned into the bottom-left corner, 6% of the short side in from both edges of the frame |
| `bottom-right` | bottom right corner, lower right corner | pinned into the bottom-right corner, 6% of the short side in from both edges of the frame |
| `center` | dead center, middle of frame | dead centre of the canvas, vertically at the optical centre rather than the exact middle |
| `left` | left edge, middle left | pinned to the left safe edge (6% of the short side in from the frame), vertically centred |
| `lower-band` | bottom strip, closing rule position | a thin strip near the bottom safe edge, left-anchored: where a closing rule or a small persistent label sits |
| `right` | right edge, middle right | pinned to the right safe edge (6% of the short side in from the frame), vertically centred |
| `stage` | content column, stage left margin | the full-width content column between the left and right composition margins, at any aspect ratio: the repo's hand-typed x:160/w:1600 anchor, portable |
| `text-band` | lower third band, headline band | the horizontal strip roughly two-thirds down the frame where a headline or a sub-line usually sits |
| `thirds-b` | lower third line, bottom third | centred horizontally, sat on the lower third line at 67% down the frame |
| `thirds-bl` | lower left power point, rule of thirds bottom left | lands at 33% across and 67% down: the lower-left rule-of-thirds power point |
| `thirds-br` | lower right power point, rule of thirds bottom right | lands at 67% across and 67% down: the lower-right rule-of-thirds power point |
| `thirds-l` | left third line, left third | vertically centred, sat on the left third line at 33% across the frame |
| `thirds-r` | right third line, right third | vertically centred, sat on the right third line at 67% across the frame |
| `thirds-t` | upper third line, top third | centred horizontally, sat on the upper third line at 33% down the frame |
| `thirds-tl` | upper left power point, rule of thirds top left | lands at 33% across and 33% down: the upper-left rule-of-thirds power point |
| `thirds-tr` | upper right power point, rule of thirds top right | lands at 67% across and 33% down: the upper-right rule-of-thirds power point |
| `top` | top center, top edge middle | horizontally centred, pinned to the top edge of the safe area |
| `top-left` | top left corner, upper left corner | pinned into the top-left corner, 6% of the short side in from both edges of the frame |
| `top-right` | upper right corner, northeast corner | pinned into the top-right corner, 6% of the short side in from both edges of the frame |

## ransom face  `[ransom.faces (a list of {family, weight} records on a text layer)]`

| name | words | action |
|---|---|---|
| `Anybody` | extra wide font, poster font, sci-fi font | an extremely wide display sans at its heaviest: poster shout, sci-fi proportions |
| `Archivo` | bold grotesque, newspaper headline font, heavy sans | a workhorse grotesque cut heavy and wide: the neutral, newspaper-headline shout of the set |
| `Caveat` | handwriting font, handwritten font, cursive font, sticky note | handwriting, a felt-tip scrawl: the letter somebody wrote by hand and stuck down |
| `Fraunces` | display serif, wobbly serif, old-fashioned serif | a high-contrast display serif with a soft wobble, old seed-catalogue flavour, slanted here |
| `Hanken Grotesk` | rounded sans, friendly font, soft sans | a friendly rounded sans at maximum weight: soft, solid, no edge to it |
| `Instrument Serif` | elegant serif, magazine headline font, slanted serif, italic serif | a slanted editorial serif with thin stems: magazine headline, elegant against the noise |
| `JetBrains Mono` | monospace font, code font, terminal font, typewriter font | a monospace typewriter for code and terminals, every letter the same width |
| `Space Grotesk` | geometric sans, techy font, computer font | a technical geometric sans, cool and even, the computer-lab voice in the note |

## raymarch  `[raymarch]`

| name | words | action |
|---|---|---|
| `caustics` | water surface with light bands, ocean caustics, light through water | a water surface built from crossed low-frequency waves, lit so the caustic bands come from the same field that shapes it rather than sitting on top |
| `chromeGlass` | mirror chrome shapes, reflective chrome torus, shiny metal object | a tumbling torus and a bobbing sphere in mirror chrome, reflecting a studio horizon with a hard specular glint |
| `glassRefract` | glass block, refractive crystal, a diamond or ice cube look | a tumbling block of clear glass. the ray bends going in, crosses the body, and bends again coming out, and the exit is taken three times at three slightly different indices, so edges split into red and blue the way a prism does. thick parts drink the light and take the palette colour. transparent, dispersive, refractive: the crystal, the diamond, the ice cube |
| `holoFoil` | holographic foil, iridescent foil disc, rainbow foil sheen | a rippling disc of foil: thin-film interference over bright metal, the hue turning with viewing angle. the band is deliberately NARROW, so it reads as one colour sliding rather than a rainbow, and it is bounded to a disc so it keeps a silhouette |
| `mandelbulb` | 3D fractal, a mandelbulb shape, breathing fractal | the mandelbulb fractal, depth-shaded near-to-far, its exponent breathing between 5.5 and 8.5 on a slow sine |
| `metaballs` | glossy candy blob, merging spheres, soft blob of orbiting spheres | five spheres orbiting and smooth-union-ing into one blob of soft glossy candy, two palette stops shading it top to bottom |

## resample fx  `[resample.fx]`

| name | words | action |
|---|---|---|
| `bitCrush` | color banding, reduce bit depth, palette degrade | quantise the palette down until it bands, each 0.25 of `amount` halving the bit depth, a degrade beat, never decoration |
| `chromaShift` | radial color split, rgb separation from centre, chromatic shift | radial RGB separation, the channels pulling apart from the centre outwards |
| `directionalBlur` | straight line smear, motion blur at an angle, linear blur | a straight-line smear at a fixed `angle` (degrees, 0 = rightward), the same distance everywhere in the frame, unlike `zoomBlur` which radiates from the centre. This is the AFTER EFFECTS "Directional Blur": a look an author SETS, not a byproduct of a layer's own travel speed (that one is automatic, see `engine-doctrine/CRAFT/AFTER-EFFECTS-TECHNIQUES.md` #4). `count` trades sample quality for cost (2..32, default 16) |
| `dissolve` | burn away, erode into nothing, ember dissolve | noise-thresholded erosion lit by an ember front. The way OUT of an image; ramp `amount:[0.05, 0.95]` to burn it away |
| `fisheye` | lens distortion, barrel distortion, pincushion distortion | real lens distortion: barrel above the middle of the dial, pincushion below, `0.5` the identity. Outside the source reads empty, never a stretched edge |
| `macroblock` | video glitch blocks, codec artefact, compression glitch | the flat blocks and dropped tiles of a starved codec. A glitch/degrade beat, never decoration |
| `refract` | glass distortion, liquid glass bend, refraction warp | liquid glass: the image BENDS along a noise gradient with per-channel dispersion and a specular glint, what a blur cannot do |
| `spinBlur` | rotation blur, spinning smear, pivot blur | smear along the arc with the radius preserved, so the pivot itself stays sharp, a rotating badge or seal. `cx`/`cy` move the pivot off screen-middle (0..1, default 0.5/0.5); `count` trades sample quality for cost (2..32, default 16) |
| `zoomBlur` | radial blur, zoom impact blur, speed zoom | radial smear out from a centre, near samples kept crisp. An impact moment; ramp `amount:[0.6, 0]` so the frame rushes in and snaps sharp. `cx`/`cy` move the centre off screen-middle (0..1, default 0.5/0.5); `count` trades sample quality for cost (2..32, default 16) |

## scramble charset  `[presetOpts.chars]`

| name | words | action |
|---|---|---|
| `binary` | ones and zeros, binary code scramble, matrix style | ones and zeros, the loudest cliche in the set, use it once and only where the subject IS binary |
| `blocks` | block shading, ascii blocks, noise bar | four shades of block, so the word dissolves into a bar of noise instead of into other letters |
| `lowerCase` | lowercase scramble, small letters only | lowercase only, quieter still, and the right set under a lowercase headline |
| `mixed` | default scramble, random characters, letters and numbers | capitals, digits and four symbols, the house default and the busiest of the sets |
| `numbers` | digits only, numeric scramble, counting up scramble | digits only, for a counter, a price, a code or anything the film is about to state as a number |
| `symbols` | punctuation scramble, cipher characters, terminal symbols | punctuation and operators, a terminal or a cipher rather than a word |
| `upperCase` | all caps scramble, capital letters only, uppercase only | capitals only, the calmest scramble and the one that keeps a headline reading as type |

## seam fx  `[seam]`

| name | words | action |
|---|---|---|
| `barnDoor` | barn doors seam, centre split reveal, doors open transition | two doors split from the centre outward along dir, opening onto the next beat. A confident, symmetric reveal |
| `blindsWipe` | venetian blinds seam, slatted wipe, blinds transition | slatted blinds sweep across the frame along dir, revealing the next beat. A crisp graphic wipe |
| `burnThrough` | film burn seam, ember burn transition, burn through cut | the frame burns away along a noisy front with a hot ember edge, revealing the next beat through the fire. A dramatic organic pivot |
| `cinematicZoom` | push in zoom, dolly zoom transition, dive in zoom | the outgoing beat zooms in up to 1.25x intensity while the incoming beat zooms out from 1.25x, a dive-in push into a screen |
| `clockWipe` | clock hand sweep, radial clock reveal, timed wipe | a hand sweeps around the frame like a clock, wiping the next beat in behind it with a soft glowing edge. A timed, mechanical reveal |
| `crossWarp` | warp dissolve, noise warp cut, drag and swap | both beats drag toward the centre and swap through a soft noise front. A wipe with grit, for organic brands and dark scenes |
| `dispersion` | prism split transition, chromatic dispersion, colour split seam | prism channel-split across the seam along a seeded axis, peaking mid-way, optical, techy pivots |
| `dissolve` | film dissolve, grainy dissolve, noise dissolve | grainy film dissolve, each pixel flipping as a noise front passes it, time/place change |
| `fade` | cross dissolve, crossfade, fade transition | flat cross-dissolve of both beats. The universal fallback every seam degrades to with no WebGL or a blank raster |
| `flashWhite` | flash to white seam, white flash cut, flash transition | flashes to solid white at the midpoint (p=0.5) then reveals the arriving beat, an energy pivot |
| `irisRound` | circle iris seam, spotlight reveal transition, round iris open | a soft circle opens from the centre with a bright rim, irising the next beat into view. A classic spotlight reveal |
| `lens` | lens bend transition, optical warp seam, flare lens transition | one moving optical centre bends BOTH beats through a single lens, with a warm flare. Premium product glamour, dark scenes |
| `lumaWipe` | luminance wipe, brightness wipe, luma reveal | the next beat bleeds through the darkest parts of the current one first, along a soft noisy threshold. A cinematic dissolve with grain |
| `pixelDissolve` | pixelate dissolve, mosaic transition, blocky dissolve | the frame coarsens into big pixels at the midpoint then sharpens into the next beat. A retro digital dissolve |
| `portal` | wormhole, tunnel reveal, vortex transition | a glowing ring expands once to reveal the next beat, with up to 0.14x intensity of warp bend at its edge |
| `push` | push transition, shove cut, directional push | both beats shove together toward dir, the arriving one following the leaving one off screen, dir-aware basic |
| `rippleWave` | water ripple transition, concentric wave dissolve, ripple seam | concentric waves ripple out from the centre as the frame dissolves, like a drop hitting water. Soft, calming links |
| `sdfIris` | shaped iris reveal, polygon iris, star iris | the arriving beat revealed through an expanding seeded polygon iris (star, hex, diamond or triangle) with a bright rim. Playful reveal, the shape is the personality |
| `shatterGlitch` | digital shatter, rgb tear glitch, glitch pivot | the frame breaks into blocks whose RGB channels tear apart and flicker, then snap back clean. A hard digital pivot |
| `slide` | slide cover, slide transition, push aside | the arriving beat slides in over a held outgoing one (cover), dir-aware, the basic every tool has |
| `spinZoom` | spin and scale transition, kinetic spin cut, rotate zoom seam | the outgoing beat spins and scales out while the incoming beat spins in. A kinetic whip between shots |
| `swirlWarp` | vortex twist transition, swirl warp seam, dreamy twist cut | a vortex twists the centre of the frame and unwinds into the next beat, strongest mid-cut. Organic, dreamy pivots |
| `uncover` | pull away reveal, uncover transition, slide off reveal | the leaving beat slides off toward dir and reveals a held arriving beat under it, dir-aware basic |
| `whipPan` | whip pan transition, momentum swipe, motion blur pan | a 6-sample motion-blurred smear thrown toward dir, offset up to 0.9x intensity, momentum carried between beats |
| `wipe` | soft wipe, sweep reveal, line wipe | a soft-edged line sweeps toward dir, the arriving beat revealed behind it, playful, "notice the cut" |
| `zoomBlur` | radial blur push, zoom push transition, blur punch cut | both beats streak toward the centre in a radial blur and cross-dissolve. A punchy push between shots |

## shadow direction  `[shadow.direction]`

| name | words | action |
|---|---|---|
| `bottom` | shadow falls down, southward shadow | away is downward: the frame drains towards the bottom edge and stays lit at the top. The default bearing |
| `bottom-left` | southwest shadow, shadow falls bottom left | a diagonal fall away to the bottom left, light rarely leaves along an axis, and no edge keyword says this |
| `bottom-right` | southeast shadow, shadow falls bottom right | a diagonal fall away to the bottom right, light rarely leaves along an axis, and no edge keyword says this |
| `center` | shadow in every direction, radial shadow | a radial: away in EVERY direction at once, so no edge can darken without the others darkening too |
| `left` | westward shadow, shadow falls left | a linear fall away to the left. A bearing and no centre, so only the move along it reaches the fall |
| `left-and-right` | lit vertical band, dark on both sides | a lit vertical band with darkness at both sides and nothing taken off the top or bottom |
| `right` | eastward shadow, shadow falls east | a linear fall away to the right: the frame drains toward the right edge and stays lit on the left |
| `top` | shadow falls up, northward shadow | away is upward: the frame drains towards the top edge and stays lit at the bottom |
| `top-and-bottom` | lit band across the middle, low sun band | a lit band across the middle with darkness above and below it and nothing taken off the sides, the shape a low sun makes |
| `top-left` | northwest shadow, shadow falls top left | a diagonal fall away to the top left, light rarely leaves along an axis, and no edge keyword says this |
| `top-right` | upper right corner, northeast shadow | a diagonal fall away to the top right, light rarely leaves along an axis, and no edge keyword says this |

## spectacle device  `[spectacle.device]`

| name | words | action |
|---|---|---|
| `chromaticSplit` | RGB split, color channel tear, glitchy color split | the frame tears into red/green/blue and snaps back, impact, energy, a hard landing |
| `cinematicZoom` | a fast push with blur, a lunge zoom | a fast push with the blur that comes off it, the frame lunging at the subject |
| `dispersion` | a prism split, light separating into color | the picture separates into its spectrum and reassembles, glass, prisms, luxury |
| `flash` | a white flash, a bright pop over everything | a single bright bloom over the whole frame, up and gone, the plainest peak there is |
| `glitch` | digital glitch, system breakage effect, stepped shear | a stepped horizontal shear, no smoothing. Alarm, breakage, a system under load |
| `iridescence` | an oil-slick sheen, a rainbow sheen wash | an oil-slick sheen washes across the frame once, colour as the event |
| `lens` | a lens bulge, a fisheye bend | a wide optical bulge and release. The frame bending under the weight of the moment |
| `ripple` | a shockwave ring, a ripple from impact | a ring travels out from the centre and distorts what it crosses, an impact you can watch spread |
| `sdfIris` | an iris wipe, a shutter reveal | a hard iris opens from the centre. A shutter on the moment, theatrical and exact |
| `streak` | a light streak, a specular sweep | a bright bar sweeps the frame. A specular pass over a mark, the cheapest premium peak |
| `vortex` | a spinning twist, a whirlpool distortion | the frame twists about its centre and unwinds, the loudest of the radial family |
| `whipPan` | a whip pan, a camera smear | the frame smears sideways as if the camera whipped to it, motion the picture cannot carry alone |

## stagger order  `[stagger.from]`

| name | words | action |
|---|---|---|
| `center` | middle out, from the middle, outward both ways, centre out | starts at the middle unit and opens outward both ways, so the word arrives as ONE object rather than as a train |
| `edges` | both ends, closing like a door, inward, converge on the middle | starts at BOTH ends and closes on the middle, a line that shuts like a door |
| `first` | left to right, in order, typing order, front to back | the wave starts at the first unit and runs to the last, the default, and what a line of type being typed looks like |
| `last` | right to left, reverse order, backwards, back to front | starts at the last unit and runs backwards to the first, pair it with a right-to-left exit |
| `random` | shuffled, scattered, seeded random, out of order | a hashed, seeded shuffle of the order, scattered arrival that is identical on every render and at every seek |
| `typewriter` | typing effect, one character at a time, chars per second, letters appear one by one | types one character at a time at a fixed TYPING RATE (`cps`, chars/sec) instead of a shared budget, so any split layer can reveal char by char at a chosen speed and still use a preset, a colour ramp, or a reversed exit |

## sting fx  `[sting]`

| name | words | action |
|---|---|---|
| `blinds` | venetian blinds, blinds transition, window blinds | venetian bars opening together, 14 bars sweeping in lockstep. Mechanical and rhythmic. |
| `bokeh` | bokeh drift, out of focus discs, defocus bokeh | soft out-of-focus discs drifting across frame. Ambient and quiet: closer to a texture than a cut. |
| `burn` | film burn, burn transition, flare burn | a film burn: the frame chars through and blows out, the way heat eats a print. Warm, analogue, and loud. |
| `chromaticSplit` | chromatic aberration, colour fringe ring, rgb fringe shock | an rgb-fringed shock ring with colour fringing at the edges. An impact with a lens defect. |
| `cinematicZoom` | dolly punch in, cinematic zoom cut, zoom cut cover | a dolly punch-in used as a cut cover: the camera lunges forward and the join hides inside the lunge. |
| `circle` | circle reveal, iris expand, disc reveal | a disc expanding from the centre. Focus opening outward from one point. |
| `confetti` | confetti burst, celebration burst, party confetti | a burst of coloured pieces thrown across frame. Celebration, and hard to use without looking cheap. |
| `crossWarp` | cross warp, directional veil, noise smear | a noise-smeared directional veil dragging the frame sideways as it goes. |
| `dispersion` | prism dispersion, spectral band, light split sweep | a spectral prism band sweeping the frame, splitting light into its colours as it passes. |
| `dissolve` | dissolve to white, white dissolve, wash to white | the frame dissolves away to white. A clean act break when the next beat starts bright. |
| `domainWarp` | liquid marble, domain warp wash, marble noise | a liquid marble wash, noise folded through noise. Organic, slow, and unlike anything geometric here. |
| `doors` | barn doors close, panel doors, doors close | panels closing in from both sides to meet in the middle. |
| `flash` | flash to white, white flash cut, hard flash | a single hard white flash across the whole frame. The bluntest cut cover there is, and the one to use when the two shots have nothing in common. |
| `glitch` | digital glitch, rgb split, channel split glitch | rgb channel split into displaced bars. Digital failure, interference, a system under strain. |
| `grain` | film grain, grain wash, noise texture | dense fine film grain rising over the frame. Texture rather than event: use it under something else. |
| `gridPixelateWipe` | pixelated wipe, grid pixelate, blocky diagonal wipe | chunky pixel blocks sweeping a diagonal, each block a quantised tint. A wipe and a pixelate at once. |
| `ink` | ink bleed, ink spread, bleed to black | an ink bleed spreading to near-black. The dark twin of dissolve, for a beat that gets heavier. |
| `iridescence` | iridescent sheen, oil slick shimmer, thin film shimmer | a thin-film interference sheen, the colour of oil on water shifting with angle. |
| `leak` | light leak, light leak transition, lens leak | a light leak, seed-generative and multi-hue. Colour bleeding in at the edges as if the camera back opened. |
| `lens` | anamorphic flare, lens flare, streak flare | a full anamorphic flare: hot core, ghosts down the axis, a horizontal streak. Cinematic and very loud. |
| `pinwheel` | pinwheel sweep, rotating arms, windmill sweep | an angular sweep with three arms rotating around the centre. |
| `pixel` | pixelate, mosaic transition, pixel mosaic | a chunky mosaic that fills the frame and then clears. Reads as resolution being lost and regained. |
| `polka` | polka dots, dot curtain, dot reveal | a curtain of dots growing until they cover the frame. |
| `ridgedBurn` | ember burn, paper burn, ridged fire edge | a filament ember front sweeping upward, like paper catching along a ragged edge. |
| `ripple` | ripple transition, water ripple, concentric rings | concentric rings expanding across the whole frame, like a struck water surface. |
| `scan` | scanline sweep, crt scan, monitor scan | a CRT scanline band sweeping through, with a trailing glow. Terminal, monitor, surveillance. |
| `sdfIris` | shaped iris, seeded iris wipe, custom aperture | an iris wipe through a seeded shape, so the aperture is not simply a circle. Focus, with character. |
| `squares` | grid reveal, square tiles, mosaic grid | grid cells filling in on a stagger. Modular, and it suits a layout that is already a grid. |
| `streak` | light streaks, lens streak, radial streak | sharp radial light rays from a hot core. Photographic rather than digital: a lens catching a source. |
| `swirl` | swirl transition, rotating light streaks, spin streaks | 8 rotational light streaks thrown out from the centre, spinning up to a full turn. |
| `thermal` | thermal vision, heat map veil, iron bow heat | an iron-bow heat veil in coarse cells, colour mapped from luminance rather than painted on. |
| `vortex` | ink vortex, spiral pull, swallow spiral | an ink spiral pulling inward to a dark eye. A beat being swallowed rather than ended. |
| `warp` | shock ring, refraction warp, impact warp | a barrel-warped refraction shock ring expanding outward. An impact felt through the lens. |
| `whipPan` | whip pan streaks, camera whip, motion smear pan | horizontal smear streaks racing across, as if the camera whipped sideways. Energy carried into a payoff. |
| `wipe` | directional wipe, band wipe, sweep wipe | a directional band sweeping across the frame. The plainest geometric transition, and it always reads. |

## theme look key  `[theme.look]`

| name | words | action |
|---|---|---|
| `backdrop` | brand background rotation, which bg presets to use | ordered bg preset names the brand turns through, one window per beat: planning-only, seeds a storyboard's `bg[]` plan, never read at render (bg is required, engine-doctrine/MISTAKES.md #159) |
| `cuts` | default transition style, brand cut preference | the default and accent cut/transition names the brand favours, one for almost every boundary and one reserved for its peak-energy beat |
| `field` | backdrop texture defaults, grain and vignette settings | grain and vignette numbers layered over the backdrop, both 0..1 strengths |
| `layout` | anchor and margin, where content sits on the frame | the anchor band (left/center/right) and margin every beat composes against |
| `marks` | logo settings, brand mark sizes | the logo path plus its end-card and headline-adjacent sizes, both named pixel numbers |
| `scale` | type scale, named text sizes | how big text should be: named px sizes for the hook / headline / body / caption roles at 16:9, so a layer writes `"size": "headline"` instead of guessing a number |

## three scene  `[three]`

| name | words | action |
|---|---|---|
| `codeAssemble` | code assembling from a point cloud, glyphs flying into place, text forming from scattered points | thousands of GPU points flying from a seeded cloud to the exact glyph positions and resolving into readable code |
| `codeDissolve` | code resolving from noise, code fading in through static, a chromatic burn reveal of code | code resolving out of seeded noise behind a chromatic burn edge, then holding crisp |
| `codeExtrude` | code rising out of depth, syntax-highlighted 3D slabs, code growing into blocks | syntax-coloured code on lit beveled slabs, rising out of depth and growing to thickness, top line first |
| `deviceShowcase` | a turning phone or laptop, device with a live screen, 3D product device | a GLTF device body turning in real light with a LIVE HTML screen mapped onto it |
| `extrudeText` | 3D extruded text, thick lettering that rotates, a font turned into a solid | a real font outline extruded into a lit 3D solid that rotates through space |
| `globe` | a spinning 3D globe, a rotating world with arcs, a dotted world map in 3D | a rotating globe of land dots, with arcs available between coordinates |
| `liquidBackground` | a churning liquid surface, a rippling 3D plane, a wavy specular background | a subdivided plane churning under summed sine displacement, specular highlights sliding across the swells |
| `litPlane` | a lit UI screenshot in 3D, a captured screen rising with a tilt, a shadowed product screen | a captured UI plane, LIT (MeshStandardMaterial under the studio rig, not MeshBasicMaterial), rising with a tilt and a rotation into a soft-shadowed ground with an overshoot settle; `motionBlur` (true, or a 0..1 strength, the same word every layer uses) opts it into a shutter-accumulated smear on the fast rise |
| `magnetic` | magnetic field lines, field lines between poles, charges sliding along arcs | field lines arcing from pole to pole, traced from the real summed inverse-square field, with charges sliding along them |
| `pointCloud` | a cloud of points, particle point cloud, thousands of lit dots | a GPU point cloud: thousands of lit points posed absolutely from t |
| `shatter` | breaking into pieces, a slab shattering, shards flying outward | one solid slab holds, then breaks into a seeded grid of shards that tumble outward and toward camera |
| `uiParallax` | UI layers at depth, parallax screens, stacked flat panels in 3D | flat UI planes stacked at depth, the camera moving past them so the layers separate |

## time remap  `[timeRemap]`

| name | words | action |
|---|---|---|
| `freeze` | freeze frame, hold the last pose, stop on the last frame | FREEZE FRAME: the layer plays out over the first 60% of its window, then holds dead still on its own last pose for the rest of it |
| `hold` | slow motion hold, slo-mo dip, ramp down to slow | the slow-motion HOLD: in fast, down to 30% speed across the middle half, out fast. The part you want the eye to actually read sits in the slow part |
| `rewind` | reverse, backwards, boomerang, ping-pong, play in reverse | plays to the end at double speed, then runs itself BACKWARDS to where it started. One layer, one window, and the return is the same motion reversed |
| `whip` | speed ramp, crawl then whip, fast-slow-fast clock | SPEED RAMP: crawls at 20% speed, whips through the middle at about 530%, lands slow. The AE three-key ramp, for a dull passage you want crossed fast |

## transition relationship  `[transition_why]`

| name | words | action |
|---|---|---|
| `act-break` | beginning or ending, open or close the film, section break | a beginning or an ending, an act break: open or close the film or a major section. Candidates: fade. |
| `continuity` | no transition, one continuous thought, invisible cut | nothing: invisible, respects momentum. Two beats are one continuous thought. Candidates: none. |
| `contrast` | shock cut, jolt, wake from a dream | shock, jolt: end on maximum tonal contrast; wake from a dream. Candidates: none, flashWhite. |
| `new-place-energy` | meanwhile cut, location change, energetic scene change | frantic energy, momentum, "meanwhile": an energetic location or time change. Candidates: whipPan, wipe. |
| `rhyme` | visual rhyme, these two things are the same, compositional echo | "these two things are the same": bridge scenes by a visual or compositional rhyme. Candidates: none. |
| `same-action` | match on action, cut on a movement, carry the motion through | seamless, energy carried through: cut on a movement so the eye rides it past the seam. Candidates: match-on-action. |
| `same-object` | shared element, morph between states, the same thing changing | magic: the same identity across states, the highest-craft continuity there is. Candidates: shared-element morph. |
| `spatial-travel` | walk the space, camera travels the world, one continuous place | one world: the beats are PLACES, not claims. The content has a spatial logic worth walking. Candidates: camera travel. |
| `time` | passage of time, dissolve between images, time passing | passage of time, a connection, gentleness: link two images, soften, show time passing. Candidates: dissolve, fade. |

---
_718 primitives across 61 registries, 718 meeting the word-action contract today.
Regenerate: `make vocab`. Ratchet: `make check GATE=word-action`._
