{
  "name": "vawe",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Data-driven faceless Shorts/Reels engine — self-contained HTML/CSS format folders, GPU render, ffmpeg grain + audio.",
  "scripts": {
    "setup:audio": "node scripts/media/gen-audio.mjs",
    "verify": "node verify/run.js",
    "look": "node scripts/author/preview.mjs"
  },
  "//": "Rendering is the Go service: `make render M=<format>` / `make all` (see Makefile).",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "puppeteer": "^23.6.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "d3-geo": "^3.1.1",
    "fontkit": "^2.0.4",
    "three": "^0.185.1",
    "wawoff2": "^2.0.1"
  }
}
