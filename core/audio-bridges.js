// core/audio-bridges.js: thin re-export. Moved to core/audio/bridges.js as part of the audio/
// package (W9). Kept so every existing `from './audio-bridges.js'` import keeps working unchanged.
export * from './audio/bridges.js';
