// core/transitions.js: thin re-export. Moved to core/transitions/catalog.js as part of the
// transitions/ package (W9). Kept so every existing `from './transitions.js'` import keeps
// working unchanged.
export * from './transitions/catalog.js';
