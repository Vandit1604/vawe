// core/energy.js: thin re-export. Moved to core/transitions/energy.js as part of the
// transitions/ package (W9). Kept so every existing `from './energy.js'` import keeps working
// unchanged.
export * from './transitions/energy.js';
