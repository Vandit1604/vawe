// core/resample.js: thin re-export. Moved to core/resample/index.js as part of the resample/
// package (W9). Kept so every existing `from './resample.js'` import keeps working unchanged.
export * from './resample/index.js';
