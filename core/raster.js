// core/raster.js: thin re-export. Moved to core/resample/raster.js as part of the resample/
// package (W9). Kept so every existing `from './raster.js'` import keeps working unchanged.
export * from './resample/raster.js';
