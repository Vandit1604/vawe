// core/transitions-lower.js: thin re-export. Moved to core/transitions/lower.js as part of the
// transitions/ package (W9). Kept so every existing `from './transitions-lower.js'` import keeps
// working unchanged.
export * from './transitions/lower.js';
