// core/audio-kit.mjs: thin re-export. Moved to core/audio/kit.mjs as part of the audio/ package
// (W9). Kept so every existing `from './audio-kit.mjs'` import keeps working unchanged.
export * from './audio/kit.mjs';
